global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  # OTel Collector에서 메트릭 수집
  - job_name: 'otel-collector'
    static_configs:
      - targets: ['otel-collector.railway.internal:8889']
