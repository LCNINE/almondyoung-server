# 크론 «주기당 한 번» 선점 (`@app/cron-once`) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 한 앱의 ECS 태스크가 몇 개 떠 있든 크론 한 주기가 클러스터 전체에서 정확히 한 번만 실행되게 하고, 새 크론이 이 규칙을 빠뜨릴 수 없게 게이트로 막는다 (#821).

**Architecture:** 새 라이브러리 `@app/cron-once` 가 `@CronOnce(expr, { name })` 데코레이터 · 탐색기(`DiscoveryService` 로 찾아 `SchedulerRegistry` 에 등록) · 선점기(앱 DB 의 `cron_runs(name, period_at)` PK 에 `INSERT … ON CONFLICT DO NOTHING RETURNING` 으로 주기 선점) 를 제공한다. 앱 7개의 `@Cron` 40개를 `@CronOnce` 로 바꾸고, CAS 로 이미 안전한 core 폴러 4개만 마커를 달아 남긴다. 가드 스펙이 마커 없는 `@Cron` · 이름 중복 · 모듈 누락을 막는다.

**Tech Stack:** NestJS 11, `@nestjs/schedule` 6.1 (`SchedulerRegistry`), `cron` 4.4 (`CronJob.from`), `cron-parser` 4.9 (`prev()`), Drizzle ORM 0.44 + postgres.js (raw `sql`), Jest.

**Spec:** `docs/superpowers/specs/2026-09-10-cron-once-design.md`

## Global Constraints

- 새 라이브러리는 손으로 만들지 않고 `nest g lib cron-once --no-spec` 로 만든다 (CLAUDE.md).
- `@CronOnce` 의 `name` 은 필수이고 앱 안에서 유일하다. 받는 옵션은 `name`·`timeZone` 뿐이다.
- 주기 키 = `cron-parser` 의 `parseExpression(expr, { currentDate: now + 50ms, tz }).prev()`. 벽시계가 아니다.
- 선점 SQL 은 단일 왕복이며 `period_at` 은 **ISO 문자열**로 바인딩한다 (raw `sql` 에 `Date` 를 넘기면 매 호출 TypeError).
- `claimed_by` = provider 토큰 `CRON_ONCE_INSTANCE_ID` 의 값, 없으면 `os.hostname()`.
- 선점 쿼리 실패 = 건너뛰고 `error` 로그 (fail closed).
- 보존 기간 7일. 같은 이름의 오래된 행을 선점 문장의 CTE 로 지운다.
- `cron_runs` 는 `public` 스키마, PK `(name, period_at)`. 정의는 `libs/cron-once/src/cron-runs.schema.ts` 한 곳.
- 남기는 `@Cron` 4개(core CAS 폴러)는 바로 윗줄에 `// cron-overlap-safe: <근거>` 마커가 있어야 한다.
- 마이그레이션은 additive 이므로 배포는 `migrate → deploy`. user-service 는 `lcnine-auth` 배포.
- 검증 게이트: `npm run type-check` 에러 0, `npx jest --maxWorkers=2` 실패 0.
- 통합 스펙은 `REQUIRE_CRON_ONCE_DB=1` 가드 + `DATABASE_URL` 로만 켜진다. 스펙 안에서 `dotenv.config()` 금지.
- 커밋 메시지 끝에 `Claude-Session: https://claude.ai/code/session_01Wc3bLKCeRbM6pzjspyK2zt` 를 붙인다.
- 작업 브랜치는 `feat/821-cron-once` (이미 체크아웃됨).

---

## 파일 구조

```
libs/cron-once/
  src/
    index.ts                       # 공개 표면 re-export
    cron-once.constants.ts         # 메타데이터 키, CRON_ONCE_INSTANCE_ID 토큰, 옵션/메타 타입
    cron-once.decorator.ts         # @CronOnce — 메타데이터만 남긴다
    period-key.ts                  # computePeriodAt(expr, now, tz) 순수 함수
    period-key.spec.ts
    cron-runs.schema.ts            # drizzle 테이블 정의 (앱 7개 drizzle.config 가 문다)
    cron-run.claimer.ts            # CronRunClaimer — claim/finish raw SQL
    cron-run.claimer.spec.ts
    cron-once.runner.ts            # CronOnceRunner — 주기 키 → 선점 → 본문 → 완료, 로그·오류 정책
    cron-once.runner.spec.ts
    cron-once.explorer.ts          # CronOnceExplorer — discovery → CronJob → SchedulerRegistry
    cron-once.explorer.spec.ts
    cron-once.module.ts            # @Global CronOnceModule
    cron-once.integration.spec.ts  # 앱 컨텍스트 둘 경쟁 (REQUIRE_CRON_ONCE_DB)
    guards/
      cron-once-guard.ts           # findGuardViolations(files) 순수 함수
      cron-once-guard.spec.ts      # 픽스처 단위 + 저장소 전수
  tsconfig.lib.json
apps/<7개>/drizzle.config.ts       # schema 목록에 cron-runs.schema.ts 추가
apps/<7개>/<drizzle out>/<ts>_add-cron-runs.sql + meta/
apps/<7개>/src/<root>.module.ts    # CronOnceModule import
apps/<7개>/src/**/*.ts             # @Cron → @CronOnce (40곳), 마커 (4곳)
scripts/local/test-core-integration-local.sh  # 기본 패턴에 libs/cron-once 추가
docs/adr/0036-cron-runs-once-per-period-by-claim.md
CLAUDE.md                          # Architecture 절에 한 단락
```

---

### Task 1: 라이브러리 뼈대

**Files:**
- Create (CLI): `libs/cron-once/src/index.ts`, `libs/cron-once/src/cron-once.module.ts`, `libs/cron-once/tsconfig.lib.json`
- Modify (CLI): `nest-cli.json`, `tsconfig.json`
- Modify: `package.json` (jest `moduleNameMapper`)
- Delete: `libs/cron-once/src/cron-once.service.ts` (CLI 가 만드는 빈 서비스)
- Create: `libs/cron-once/src/cron-once.constants.ts`

**Interfaces:**
- Produces: `CRON_ONCE_METADATA`, `CRON_ONCE_INSTANCE_ID`, `CronOnceOptions`, `CronOnceMetadata` (아래 코드 그대로)

- [ ] **Step 1: CLI 로 라이브러리 생성**

```bash
npx nest g lib cron-once --no-spec
```

`nest-cli.json` 의 `defaultLibraryPrefix` 가 `@app` 이라 prefix 프롬프트는 뜨지 않는다. 뜨면 `@app` 을 입력한다.

- [ ] **Step 2: 생성 결과 확인**

```bash
git status --short
cat libs/cron-once/src/index.ts
grep -n '"@app/cron-once' tsconfig.json
python3 -c "import json;print(json.load(open('nest-cli.json'))['projects']['cron-once'])"
```

Expected: `libs/cron-once/src/{index.ts,cron-once.module.ts,cron-once.service.ts}`, `libs/cron-once/tsconfig.lib.json` 이 생겼고, `tsconfig.json` 에 `@app/cron-once` / `@app/cron-once/*` paths, `nest-cli.json` 에 `cron-once` 프로젝트가 있다.

- [ ] **Step 3: 빈 서비스 제거, jest 매퍼 추가**

```bash
git rm -q --cached libs/cron-once/src/cron-once.service.ts 2>/dev/null; rm -f libs/cron-once/src/cron-once.service.ts
```

`package.json` 의 `jest.moduleNameMapper` 에 `@app/db` 줄 바로 앞에 한 줄 추가:

```json
      "^@app/cron-once(|/.*)$": "<rootDir>/libs/cron-once/src/$1",
```

- [ ] **Step 4: 상수 파일 작성**

`libs/cron-once/src/cron-once.constants.ts`:

```ts
/** `@CronOnce` 가 메서드에 남기는 메타데이터 키. */
export const CRON_ONCE_METADATA = 'cron-once:metadata';

/**
 * 선택 provider 토큰. 값이 있으면 `cron_runs.claimed_by` 에 쓰고, 없으면 `os.hostname()`.
 * 통합 스펙이 앱 컨텍스트 둘을 구별하려고 쓴다. 프로덕션 앱은 등록하지 않는다.
 */
export const CRON_ONCE_INSTANCE_ID = 'CRON_ONCE_INSTANCE_ID';

/** 선점 기록 보존 기간(일). 같은 이름의 이보다 오래된 행은 선점 문장이 함께 지운다. */
export const CRON_RUNS_RETENTION_DAYS = 7;

export interface CronOnceOptions {
  /** 선점 키이자 SchedulerRegistry 등록 이름. 앱 안에서 유일해야 한다. */
  name: string;
  /** `@Cron` 의 timeZone 과 같은 의미. 발화와 주기 키 도출 양쪽에 쓴다. */
  timeZone?: string;
}

export interface CronOnceMetadata extends CronOnceOptions {
  expression: string;
}
```

- [ ] **Step 5: index.ts 를 상수만 export 하도록 정리**

`libs/cron-once/src/index.ts`:

```ts
export * from './cron-once.constants';
export * from './cron-once.module';
```

`libs/cron-once/src/cron-once.module.ts` 는 CLI 가 만든 것에서 `CronOnceService` 참조를 지워 빈 모듈로 둔다 (Task 6 에서 채운다):

```ts
import { Module } from '@nestjs/common';

@Module({})
export class CronOnceModule {}
```

- [ ] **Step 6: 타입체크**

```bash
npx tsc --noEmit -p libs/cron-once/tsconfig.lib.json
```

Expected: 에러 0.

- [ ] **Step 7: Commit**

```bash
git add nest-cli.json tsconfig.json package.json libs/cron-once
git commit -m "chore(cron-once): @app/cron-once 라이브러리 뼈대 (#821)

Claude-Session: https://claude.ai/code/session_01Wc3bLKCeRbM6pzjspyK2zt"
```

---

### Task 2: 주기 키 도출 `computePeriodAt`

**Files:**
- Create: `libs/cron-once/src/period-key.ts`
- Test: `libs/cron-once/src/period-key.spec.ts`

**Interfaces:**
- Produces: `computePeriodAt(expression: string, now: Date, timeZone?: string): Date`, `PERIOD_TOLERANCE_MS = 50`

- [ ] **Step 1: 실패하는 테스트 작성**

`libs/cron-once/src/period-key.spec.ts`:

```ts
import { computePeriodAt } from './period-key';

/**
 * 주기 키는 벽시계가 아니라 **크론식에서 도출한 예정 발화 시각**이다. 두 태스크가 각자 발화해도
 * 같은 키를 계산해야 선점 경쟁이 성립한다. `cron-parser` 의 `prev()` 는 초 단위로 «직전» 을
 * 고르므로 정각(`.000`)에 부르면 한 주기 전을 돌려준다 — 50ms 여유가 그걸 막는다 (2026-09-10 실측).
 */
describe('computePeriodAt', () => {
  const at = (iso: string) => new Date(iso);

  it('6필드(초 포함) 크론식: 지연 발화면 현재 주기', () => {
    expect(computePeriodAt('*/10 * * * * *', at('2026-09-10T00:00:00.999Z'))).toEqual(at('2026-09-10T00:00:00.000Z'));
  });

  it('정각 발화(.000)도 현재 주기다 — 50ms 여유', () => {
    expect(computePeriodAt('*/10 * * * * *', at('2026-09-10T00:00:00.000Z'))).toEqual(at('2026-09-10T00:00:00.000Z'));
  });

  it('경계 직전 발화(-10ms)도 다가오는 주기로 본다', () => {
    expect(computePeriodAt('*/10 * * * * *', at('2026-09-09T23:59:59.990Z'))).toEqual(at('2026-09-10T00:00:00.000Z'));
  });

  it('5필드 크론식과 Nest 상수의 앞자리 0 (`0 04 * * *`)', () => {
    expect(computePeriodAt('0 04 * * *', at('2026-09-10T04:00:00.000Z'))).toEqual(at('2026-09-10T04:00:00.000Z'));
    expect(computePeriodAt('*/1 * * * *', at('2026-09-10T04:07:00.300Z'))).toEqual(at('2026-09-10T04:07:00.000Z'));
  });

  it('timeZone 이 있으면 그 시간대의 벽시계로 해석한다 (서울 04:00 = UTC 전날 19:00)', () => {
    expect(computePeriodAt('0 4 * * *', at('2026-09-10T19:00:00.000Z'), 'Asia/Seoul')).toEqual(
      at('2026-09-10T19:00:00.000Z'),
    );
  });

  it('매초 크론식도 초 단위 키를 낸다', () => {
    expect(computePeriodAt('* * * * * *', at('2026-09-10T00:00:03.120Z'))).toEqual(at('2026-09-10T00:00:03.000Z'));
  });
});
```

- [ ] **Step 2: 실패 확인**

```bash
npx jest libs/cron-once/src/period-key.spec.ts
```

Expected: FAIL — `Cannot find module './period-key'`.

- [ ] **Step 3: 구현**

`libs/cron-once/src/period-key.ts`:

```ts
import { parseExpression } from 'cron-parser';

/**
 * 타이머 지터 여유. `cron` 은 setTimeout 으로 발화하므로 경계보다 ms 단위로만 앞설 수 있고,
 * `cron-parser.prev()` 는 초 단위로 «직전» 을 고르므로 정각에는 한 주기 전을 돌려준다.
 * 둘 다 이 여유로 흡수한다. 최소 주기(1초)보다 훨씬 작아야 한다.
 */
export const PERIOD_TOLERANCE_MS = 50;

/**
 * 크론식에서 «지금 발화한 주기의 예정 시각» 을 도출한다. 두 태스크의 시계가 한 주기 미만으로
 * 어긋나면 같은 값을 낸다 — 이것이 `cron_runs` 의 선점 키다.
 */
export function computePeriodAt(expression: string, now: Date, timeZone?: string): Date {
  const iterator = parseExpression(expression, {
    currentDate: new Date(now.getTime() + PERIOD_TOLERANCE_MS),
    ...(timeZone ? { tz: timeZone } : {}),
  });
  return iterator.prev().toDate();
}
```

- [ ] **Step 4: 통과 확인**

```bash
npx jest libs/cron-once/src/period-key.spec.ts
```

Expected: PASS 6건.

- [ ] **Step 5: Commit**

```bash
git add libs/cron-once/src/period-key.ts libs/cron-once/src/period-key.spec.ts
git commit -m "feat(cron-once): 크론식에서 주기 키를 도출한다 — 벽시계가 아니다 (#821)

Claude-Session: https://claude.ai/code/session_01Wc3bLKCeRbM6pzjspyK2zt"
```

---

### Task 3: `@CronOnce` 데코레이터와 `cron_runs` 스키마

**Files:**
- Create: `libs/cron-once/src/cron-once.decorator.ts`
- Create: `libs/cron-once/src/cron-runs.schema.ts`
- Test: `libs/cron-once/src/cron-once.decorator.spec.ts`
- Modify: `libs/cron-once/src/index.ts`

**Interfaces:**
- Consumes: `CRON_ONCE_METADATA`, `CronOnceOptions`, `CronOnceMetadata` (Task 1)
- Produces: `CronOnce(expression: string, options: CronOnceOptions): MethodDecorator` — 메서드 함수 객체에 `CronOnceMetadata` 를 `SetMetadata` 로 남긴다. `cron_runs` drizzle 테이블 `cronRuns`.

- [ ] **Step 1: 실패하는 테스트 작성**

`libs/cron-once/src/cron-once.decorator.spec.ts`:

```ts
import { Reflector } from '@nestjs/core';
import { CRON_ONCE_METADATA, CronOnceMetadata } from './cron-once.constants';
import { CronOnce } from './cron-once.decorator';

describe('@CronOnce', () => {
  const reflector = new Reflector();

  it('메서드에 expression·name·timeZone 메타데이터를 남긴다', () => {
    class Job {
      @CronOnce('0 3 * * *', { name: 'nightly', timeZone: 'Asia/Seoul' })
      async run(): Promise<void> {}
    }
    const meta = reflector.get<CronOnceMetadata>(CRON_ONCE_METADATA, Job.prototype.run);
    expect(meta).toEqual({ expression: '0 3 * * *', name: 'nightly', timeZone: 'Asia/Seoul' });
  });

  it('name 이 비면 데코레이션 시점에 throw 한다', () => {
    expect(() => {
      class Job {
        @CronOnce('0 3 * * *', { name: '' })
        async run(): Promise<void> {}
      }
      return Job;
    }).toThrow(/name/);
  });

  it('메서드 본문은 건드리지 않는다 (호출하면 그대로 실행된다)', async () => {
    class Job {
      calls = 0;
      @CronOnce('* * * * * *', { name: 'tick' })
      async run(): Promise<void> {
        this.calls += 1;
      }
    }
    const job = new Job();
    await job.run();
    expect(job.calls).toBe(1);
  });
});
```

- [ ] **Step 2: 실패 확인**

```bash
npx jest libs/cron-once/src/cron-once.decorator.spec.ts
```

Expected: FAIL — `Cannot find module './cron-once.decorator'`.

- [ ] **Step 3: 데코레이터 구현**

`libs/cron-once/src/cron-once.decorator.ts`:

```ts
import { SetMetadata } from '@nestjs/common';
import { CRON_ONCE_METADATA, CronOnceMetadata, CronOnceOptions } from './cron-once.constants';

/**
 * `@nestjs/schedule` 의 `@Cron` 을 대체한다. 메타데이터만 남기고, 등록·선점은 `CronOnceExplorer`
 * 가 한다. 그래서 `CronOnceModule` 이 앱에 없으면 이 크론은 **조용히 안 돈다** — 가드 스펙
 * (`guards/cron-once-guard.spec.ts`) 이 그 누락을 막는다.
 *
 * `name` 은 선점 키이자 `SchedulerRegistry` 등록 이름이라 필수이고 앱 안에서 유일해야 한다.
 */
export function CronOnce(expression: string, options: CronOnceOptions): MethodDecorator {
  if (!options?.name?.trim()) {
    throw new Error(`@CronOnce('${expression}') requires a non-empty name — it is the claim key.`);
  }
  const metadata: CronOnceMetadata = {
    expression,
    name: options.name,
    ...(options.timeZone ? { timeZone: options.timeZone } : {}),
  };
  return SetMetadata(CRON_ONCE_METADATA, metadata);
}
```

- [ ] **Step 4: 통과 확인**

```bash
npx jest libs/cron-once/src/cron-once.decorator.spec.ts
```

Expected: PASS 3건.

- [ ] **Step 5: 스키마 파일 작성**

`libs/cron-once/src/cron-runs.schema.ts`:

```ts
import { pgTable, primaryKey, timestamp, varchar } from 'drizzle-orm/pg-core';

/**
 * 크론 «주기당 한 번» 선점 기록 (ADR-0036, #821). **각 앱이 자기 DB 의 `public.cron_runs` 를 쓴다.**
 *
 * 이 파일은 크론이 있는 앱 7개(analytics · channel-adapter · core · membership · ugc-service ·
 * user-service · wallet)의 `drizzle.config.ts` 가 **전부 schema 목록에 물고 있다**. 여기를 고치면
 * 그 7개 앱 모두에 마이그레이션이 생긴다 — 하나라도 빠뜨리면 다음 무관한 `db:generate` 가 이 변경을
 * 조용히 그 마이그레이션에 끼워 넣는다 (`libs/events/src/outbox/outbox.schema.ts` 와 같은 관례).
 *
 * `CronRunClaimer` 는 raw SQL 로 이 테이블을 쓴다 — 앱 스키마 타입에 의존하지 않기 위해서다.
 * 따라서 이 정의는 마이그레이션 생성용이고, 컬럼을 바꾸면 claimer 의 SQL 도 같이 고쳐야 한다.
 */
export const cronRuns = pgTable(
  'cron_runs',
  {
    /** `@CronOnce` 의 name. */
    name: varchar('name', { length: 100 }).notNull(),
    /** 크론식에서 도출한 예정 발화 시각 (`computePeriodAt`). */
    periodAt: timestamp('period_at', { withTimezone: true }).notNull(),
    /** 선점한 인스턴스 — 기본 `os.hostname()` (Fargate 에서는 컨테이너 id). */
    claimedBy: varchar('claimed_by', { length: 100 }).notNull(),
    claimedAt: timestamp('claimed_at', { withTimezone: true }).notNull().defaultNow(),
    finishedAt: timestamp('finished_at', { withTimezone: true }),
    /** `ok` | `error`. 선점 후 본문이 끝나면 채운다. */
    outcome: varchar('outcome', { length: 20 }),
  },
  (t) => [primaryKey({ columns: [t.name, t.periodAt] })],
);
```

- [ ] **Step 6: index.ts 갱신**

`libs/cron-once/src/index.ts`:

```ts
export * from './cron-once.constants';
export * from './cron-once.decorator';
export * from './cron-once.module';
export * from './cron-runs.schema';
export * from './period-key';
```

- [ ] **Step 7: 타입체크**

```bash
npx tsc --noEmit -p libs/cron-once/tsconfig.lib.json
```

Expected: 에러 0. (`primaryKey` 배열 반환 형식이 이 drizzle 버전에서 안 맞으면 `(t) => ({ pk: primaryKey({ columns: [t.name, t.periodAt] }) })` 로 바꾼다 — 저장소의 다른 schema 파일에서 `primaryKey(` 를 grep 해 같은 형식을 쓴다.)

- [ ] **Step 8: Commit**

```bash
git add libs/cron-once/src
git commit -m "feat(cron-once): @CronOnce 데코레이터와 cron_runs 스키마 (#821)

Claude-Session: https://claude.ai/code/session_01Wc3bLKCeRbM6pzjspyK2zt"
```

---

### Task 4: `CronRunClaimer` — 선점·완료 raw SQL

**Files:**
- Create: `libs/cron-once/src/cron-run.claimer.ts`
- Test: `libs/cron-once/src/cron-run.claimer.spec.ts`

**Interfaces:**
- Consumes: `DbService` (`@app/db`), `CRON_ONCE_INSTANCE_ID`, `CRON_RUNS_RETENTION_DAYS`
- Produces:
  ```ts
  class CronRunClaimer {
    readonly instanceId: string;
    claim(name: string, periodAt: Date): Promise<boolean>;   // true = 이 인스턴스가 선점
    finish(name: string, periodAt: Date, outcome: 'ok' | 'error'): Promise<void>;
  }
  ```

- [ ] **Step 1: 실패하는 테스트 작성**

`libs/cron-once/src/cron-run.claimer.spec.ts`:

```ts
import { DbService } from '@app/db';
import { CronRunClaimer } from './cron-run.claimer';

/** drizzle `db.execute(sql\`…\`)` 의 postgres.js 결과는 행 배열이다. */
const makeDb = (rows: Record<string, unknown>[]) => {
  const execute = jest.fn().mockResolvedValue(rows);
  const dbService = { db: { execute } } as unknown as DbService;
  return { dbService, execute };
};

/** `sql` 태그 결과에서 SQL 문자열과 파라미터를 꺼낸다 (드라이버 독립적인 최소 검사). */
const queryOf = (execute: jest.Mock, call = 0) => {
  const chunk = execute.mock.calls[call][0] as { queryChunks: unknown[] };
  const text = chunk.queryChunks
    .map((c) => (typeof c === 'object' && c !== null && 'value' in c ? String((c as { value: string[] }).value.join('')) : '?'))
    .join('');
  const params = chunk.queryChunks.filter((c) => !(typeof c === 'object' && c !== null && 'value' in c));
  return { text, params };
};

describe('CronRunClaimer', () => {
  const periodAt = new Date('2026-09-10T00:00:00.000Z');

  it('행이 돌아오면 선점 성공', async () => {
    const { dbService, execute } = makeDb([{ name: 'nightly' }]);
    const claimer = new CronRunClaimer(dbService, 'task-a');
    await expect(claimer.claim('nightly', periodAt)).resolves.toBe(true);
    expect(execute).toHaveBeenCalledTimes(1);
  });

  it('행이 없으면(ON CONFLICT) 선점 실패', async () => {
    const { dbService } = makeDb([]);
    const claimer = new CronRunClaimer(dbService, 'task-a');
    await expect(claimer.claim('nightly', periodAt)).resolves.toBe(false);
  });

  it('선점 문장은 INSERT … ON CONFLICT DO NOTHING RETURNING 이고 보존 기간 purge CTE 를 품는다', async () => {
    const { dbService, execute } = makeDb([{ name: 'nightly' }]);
    await new CronRunClaimer(dbService, 'task-a').claim('nightly', periodAt);
    const { text } = queryOf(execute);
    expect(text).toMatch(/DELETE FROM cron_runs/);
    expect(text).toMatch(/interval '7 days'/);
    expect(text).toMatch(/INSERT INTO cron_runs/);
    expect(text).toMatch(/ON CONFLICT \(name, period_at\) DO NOTHING/);
    expect(text).toMatch(/RETURNING name/);
  });

  it('period_at 은 Date 가 아니라 ISO 문자열로 바인딩한다 (raw sql Date 바인딩 함정)', async () => {
    const { dbService, execute } = makeDb([{ name: 'nightly' }]);
    await new CronRunClaimer(dbService, 'task-a').claim('nightly', periodAt);
    const { params } = queryOf(execute);
    expect(params).toContain('2026-09-10T00:00:00.000Z');
    expect(params.some((p) => p instanceof Date)).toBe(false);
    expect(params).toContain('task-a');
  });

  it('finish 는 finished_at 과 outcome 을 갱신한다', async () => {
    const { dbService, execute } = makeDb([]);
    await new CronRunClaimer(dbService, 'task-a').finish('nightly', periodAt, 'error');
    const { text, params } = queryOf(execute);
    expect(text).toMatch(/UPDATE cron_runs SET finished_at = now\(\), outcome = /);
    expect(params).toEqual(expect.arrayContaining(['error', 'nightly', '2026-09-10T00:00:00.000Z']));
  });

  it('instanceId 가 주입되지 않으면 호스트명을 쓴다', () => {
    const { dbService } = makeDb([]);
    const claimer = new CronRunClaimer(dbService, undefined);
    expect(claimer.instanceId).toBe(require('node:os').hostname());
  });
});
```

- [ ] **Step 2: 실패 확인**

```bash
npx jest libs/cron-once/src/cron-run.claimer.spec.ts
```

Expected: FAIL — `Cannot find module './cron-run.claimer'`.

- [ ] **Step 3: 구현**

`libs/cron-once/src/cron-run.claimer.ts`:

```ts
import { Inject, Injectable, Optional } from '@nestjs/common';
import { DbService } from '@app/db';
import { sql } from 'drizzle-orm';
import { hostname } from 'node:os';
import { CRON_ONCE_INSTANCE_ID, CRON_RUNS_RETENTION_DAYS } from './cron-once.constants';

export type CronRunOutcome = 'ok' | 'error';

/**
 * `cron_runs(name, period_at)` PK 로 «이 주기의 이 크론» 을 한 번만 성립시킨다.
 *
 * raw SQL 인 이유: 앱마다 DbService 의 스키마 타입이 다르고 이 라이브러리는 그 타입을 모른다.
 * 테이블 모양은 `cron-runs.schema.ts` 가 정본이며, 컬럼이 바뀌면 여기 SQL 도 같이 바뀐다.
 *
 * `period_at` 은 ISO 문자열로 넘긴다 — drizzle raw `sql` 에 `Date` 를 넘기면 postgres.js 가
 * 매 호출 TypeError 를 낸다 (`notification/metrics.service.ts:54` 전례).
 */
@Injectable()
export class CronRunClaimer {
  readonly instanceId: string;

  constructor(
    private readonly dbService: DbService,
    @Optional() @Inject(CRON_ONCE_INSTANCE_ID) instanceId?: string,
  ) {
    this.instanceId = instanceId ?? hostname();
  }

  /** true 면 이 인스턴스가 이 주기를 선점했다. 같은 이름의 보존 기간 지난 행을 함께 지운다. */
  async claim(name: string, periodAt: Date): Promise<boolean> {
    const periodIso = periodAt.toISOString();
    const rows = await this.dbService.db.execute(sql`
      WITH purge AS (
        DELETE FROM cron_runs
         WHERE name = ${name}
           AND period_at < ${periodIso}::timestamptz - interval '${sql.raw(String(CRON_RUNS_RETENTION_DAYS))} days'
      )
      INSERT INTO cron_runs (name, period_at, claimed_by)
      VALUES (${name}, ${periodIso}::timestamptz, ${this.instanceId})
      ON CONFLICT (name, period_at) DO NOTHING
      RETURNING name
    `);
    return rows.length > 0;
  }

  async finish(name: string, periodAt: Date, outcome: CronRunOutcome): Promise<void> {
    const periodIso = periodAt.toISOString();
    await this.dbService.db.execute(sql`
      UPDATE cron_runs SET finished_at = now(), outcome = ${outcome}
       WHERE name = ${name} AND period_at = ${periodIso}::timestamptz
    `);
  }
}
```

주의: `sql.raw` 로 넣는 값은 상수 정수 하나뿐이다. 이름·시각·인스턴스는 전부 바인딩 파라미터다.

- [ ] **Step 4: 통과 확인**

```bash
npx jest libs/cron-once/src/cron-run.claimer.spec.ts
```

Expected: PASS 6건. `queryOf` 헬퍼가 drizzle 내부 형식(`queryChunks`)과 안 맞아 실패하면, 대신 `execute.mock.calls[0][0]` 을 `new PgDialect().sqlToQuery(chunk)` (`drizzle-orm/pg-core`) 로 `{ sql, params }` 로 바꿔 검사한다 — 검사 의도(문장 형태·파라미터 타입)는 같다.

- [ ] **Step 5: Commit**

```bash
git add libs/cron-once/src/cron-run.claimer.ts libs/cron-once/src/cron-run.claimer.spec.ts
git commit -m "feat(cron-once): cron_runs 선점·완료 기록 (#821)

Claude-Session: https://claude.ai/code/session_01Wc3bLKCeRbM6pzjspyK2zt"
```

---

### Task 5: `CronOnceRunner` — 주기 키 → 선점 → 본문 → 완료

**Files:**
- Create: `libs/cron-once/src/cron-once.runner.ts`
- Test: `libs/cron-once/src/cron-once.runner.spec.ts`

**Interfaces:**
- Consumes: `CronRunClaimer.claim/finish` (Task 4), `computePeriodAt` (Task 2), `CronOnceMetadata` (Task 1)
- Produces:
  ```ts
  class CronOnceRunner {
    wrap(meta: CronOnceMetadata, body: () => Promise<unknown>, now?: () => Date): () => Promise<void>;
  }
  ```
  반환된 함수가 `CronJob` 의 `onTick` 이 된다. 절대 reject 하지 않는다.

- [ ] **Step 1: 실패하는 테스트 작성**

`libs/cron-once/src/cron-once.runner.spec.ts`:

```ts
import { Logger } from '@nestjs/common';
import { CronRunClaimer } from './cron-run.claimer';
import { CronOnceRunner } from './cron-once.runner';

const makeClaimer = (claimResult: boolean | Error) => {
  const claim = jest.fn(async () => {
    if (claimResult instanceof Error) throw claimResult;
    return claimResult;
  });
  const finish = jest.fn(async () => undefined);
  return { claimer: { claim, finish, instanceId: 'task-a' } as unknown as CronRunClaimer, claim, finish };
};

const META = { expression: '*/10 * * * * *', name: 'probe' };
const NOW = () => new Date('2026-09-10T00:00:00.300Z');
const PERIOD = new Date('2026-09-10T00:00:00.000Z');

describe('CronOnceRunner.wrap', () => {
  let errorSpy: jest.SpyInstance;
  let debugSpy: jest.SpyInstance;
  beforeEach(() => {
    errorSpy = jest.spyOn(Logger.prototype, 'error').mockImplementation(() => undefined);
    debugSpy = jest.spyOn(Logger.prototype, 'debug').mockImplementation(() => undefined);
  });
  afterEach(() => jest.restoreAllMocks());

  it('선점하면 본문을 돌리고 ok 로 마감한다', async () => {
    const { claimer, claim, finish } = makeClaimer(true);
    const body = jest.fn(async () => undefined);
    await new CronOnceRunner(claimer).wrap(META, body, NOW)();
    expect(claim).toHaveBeenCalledWith('probe', PERIOD);
    expect(body).toHaveBeenCalledTimes(1);
    expect(finish).toHaveBeenCalledWith('probe', PERIOD, 'ok');
  });

  it('선점 못 하면 본문을 돌리지 않고 debug 로그만 남긴다', async () => {
    const { claimer, finish } = makeClaimer(false);
    const body = jest.fn(async () => undefined);
    await new CronOnceRunner(claimer).wrap(META, body, NOW)();
    expect(body).not.toHaveBeenCalled();
    expect(finish).not.toHaveBeenCalled();
    expect(debugSpy).toHaveBeenCalledWith(expect.stringContaining('probe'));
  });

  it('본문이 throw 하면 error 로 마감하고 error 로그를 남기되 reject 하지 않는다', async () => {
    const { claimer, finish } = makeClaimer(true);
    const body = jest.fn(async () => {
      throw new Error('boom');
    });
    await expect(new CronOnceRunner(claimer).wrap(META, body, NOW)()).resolves.toBeUndefined();
    expect(finish).toHaveBeenCalledWith('probe', PERIOD, 'error');
    expect(errorSpy).toHaveBeenCalled();
  });

  it('선점 쿼리가 실패하면 건너뛰고 error 로그 (fail closed)', async () => {
    const { claimer } = makeClaimer(new Error('db down'));
    const body = jest.fn(async () => undefined);
    await expect(new CronOnceRunner(claimer).wrap(META, body, NOW)()).resolves.toBeUndefined();
    expect(body).not.toHaveBeenCalled();
    expect(errorSpy).toHaveBeenCalledWith(expect.stringContaining('probe'), expect.anything());
  });

  it('timeZone 이 있으면 주기 키에 반영한다', async () => {
    const { claimer, claim } = makeClaimer(true);
    const seoul = { expression: '0 4 * * *', name: 'nightly', timeZone: 'Asia/Seoul' };
    await new CronOnceRunner(claimer).wrap(seoul, async () => undefined, () => new Date('2026-09-10T19:00:00.200Z'))();
    expect(claim).toHaveBeenCalledWith('nightly', new Date('2026-09-10T19:00:00.000Z'));
  });
});
```

- [ ] **Step 2: 실패 확인**

```bash
npx jest libs/cron-once/src/cron-once.runner.spec.ts
```

Expected: FAIL — `Cannot find module './cron-once.runner'`.

- [ ] **Step 3: 구현**

`libs/cron-once/src/cron-once.runner.ts`:

```ts
import { Injectable, Logger } from '@nestjs/common';
import { CronOnceMetadata } from './cron-once.constants';
import { CronRunClaimer } from './cron-run.claimer';
import { computePeriodAt } from './period-key';

/**
 * `@CronOnce` 메서드를 «주기당 한 번» 콜백으로 감싼다: 주기 키 도출 → 선점 → 본문 → 완료 기록.
 *
 * - 선점 못 함(다른 태스크가 먼저) → 본문 생략, debug.
 * - 선점 쿼리 실패(DB 불통 등) → 본문 생략, error. 본문은 어차피 DB 를 쓰므로 건너뛰는 편이 낫다.
 * - 본문 throw → error 기록 + error 로그. Nest 의 `@Cron` 래퍼와 같이 프로세스를 죽이지 않는다.
 *
 * 반환 함수는 절대 reject 하지 않는다 — `cron` 의 onTick 에서 던져진 예외는 unhandled 가 된다.
 */
@Injectable()
export class CronOnceRunner {
  private readonly logger = new Logger('CronOnce');

  constructor(private readonly claimer: CronRunClaimer) {}

  wrap(meta: CronOnceMetadata, body: () => Promise<unknown>, now: () => Date = () => new Date()): () => Promise<void> {
    return async () => {
      const periodAt = computePeriodAt(meta.expression, now(), meta.timeZone);

      let claimed: boolean;
      try {
        claimed = await this.claimer.claim(meta.name, periodAt);
      } catch (error) {
        this.logger.error(`claim failed, skipping ${meta.name}@${periodAt.toISOString()}`, error);
        return;
      }
      if (!claimed) {
        this.logger.debug(`skip ${meta.name}@${periodAt.toISOString()}: claimed by another instance`);
        return;
      }

      let outcome: 'ok' | 'error' = 'ok';
      try {
        await body();
      } catch (error) {
        outcome = 'error';
        this.logger.error(`${meta.name}@${periodAt.toISOString()} failed`, error);
      }
      try {
        await this.claimer.finish(meta.name, periodAt, outcome);
      } catch (error) {
        this.logger.error(`finish failed for ${meta.name}@${periodAt.toISOString()}`, error);
      }
    };
  }
}
```

- [ ] **Step 4: 통과 확인**

```bash
npx jest libs/cron-once/src/cron-once.runner.spec.ts
```

Expected: PASS 5건.

- [ ] **Step 5: Commit**

```bash
git add libs/cron-once/src/cron-once.runner.ts libs/cron-once/src/cron-once.runner.spec.ts
git commit -m "feat(cron-once): 주기 키 → 선점 → 본문 → 완료 러너 (#821)

Claude-Session: https://claude.ai/code/session_01Wc3bLKCeRbM6pzjspyK2zt"
```

---

### Task 6: `CronOnceExplorer` 와 `CronOnceModule`

**Files:**
- Create: `libs/cron-once/src/cron-once.explorer.ts`
- Modify: `libs/cron-once/src/cron-once.module.ts`
- Modify: `libs/cron-once/src/index.ts`
- Test: `libs/cron-once/src/cron-once.explorer.spec.ts`

**Interfaces:**
- Consumes: `CronOnceRunner.wrap` (Task 5), `CronRunClaimer` (Task 4), `CRON_ONCE_METADATA`
- Produces: `CronOnceModule` (`@Global`, 앱 루트 모듈이 import), `CronOnceExplorer` (`onModuleInit` 에서 탐색·등록, `onApplicationBootstrap` 에서 start)

- [ ] **Step 1: 실패하는 테스트 작성**

`libs/cron-once/src/cron-once.explorer.spec.ts`:

```ts
import { Injectable, Module } from '@nestjs/common';
import { SchedulerRegistry } from '@nestjs/schedule';
import { Test, TestingModule } from '@nestjs/testing';
import { DbService } from '@app/db';
import { SCHEDULE_ROOT } from '@app/shared/schedule/schedule-root';
import { CronOnce } from './cron-once.decorator';
import { CronOnceModule } from './cron-once.module';
import { CronRunClaimer } from './cron-run.claimer';

/** 절대 발화하지 않는 크론식 — 등록만 검사한다. */
const NEVER = '0 0 0 29 2 1';

const fakeDb = { provide: DbService, useValue: { db: { execute: jest.fn().mockResolvedValue([]) } } };

describe('CronOnceExplorer', () => {
  let app: TestingModule | undefined;
  afterEach(async () => {
    await app?.close();
    app = undefined;
  });

  it('@CronOnce 메서드를 이름으로 SchedulerRegistry 에 등록하고 시작한다', async () => {
    @Injectable()
    class Jobs {
      @CronOnce(NEVER, { name: 'alpha' })
      async a(): Promise<void> {}
      @CronOnce(NEVER, { name: 'beta', timeZone: 'Asia/Seoul' })
      async b(): Promise<void> {}
      async notACron(): Promise<void> {}
    }
    @Module({ imports: [SCHEDULE_ROOT, CronOnceModule], providers: [Jobs, fakeDb] })
    class Root {}

    app = await Test.createTestingModule({ imports: [Root] }).compile();
    await app.init();

    const registry = app.get(SchedulerRegistry);
    expect([...registry.getCronJobs().keys()].sort()).toEqual(['alpha', 'beta']);
    expect(registry.getCronJob('alpha').isActive).toBe(true);
  });

  it('같은 이름이 둘이면 부팅에서 throw 한다 (#599 의 «조용한 두 벌» 을 시끄럽게)', async () => {
    @Injectable()
    class A {
      @CronOnce(NEVER, { name: 'dup' })
      async run(): Promise<void> {}
    }
    @Injectable()
    class B {
      @CronOnce(NEVER, { name: 'dup' })
      async run(): Promise<void> {}
    }
    @Module({ imports: [SCHEDULE_ROOT, CronOnceModule], providers: [A, B, fakeDb] })
    class Root {}

    const building = Test.createTestingModule({ imports: [Root] }).compile();
    await expect(building.then((m) => m.init())).rejects.toThrow(/dup/);
  });

  it('등록된 콜백은 러너를 거친다 — 선점 실패면 본문이 안 돈다', async () => {
    const calls: string[] = [];
    @Injectable()
    class Jobs {
      @CronOnce(NEVER, { name: 'gated' })
      async run(): Promise<void> {
        calls.push('ran');
      }
    }
    @Module({ imports: [SCHEDULE_ROOT, CronOnceModule], providers: [Jobs, fakeDb] })
    class Root {}

    app = await Test.createTestingModule({ imports: [Root] }).compile();
    await app.init();
    jest.spyOn(app.get(CronRunClaimer), 'claim').mockResolvedValue(false);

    await app.get(SchedulerRegistry).getCronJob('gated').fireOnTick();
    expect(calls).toEqual([]);

    jest.spyOn(app.get(CronRunClaimer), 'claim').mockResolvedValue(true);
    await app.get(SchedulerRegistry).getCronJob('gated').fireOnTick();
    expect(calls).toEqual(['ran']);
  });
});
```

- [ ] **Step 2: 실패 확인**

```bash
npx jest libs/cron-once/src/cron-once.explorer.spec.ts
```

Expected: FAIL — `CronOnceModule` 에 provider 가 없어 `CronRunClaimer` 를 못 찾거나 등록이 0건.

- [ ] **Step 3: 탐색기 구현**

`libs/cron-once/src/cron-once.explorer.ts`:

```ts
import { Injectable, Logger, OnApplicationBootstrap, OnModuleInit } from '@nestjs/common';
import { DiscoveryService, MetadataScanner, Reflector } from '@nestjs/core';
import { SchedulerRegistry } from '@nestjs/schedule';
import { CronJob } from 'cron';
import { CRON_ONCE_METADATA, CronOnceMetadata } from './cron-once.constants';
import { CronOnceRunner } from './cron-once.runner';

/**
 * `@nestjs/schedule` 의 `ScheduleExplorer` 와 같은 절차로 `@CronOnce` 메서드를 찾아 `CronJob` 으로
 * 만들고 `SchedulerRegistry` 에 등록한다. 콜백은 `CronOnceRunner.wrap` 을 거친다.
 *
 * - 탐색·등록은 `onModuleInit`, 시작은 `onApplicationBootstrap` — Nest 의 orchestrator 가 크론을
 *   mount 하는 시점과 같다. 앱이 다 뜨기 전에 본문이 도는 일을 막는다.
 * - `SchedulerRegistry.addCronJob` 은 같은 이름이 있으면 throw 한다. 모듈이 두 벌 떠서 크론이
 *   두 번 등록되는 사고(#599, 8일 무증상)가 **부팅 실패**가 된다. 의도한 성질이다.
 * - 종료 시 정리는 Nest 의 `SchedulerOrchestrator.onApplicationShutdown` 이 registry 전체를 닫으므로
 *   여기서 따로 하지 않는다.
 */
@Injectable()
export class CronOnceExplorer implements OnModuleInit, OnApplicationBootstrap {
  private readonly logger = new Logger('CronOnce');
  private readonly jobs: CronJob[] = [];

  constructor(
    private readonly discovery: DiscoveryService,
    private readonly scanner: MetadataScanner,
    private readonly reflector: Reflector,
    private readonly registry: SchedulerRegistry,
    private readonly runner: CronOnceRunner,
  ) {}

  onModuleInit(): void {
    for (const wrapper of [...this.discovery.getProviders(), ...this.discovery.getControllers()]) {
      const { instance } = wrapper;
      if (!instance || !Object.getPrototypeOf(instance)) continue;
      if (!wrapper.isDependencyTreeStatic()) continue;
      const prototype = Object.getPrototypeOf(instance) as object;
      for (const methodName of this.scanner.getAllMethodNames(prototype)) {
        const methodRef = (instance as Record<string, unknown>)[methodName];
        if (typeof methodRef !== 'function') continue;
        const meta = this.reflector.get<CronOnceMetadata | undefined>(CRON_ONCE_METADATA, methodRef);
        if (!meta) continue;
        this.register(meta, () => Promise.resolve(methodRef.call(instance)));
      }
    }
    this.logger.log(`registered ${this.jobs.length} cron-once job(s): ${this.jobs.map((j) => j.name).join(', ')}`);
  }

  onApplicationBootstrap(): void {
    for (const job of this.jobs) job.start();
  }

  private register(meta: CronOnceMetadata, body: () => Promise<unknown>): void {
    const job = CronJob.from({
      cronTime: meta.expression,
      onTick: this.runner.wrap(meta, body),
      start: false,
      name: meta.name,
      ...(meta.timeZone ? { timeZone: meta.timeZone } : {}),
    });
    this.registry.addCronJob(meta.name, job);
    this.jobs.push(job);
  }
}
```

- [ ] **Step 4: 모듈 채우기**

`libs/cron-once/src/cron-once.module.ts`:

```ts
import { Global, Module } from '@nestjs/common';
import { DiscoveryModule } from '@nestjs/core';
import { CronOnceExplorer } from './cron-once.explorer';
import { CronOnceRunner } from './cron-once.runner';
import { CronRunClaimer } from './cron-run.claimer';

/**
 * 앱 루트 모듈이 `SCHEDULE_ROOT` 옆에 import 한다. 정적 모듈이라 Nest 11 의 참조 기준 dedupe 에
 * 걸리지 않는다 — 여러 곳에서 import 해도 한 벌이다.
 *
 * 의존: `SchedulerRegistry` 는 전역 `SCHEDULE_ROOT` 가, `DbService` 는 앱의 `DbModule.forRoot` 가
 * export 한다. 이 모듈이 빠지면 `@CronOnce` 는 조용히 안 돈다 — `guards/cron-once-guard.spec.ts`.
 */
@Global()
@Module({
  imports: [DiscoveryModule],
  providers: [CronRunClaimer, CronOnceRunner, CronOnceExplorer],
  exports: [CronRunClaimer],
})
export class CronOnceModule {}
```

`libs/cron-once/src/index.ts` 에 추가:

```ts
export * from './cron-run.claimer';
```

- [ ] **Step 5: 통과 확인**

```bash
npx jest libs/cron-once/src/cron-once.explorer.spec.ts
```

Expected: PASS 3건. `getCronJob('alpha').isActive` 가 cron 4.4 에 없으면 `.running` 을 쓴다 (`node_modules/cron/dist/job.d.ts` 에서 확인).

- [ ] **Step 6: 라이브러리 전체 게이트**

```bash
npx tsc --noEmit -p libs/cron-once/tsconfig.lib.json && npx jest libs/cron-once
```

Expected: 에러 0, PASS.

- [ ] **Step 7: Commit**

```bash
git add libs/cron-once/src
git commit -m "feat(cron-once): 탐색기·모듈 — @CronOnce 를 SchedulerRegistry 에 선점 콜백으로 등록 (#821)

Claude-Session: https://claude.ai/code/session_01Wc3bLKCeRbM6pzjspyK2zt"
```

---

### Task 7: core 전환 — 마이그레이션·모듈·크론 11개·마커 4개

**Files:**
- Modify: `apps/core/drizzle.config.ts` (schema 목록)
- Create: `apps/core/drizzle/<ts>_add-cron-runs.sql`, `apps/core/drizzle/meta/*` (생성)
- Modify: `apps/core/src/app.module.ts` (imports)
- Modify (교체 11곳):
  - `apps/core/src/modules/inventory/replenishment/demand/replenishment-refresh.job.ts:46`
  - `apps/core/src/modules/inventory/core/services/fulfillment-reservation-reconciliation.service.ts:142`
  - `apps/core/src/modules/inventory/core/services/ledger-reconciliation.service.ts:202`
  - `apps/core/src/modules/inventory/core/services/inventory-idempotency.service.ts:78`
  - `apps/core/src/modules/inventory/product-sellable-quantity/services/product-sellable-quantity.service.ts:461`
  - `apps/core/src/modules/inventory/warehouse-transfer/services/transfer-stagnation.monitor.ts:36`
  - `apps/core/src/modules/fulfillment/services/fulfillment-reconciliation.service.ts:195`
  - `apps/core/src/modules/sales-order/services/entrance-password-cleaner.ts:98`
  - `apps/core/src/modules/catalog/operations/bulk-session/services/bulk-session.cleaner.ts:42`
  - `apps/core/src/modules/catalog/operations/bulk-session/services/bulk-image.cleaner.ts:58`
  - `apps/core/src/modules/catalog/operations/bulk-session/services/form-export-job.worker.ts:68` (일일 purge 만)
- Modify (마커 4곳):
  - `apps/core/src/modules/catalog/operations/bulk-session/services/bulk-session-job.worker.ts:34`
  - `apps/core/src/modules/catalog/operations/bulk-session/services/form-export-job.worker.ts:33`
  - `apps/core/src/modules/fulfillment/services/fulfillment-order-reservation-retry.worker.ts:43`
  - `apps/core/src/modules/fulfillment/services/fulfillment-order-creation-backlog.worker.ts:33`

**Interfaces:**
- Consumes: `CronOnce`, `CronOnceModule` (`@app/cron-once`)

- [ ] **Step 1: drizzle config 에 스키마 추가**

`apps/core/drizzle.config.ts` 의 `schema` 배열 끝, `'./libs/events/src/tracking/tracking.schema.ts'` 다음 줄에:

```ts
    './libs/cron-once/src/cron-runs.schema.ts',
```

- [ ] **Step 2: 마이그레이션 생성**

```bash
npm run db:generate:core -- --name add-cron-runs
cat apps/core/drizzle/*_add-cron-runs.sql
```

Expected: 정확히 아래와 같은 뜻의 SQL 한 문장. 다른 테이블 변경이 섞여 있으면 **중단**하고 원인을 보고한다 (누군가의 미커밋 schema 변경이 끼어든 것).

```sql
CREATE TABLE "cron_runs" (
	"name" varchar(100) NOT NULL,
	"period_at" timestamp with time zone NOT NULL,
	"claimed_by" varchar(100) NOT NULL,
	"claimed_at" timestamp with time zone DEFAULT now() NOT NULL,
	"finished_at" timestamp with time zone,
	"outcome" varchar(20),
	CONSTRAINT "cron_runs_name_period_at_pk" PRIMARY KEY("name","period_at")
);
```

- [ ] **Step 3: 루트 모듈에 import**

`apps/core/src/app.module.ts` 상단 import 에:

```ts
import { CronOnceModule } from '@app/cron-once';
```

`imports: [` 배열 안, `LoggerModule.forRoot(loggerConfig),` 다음 줄에:

```ts
    // 크론 «주기당 한 번» 선점 (ADR-0036). SCHEDULE_ROOT 는 inventory.module 등이 이미 import 한다.
    CronOnceModule,
```

- [ ] **Step 4: 데코레이터 교체 (이름 있는 7곳)**

각 파일에서 `import { Cron, ... } from '@nestjs/schedule'` 의 `Cron` 을 지우고(다른 것 — `CronExpression` — 은 남긴다; import 가 비면 줄을 지운다) `import { CronOnce } from '@app/cron-once';` 를 더한 뒤, 데코레이터를 바꾼다. 이름과 timeZone 은 그대로 둔다.

| 파일 | 전 | 후 |
|---|---|---|
| `replenishment-refresh.job.ts` | `@Cron('40 3 * * *', { name: 'replenishment-profile-refresh', timeZone: 'Asia/Seoul' })` | `@CronOnce('40 3 * * *', { name: 'replenishment-profile-refresh', timeZone: 'Asia/Seoul' })` |
| `fulfillment-reservation-reconciliation.service.ts` | `@Cron('5 3 * * *', { name: 'zombie-reservation-reconciliation', timeZone: 'Asia/Seoul' })` | `@CronOnce('5 3 * * *', { name: 'zombie-reservation-reconciliation', timeZone: 'Asia/Seoul' })` |
| `ledger-reconciliation.service.ts` | `@Cron('0 3 * * *', { name: 'ledger-reconciliation', timeZone: 'Asia/Seoul' })` | `@CronOnce('0 3 * * *', { name: 'ledger-reconciliation', timeZone: 'Asia/Seoul' })` |
| `inventory-idempotency.service.ts` | `@Cron('30 3 * * *', { name: 'inventory-idempotency-purge', timeZone: 'Asia/Seoul' })` | `@CronOnce('30 3 * * *', { name: 'inventory-idempotency-purge', timeZone: 'Asia/Seoul' })` |
| `product-sellable-quantity.service.ts` | `@Cron(CronExpression.EVERY_MINUTE, {` … `name: 'product-sellable-quantity-sales-period-refresh',` | `@CronOnce(CronExpression.EVERY_MINUTE, {` (옵션 객체는 그대로; `name` 외 다른 키가 있으면 `timeZone` 만 남기고 보고) |
| `transfer-stagnation.monitor.ts` | `@Cron('0 4 * * *', { name: 'transfer-stagnation-monitor', timeZone: 'Asia/Seoul' })` | `@CronOnce('0 4 * * *', { name: 'transfer-stagnation-monitor', timeZone: 'Asia/Seoul' })` |
| `fulfillment-reconciliation.service.ts` | `@Cron('10 3 * * *', { name: 'fulfillment-v2-reconciliation', timeZone: 'Asia/Seoul' })` | `@CronOnce('10 3 * * *', { name: 'fulfillment-v2-reconciliation', timeZone: 'Asia/Seoul' })` |

- [ ] **Step 5: 데코레이터 교체 (이름 없던 4곳 — 이름을 새로 준다)**

| 파일 | 전 | 후 |
|---|---|---|
| `entrance-password-cleaner.ts` | `@Cron(CronExpression.EVERY_DAY_AT_4AM)` | `@CronOnce(CronExpression.EVERY_DAY_AT_4AM, { name: 'entrance-password-sweep' })` |
| `bulk-session.cleaner.ts` | `@Cron(CronExpression.EVERY_DAY_AT_4AM)` | `@CronOnce(CronExpression.EVERY_DAY_AT_4AM, { name: 'bulk-session-sweep' })` |
| `bulk-image.cleaner.ts` | `@Cron(CronExpression.EVERY_MINUTE)` | `@CronOnce(CronExpression.EVERY_MINUTE, { name: 'bulk-image-sweep' })` |
| `form-export-job.worker.ts:68` | `@Cron(CronExpression.EVERY_DAY_AT_4AM)` | `@CronOnce(CronExpression.EVERY_DAY_AT_4AM, { name: 'form-export-job-purge' })` |

`form-export-job.worker.ts` 는 33행의 10초 폴러가 `@Cron` 으로 남으므로 `Cron` import 를 지우지 않는다.

- [ ] **Step 6: CAS 폴러 4곳에 마커**

각 `@Cron(...)` 줄 **바로 윗줄**에 한 줄 주석을 넣는다. 마커 접두 `// cron-overlap-safe:` 는 가드 스펙이 정확히 이 문자열로 찾는다.

```ts
// cron-overlap-safe: lease_until CAS 로 작업을 집으므로 인스턴스가 겹쳐도 한 작업은 한 번만 처리된다. 스케일아웃 대상 폴러라 주기당 한 번으로 묶지 않는다 (ADR-0036).
@Cron(CronExpression.EVERY_5_SECONDS)
```

- `bulk-session-job.worker.ts:34` (5초)
- `form-export-job.worker.ts:33` (10초)
- `fulfillment-order-reservation-retry.worker.ts:43` (10초)
- `fulfillment-order-creation-backlog.worker.ts:33` (10초)

세 파일 모두 문구는 같아도 되지만, `fulfillment-order-*` 두 워커는 CAS 근거를 그 파일에서 확인한다 — `lease`·`FOR UPDATE SKIP LOCKED`·상태 CAS 중 실제로 쓰는 것을 문구에 적는다 (`grep -n "lease\|SKIP LOCKED\|claim" <파일>`). 근거가 없으면 마커를 달지 말고 `@CronOnce` 로 교체하고 보고한다.

- [ ] **Step 7: 검증**

```bash
grep -rn "@Cron(" apps/core/src --include=*.ts | grep -v '\.spec\.'
```

Expected: 정확히 4줄(마커 대상). 

```bash
grep -rn "@CronOnce(" apps/core/src --include=*.ts | grep -v '\.spec\.' | wc -l
```

Expected: 11.

```bash
npm run type-check 2>&1 | tail -3
npx jest --maxWorkers=2 apps/core libs/cron-once 2>&1 | tail -6
```

Expected: 에러 0 / 실패 0. 기존 core 스펙 중 `@Cron` 메타데이터를 검사하거나 `Cron` 를 mock 하는 것이 깨지면 `@CronOnce` 로 같은 검사를 하도록 스펙을 고친다 (`grep -rn "Cron" apps/core/src --include=*.spec.ts`).

- [ ] **Step 8: Commit (schema + migration + 코드 한 커밋)**

```bash
git add apps/core libs/cron-once
git commit -m "feat(core): 크론 11개를 @CronOnce 로, CAS 폴러 4개는 마커로 남긴다 + cron_runs 마이그 (#821)

Claude-Session: https://claude.ai/code/session_01Wc3bLKCeRbM6pzjspyK2zt"
```

---

### Task 8: channel-adapter · analytics 전환

**Files:**
- Modify: `apps/channel-adapter/drizzle.config.ts`, `apps/analytics/drizzle.config.ts`
- Create: `apps/channel-adapter/drizzle/<ts>_add-cron-runs.sql`, `apps/analytics/drizzle/<ts>_add-cron-runs.sql` (+ meta)
- Modify: `apps/channel-adapter/src/adapter.module.ts:136`, `apps/analytics/src/analytics.module.ts:45`
- Modify (교체 7곳): 아래 표

- [ ] **Step 1: drizzle config 두 곳에 스키마 추가**

두 파일의 `schema` 배열 끝(`'libs/authorization/src/database/auth.schema.ts',` 다음)에:

```ts
    'libs/cron-once/src/cron-runs.schema.ts',
```

- [ ] **Step 2: 마이그레이션 생성 및 확인**

```bash
npm run db:generate:channel-adapter -- --name add-cron-runs
npm run db:generate:analytics -- --name add-cron-runs
cat apps/channel-adapter/drizzle/*_add-cron-runs.sql apps/analytics/drizzle/*_add-cron-runs.sql
```

Expected: 각각 `CREATE TABLE "cron_runs"` 한 문장뿐 (Task 7 Step 2 와 같은 모양). 다른 변경이 섞이면 중단·보고.

- [ ] **Step 3: 루트 모듈 import**

두 모듈의 `SCHEDULE_ROOT,` 줄 바로 다음에 `CronOnceModule,` 를 넣고 상단에 `import { CronOnceModule } from '@app/cron-once';`.

- [ ] **Step 4: 데코레이터 교체**

각 파일에서 `Cron` import 를 `@nestjs/schedule` 에서 빼고 `import { CronOnce } from '@app/cron-once';` 추가.

| 파일 | 전 | 후 |
|---|---|---|
| `apps/channel-adapter/src/services/membership-daily-sync.service.ts:31` | `@Cron('0 2 * * *', { timeZone: 'Asia/Seoul' })` | `@CronOnce('0 2 * * *', { name: 'membership-daily-reconcile', timeZone: 'Asia/Seoul' })` |
| `…/membership-daily-sync.service.ts:71` | `@Cron('30 2 * * *', { timeZone: 'Asia/Seoul' })` | `@CronOnce('30 2 * * *', { name: 'membership-daily-group-reconcile', timeZone: 'Asia/Seoul' })` |
| `apps/channel-adapter/src/services/coupon-issue-reconciliation.service.ts:32` | `@Cron('0 3 * * *', { timeZone: 'Asia/Seoul' })` | `@CronOnce('0 3 * * *', { name: 'coupon-issue-reconcile', timeZone: 'Asia/Seoul' })` |
| `…/coupon-issue-reconciliation.service.ts:54` | `@Cron('*/15 * * * *', { timeZone: 'Asia/Seoul' })` | `@CronOnce('*/15 * * * *', { name: 'coupon-issue-sweep-recent-failures', timeZone: 'Asia/Seoul' })` |
| `apps/channel-adapter/src/services/order-collection/order-poller.orchestrator.ts:60` | `@Cron('*/5 * * * *')` | `@CronOnce('*/5 * * * *', { name: 'order-poll' })` |
| `apps/channel-adapter/src/adapters/medusa/inbox-failed-revival.service.ts:34` | `@Cron('0 4 * * *', { timeZone: 'Asia/Seoul' })` | `@CronOnce('0 4 * * *', { name: 'inbox-failed-revival', timeZone: 'Asia/Seoul' })` |
| `apps/analytics/src/datasets/memberships/aggregates/membership-daily-snapshot.service.ts:36` | `@Cron('5 0 * * *', { timeZone: SEOUL_TZ })` | `@CronOnce('5 0 * * *', { name: 'membership-daily-snapshot', timeZone: SEOUL_TZ })` |

- [ ] **Step 5: 검증**

```bash
grep -rn "@Cron(" apps/channel-adapter/src apps/analytics/src --include=*.ts | grep -v '\.spec\.' | wc -l   # 0
grep -rn "@CronOnce(" apps/channel-adapter/src apps/analytics/src --include=*.ts | grep -v '\.spec\.' | wc -l   # 7
npm run type-check 2>&1 | tail -3
npx jest --maxWorkers=2 apps/channel-adapter apps/analytics 2>&1 | tail -6
```

Expected: 0 / 7 / 에러 0 / 실패 0. 깨지는 스펙은 Task 7 Step 7 과 같은 방식으로 고친다.

- [ ] **Step 6: Commit**

```bash
git add apps/channel-adapter apps/analytics
git commit -m "feat(channel-adapter,analytics): 크론 7개를 @CronOnce 로 + cron_runs 마이그 (#821)

Claude-Session: https://claude.ai/code/session_01Wc3bLKCeRbM6pzjspyK2zt"
```

---

### Task 9: membership · wallet 전환

**Files:**
- Modify: `apps/membership/drizzle.config.ts`, `apps/wallet/drizzle.config.ts`
- Create: `apps/membership/drizzle/<ts>_add-cron-runs.sql`, `apps/wallet/drizzle/<ts>_add-cron-runs.sql` (+ meta)
- Modify: `apps/membership/src/app.module.ts:94`, `apps/wallet/src/wallet.module.ts:386`
- Modify (교체 15곳): 아래 표

- [ ] **Step 1: drizzle config 두 곳에 스키마 추가** — `schema` 배열 끝에 `'libs/cron-once/src/cron-runs.schema.ts',`.

- [ ] **Step 2: 마이그레이션 생성 및 확인**

```bash
npm run db:generate:membership -- --name add-cron-runs
npm run db:generate:wallet -- --name add-cron-runs
cat apps/membership/drizzle/*_add-cron-runs.sql apps/wallet/drizzle/*_add-cron-runs.sql
```

Expected: 각각 `CREATE TABLE "cron_runs"` 한 문장뿐. wallet 은 스냅샷 체인이 한 번 깨진 적이 있다(#593) — generate 가 실패하면 `apps/wallet/drizzle/meta/_journal.json` 의 마지막 항목과 snapshot 파일이 짝이 맞는지 확인하고 보고한다. 손으로 SQL 을 쓰지 않는다.

- [ ] **Step 3: 루트 모듈 import** — 두 모듈의 `SCHEDULE_ROOT,` 다음 줄에 `CronOnceModule,`, 상단에 `import { CronOnceModule } from '@app/cron-once';`.

- [ ] **Step 4: 데코레이터 교체**

| 파일 | 전 | 후 |
|---|---|---|
| `apps/membership/src/services/pause.service.ts:36` | `@Cron(CronExpression.EVERY_HOUR)` | `@CronOnce(CronExpression.EVERY_HOUR, { name: 'pause-auto-resume' })` |
| `apps/membership/src/services/subscription/agreement-cleanup.service.ts:53` | `@Cron(CronExpression.EVERY_HOUR)` | `@CronOnce(CronExpression.EVERY_HOUR, { name: 'agreement-revoke-retry' })` |
| `apps/membership/src/services/billing/recurring-billing.service.ts:44` | `@Cron(CronExpression.EVERY_DAY_AT_9AM)` | `@CronOnce(CronExpression.EVERY_DAY_AT_9AM, { name: 'billing-daily-scheduler' })` |
| `…/recurring-billing.service.ts:66` | `@Cron(CronExpression.EVERY_30_MINUTES)` | `@CronOnce(CronExpression.EVERY_30_MINUTES, { name: 'billing-reconcile-stuck' })` |
| `…/recurring-billing.service.ts:137` | `@Cron(CronExpression.EVERY_30_MINUTES)` | `@CronOnce(CronExpression.EVERY_30_MINUTES, { name: 'billing-reconcile-stuck-invoices' })` |
| `…/recurring-billing.service.ts:319` | `@Cron('0 * * * *')` | `@CronOnce('0 * * * *', { name: 'billing-expiration-check' })` |
| `apps/membership/src/services/renewal-notice/expiry-notice.service.ts:41` | `@Cron('30 10 * * *')` | `@CronOnce('30 10 * * *', { name: 'expiry-notice' })` |
| `apps/membership/src/services/renewal-notice/renewal-notice.service.ts:46` | `@Cron('0 10 * * *')` | `@CronOnce('0 10 * * *', { name: 'renewal-notice' })` |
| `apps/wallet/src/cms/cms-settlement-poller.service.ts:38` | `@Cron('0 */30 * * * *')` | `@CronOnce('0 */30 * * * *', { name: 'cms-settlement-poll' })` |
| `apps/wallet/src/cms/cms-member-poller.service.ts:45` | `@Cron('0 0 9,12,15 * * 1-5')` | `@CronOnce('0 0 9,12,15 * * 1-5', { name: 'cms-member-poll' })` |
| `apps/wallet/src/jobs/points-expiration.job.ts:13` | `@Cron(process.env.WALLET_POINTS_EXPIRATION_CRON ?? DEFAULT_CRON)` | `@CronOnce(process.env.WALLET_POINTS_EXPIRATION_CRON ?? DEFAULT_CRON, { name: 'points-expiration' })` |
| `apps/wallet/src/jobs/expiration.job.ts:34` | `@Cron(process.env.WALLET_EXPIRATION_CRON ?? DEFAULT_EXPIRATION_CRON)` | `@CronOnce(process.env.WALLET_EXPIRATION_CRON ?? DEFAULT_EXPIRATION_CRON, { name: 'wallet-expiration' })` |
| `apps/wallet/src/jobs/toss-action-expiration.job.ts:32` | `@Cron(process.env.WALLET_TOSS_ACTION_EXPIRATION_CRON ?? DEFAULT_ACTION_EXPIRATION_CRON)` | `@CronOnce(process.env.WALLET_TOSS_ACTION_EXPIRATION_CRON ?? DEFAULT_ACTION_EXPIRATION_CRON, { name: 'toss-action-expiration' })` |
| `apps/wallet/src/checkout/checkout-session-expiration.service.ts:13` | `@Cron('0 */10 * * * *')` | `@CronOnce('0 */10 * * * *', { name: 'checkout-session-expiration' })` |
| `apps/wallet/src/invoices/invoice-executor.service.ts:62` | `@Cron('0 */10 * * * *')` | `@CronOnce('0 */10 * * * *', { name: 'invoice-execute' })` |

- [ ] **Step 5: 검증**

```bash
grep -rn "@Cron(" apps/membership/src apps/wallet/src --include=*.ts | grep -v '\.spec\.' | wc -l   # 0
grep -rn "@CronOnce(" apps/membership/src apps/wallet/src --include=*.ts | grep -v '\.spec\.' | wc -l   # 15
npm run type-check 2>&1 | tail -3
npx jest --maxWorkers=2 apps/membership apps/wallet 2>&1 | tail -6
```

Expected: 0 / 15 / 에러 0 / 실패 0. (membership 통합 스펙은 `describeIfDb` 로 기본 skip 이다.)

- [ ] **Step 6: Commit**

```bash
git add apps/membership apps/wallet
git commit -m "feat(membership,wallet): 크론 15개를 @CronOnce 로 + cron_runs 마이그 (#821)

Claude-Session: https://claude.ai/code/session_01Wc3bLKCeRbM6pzjspyK2zt"
```

---

### Task 10: ugc-service · user-service 전환

**Files:**
- Modify: `apps/ugc-service/src/db/drizzle.config.ts`, `apps/user-service/database/drizzle/drizzle.config.ts`
- Create: `apps/ugc-service/src/db/<ts>_add-cron-runs.sql`, `apps/user-service/database/drizzle/<ts>_add-cron-runs.sql` (+ meta; 두 앱은 `prefix: 'supabase'` 라 파일명이 `YYYYMMDDHHmmss_` 꼴)
- Modify: `apps/ugc-service/src/ugc-service.module.ts:37`, `apps/user-service/src/app.module.ts:122`
- Modify (교체 7곳): 아래 표

- [ ] **Step 1: drizzle config 두 곳에 스키마 추가** — `schema` 배열 끝에 `'libs/cron-once/src/cron-runs.schema.ts',`.

- [ ] **Step 2: 마이그레이션 생성 및 확인**

```bash
npm run db:generate:ugc-service -- --name add-cron-runs
npm run db:generate:user-service -- --name add-cron-runs
ls apps/ugc-service/src/db/*add-cron-runs.sql apps/user-service/database/drizzle/*add-cron-runs.sql
cat apps/ugc-service/src/db/*add-cron-runs.sql apps/user-service/database/drizzle/*add-cron-runs.sql
```

Expected: 각각 `CREATE TABLE "cron_runs"` 한 문장뿐.

- [ ] **Step 3: 루트 모듈 import** — 두 모듈의 `SCHEDULE_ROOT,` 다음 줄에 `CronOnceModule,`, 상단에 `import { CronOnceModule } from '@app/cron-once';`.

- [ ] **Step 4: 데코레이터 교체**

| 파일 | 전 | 후 |
|---|---|---|
| `apps/ugc-service/src/reviews/rewards/review-best-selection.cron.ts:15` | `@Cron('0 4 * * 1', { name: 'generate-weekly-best-review-candidates', timeZone: 'Asia/Seoul' })` | `@CronOnce('0 4 * * 1', { name: 'generate-weekly-best-review-candidates', timeZone: 'Asia/Seoul' })` |
| `apps/ugc-service/src/qna/qna-cleanup-cron.service.ts:11` | `@Cron('0 3 * * *', {` … `name: 'cleanup-deleted-questions',` … | `@CronOnce('0 3 * * *', {` (옵션 객체 유지; `name`·`timeZone` 외 키가 있으면 지우고 보고) |
| `apps/user-service/src/api/admin/dormant/dormant.service.ts:26` | `@Cron(CronExpression.EVERY_DAY_AT_2AM)` | `@CronOnce(CronExpression.EVERY_DAY_AT_2AM, { name: 'dormant-accounts' })` |
| `apps/user-service/src/api/business-licenses/business-licenses.service.ts:125` | `@Cron(CronExpression.EVERY_30_MINUTES)` | `@CronOnce(CronExpression.EVERY_30_MINUTES, { name: 'business-license-revalidate' })` |
| `apps/user-service/src/api/phone-verification/services/expire-existing-codes.ts:33` | `@Cron('0 0 * * *') // 매일 자정 실행` | `@CronOnce('0 0 * * *', { name: 'phone-verification-expire' }) // 매일 자정 실행` |
| `apps/user-service/src/api/email-verification/services/expire-email-codes.service.ts:25` | `@Cron('0 0 * * *') // 매일 자정` | `@CronOnce('0 0 * * *', { name: 'email-verification-expire' }) // 매일 자정` |
| `apps/user-service/src/api/recent-views/recent-views.service.ts:60` | `@Cron('0 3 * * *')` | `@CronOnce('0 3 * * *', { name: 'recent-views-cleanup' })` |

`expire-existing-codes.ts` 의 메서드는 `tx?: DbTransaction` 을 받는다 — 크론 호출은 인자 없이 들어오므로 그대로 둔다.

- [ ] **Step 5: 검증**

```bash
grep -rn "@Cron(" apps/ugc-service/src apps/user-service/src --include=*.ts | grep -v '\.spec\.' | wc -l   # 0
grep -rn "@CronOnce(" apps/ugc-service/src apps/user-service/src --include=*.ts | grep -v '\.spec\.' | wc -l   # 7
npm run type-check 2>&1 | tail -3
npx jest --maxWorkers=2 apps/ugc-service apps/user-service 2>&1 | tail -6
```

Expected: 0 / 7 / 에러 0 / 실패 0.

- [ ] **Step 6: 저장소 전수 확인**

```bash
grep -rn "@Cron(" apps/*/src --include=*.ts | grep -v '\.spec\.'
```

Expected: core 의 마커 대상 4줄만 남는다.

- [ ] **Step 7: Commit**

```bash
git add apps/ugc-service apps/user-service
git commit -m "feat(ugc-service,user-service): 크론 7개를 @CronOnce 로 + cron_runs 마이그 (#821)

Claude-Session: https://claude.ai/code/session_01Wc3bLKCeRbM6pzjspyK2zt"
```

---

### Task 11: 가드 스펙 — 마커 없는 `@Cron` · 이름 중복 · 모듈 누락

**Files:**
- Create: `libs/cron-once/src/guards/cron-once-guard.ts`
- Test: `libs/cron-once/src/guards/cron-once-guard.spec.ts`

**Interfaces:**
- Produces:
  ```ts
  interface SourceFile { path: string; content: string }
  interface GuardViolation { path: string; line: number; rule: 'raw-cron-without-marker' | 'duplicate-name' | 'module-missing'; detail: string }
  function findGuardViolations(files: SourceFile[]): GuardViolation[];
  const OVERLAP_SAFE_MARKER = '// cron-overlap-safe:';
  ```

- [ ] **Step 1: 실패하는 테스트 작성**

`libs/cron-once/src/guards/cron-once-guard.spec.ts`:

```ts
import { execFileSync } from 'node:child_process';
import { readFileSync } from 'node:fs';
import { join } from 'node:path';
import { findGuardViolations, OVERLAP_SAFE_MARKER } from './cron-once-guard';

/**
 * 크론 «주기당 한 번» 규칙의 게이트 (ADR-0036, #821).
 *
 * 1. `apps/*\/src` 의 `@Cron(` 은 바로 윗줄에 `// cron-overlap-safe:` 마커가 있을 때만 허용한다.
 * 2. `@CronOnce` 의 `name` 은 앱 안에서 유일하다 (부팅에서도 죽지만 게이트가 먼저 잡는다).
 * 3. `@CronOnce` 를 하나라도 쓰는 앱은 `CronOnceModule` 을 import 한다. 모듈이 빠지면 데코레이터
 *    메타데이터가 탐색되지 않아 크론이 **조용히 안 돈다** — 부팅 실패보다 나쁜 실패다.
 *
 * ⚠️ 마지막 it 은 저장소 전체를 읽는다. 새 앱에서 무심코 `@Cron` 을 쓰면 그 앱의 diff 만 보는
 * 리뷰로는 안 걸리지만 여기서는 걸린다.
 */
const file = (path: string, content: string) => ({ path, content });

describe('findGuardViolations (픽스처)', () => {
  const moduleOk = file('apps/foo/src/app.module.ts', `import { CronOnceModule } from '@app/cron-once';\n@Module({ imports: [CronOnceModule] })`);

  it('마커 없는 @Cron 은 위반', () => {
    const v = findGuardViolations([file('apps/foo/src/a.service.ts', `  @Cron('0 3 * * *')\n  async run() {}`)]);
    expect(v).toEqual([expect.objectContaining({ rule: 'raw-cron-without-marker', path: 'apps/foo/src/a.service.ts', line: 1 })]);
  });

  it('바로 윗줄 마커가 있으면 허용', () => {
    const v = findGuardViolations([
      file('apps/foo/src/a.worker.ts', `  ${OVERLAP_SAFE_MARKER} lease CAS\n  @Cron(CronExpression.EVERY_5_SECONDS)\n  async tick() {}`),
    ]);
    expect(v).toEqual([]);
  });

  it('마커가 두 줄 위에 있으면 허용하지 않는다 (바로 윗줄만)', () => {
    const v = findGuardViolations([
      file('apps/foo/src/a.worker.ts', `  ${OVERLAP_SAFE_MARKER} lease CAS\n\n  @Cron('* * * * *')\n  async tick() {}`),
    ]);
    expect(v.map((x) => x.rule)).toEqual(['raw-cron-without-marker']);
  });

  it('같은 앱에서 name 이 겹치면 위반, 다른 앱이면 허용', () => {
    const v = findGuardViolations([
      moduleOk,
      file('apps/foo/src/a.ts', `@CronOnce('0 3 * * *', { name: 'nightly' })`),
      file('apps/foo/src/b.ts', `@CronOnce('0 4 * * *', {\n  name: 'nightly',\n})`),
      file('apps/bar/src/app.module.ts', `CronOnceModule`),
      file('apps/bar/src/c.ts', `@CronOnce('0 5 * * *', { name: 'nightly' })`),
    ]);
    expect(v.map((x) => [x.rule, x.path])).toEqual([['duplicate-name', 'apps/foo/src/b.ts']]);
  });

  it('@CronOnce 를 쓰는데 CronOnceModule import 가 없으면 위반', () => {
    const v = findGuardViolations([file('apps/foo/src/a.ts', `@CronOnce('0 3 * * *', { name: 'x' })`)]);
    expect(v).toEqual([expect.objectContaining({ rule: 'module-missing', path: 'apps/foo' })]);
  });

  it('스펙 파일은 검사하지 않는다', () => {
    const v = findGuardViolations([file('apps/foo/src/a.spec.ts', `@Cron('* * * * *')`)]);
    expect(v).toEqual([]);
  });
});

describe('저장소 전수 (#821)', () => {
  it('apps/*/src 에 위반이 없다', () => {
    const REPO = join(__dirname, '..', '..', '..', '..');
    const paths = execFileSync('git', ['ls-files', '--', 'apps/*/src/**/*.ts'], { cwd: REPO, encoding: 'utf8' })
      .split('\n')
      .filter((p) => p.length > 0);
    const files = paths.map((p) => ({ path: p, content: readFileSync(join(REPO, p), 'utf8') }));
    expect(findGuardViolations(files)).toEqual([]);
  });
});
```

- [ ] **Step 2: 실패 확인**

```bash
npx jest libs/cron-once/src/guards
```

Expected: FAIL — `Cannot find module './cron-once-guard'`.

- [ ] **Step 3: 구현**

`libs/cron-once/src/guards/cron-once-guard.ts`:

```ts
/**
 * `cron-once-guard.spec.ts` 가 쓰는 순수 판정. 파일 목록을 받아 위반을 돌려준다 — 파일 시스템·git
 * 은 스펙 쪽에 있다. 픽스처로 규칙을 고정하고, 저장소 전수는 같은 함수를 한 번 더 부른다.
 */
export const OVERLAP_SAFE_MARKER = '// cron-overlap-safe:';

export interface SourceFile {
  path: string;
  content: string;
}

export type GuardRule = 'raw-cron-without-marker' | 'duplicate-name' | 'module-missing';

export interface GuardViolation {
  path: string;
  line: number;
  rule: GuardRule;
  detail: string;
}

const RAW_CRON = /^\s*@Cron\(/;
const CRON_ONCE_START = /@CronOnce\(/;
const NAME_IN_OPTIONS = /name:\s*'([^']+)'/;

const appOf = (path: string): string | undefined => /^(apps\/[^/]+)\/src\//.exec(path)?.[1];
const isSpec = (path: string) => path.endsWith('.spec.ts');

export function findGuardViolations(files: SourceFile[]): GuardViolation[] {
  const violations: GuardViolation[] = [];
  const namesByApp = new Map<string, Map<string, string>>(); // app → name → first path
  const appsUsingCronOnce = new Set<string>();
  const appsImportingModule = new Set<string>();

  for (const { path, content } of files) {
    const app = appOf(path);
    if (!app || isSpec(path)) continue;
    const lines = content.split('\n');

    if (/\bCronOnceModule\b/.test(content)) appsImportingModule.add(app);

    lines.forEach((text, i) => {
      if (RAW_CRON.test(text)) {
        const prev = lines[i - 1]?.trim() ?? '';
        if (!prev.startsWith(OVERLAP_SAFE_MARKER)) {
          violations.push({ path, line: i + 1, rule: 'raw-cron-without-marker', detail: `@Cron without '${OVERLAP_SAFE_MARKER}' on the previous line — use @CronOnce (ADR-0036)` });
        }
      }
      if (CRON_ONCE_START.test(text)) {
        appsUsingCronOnce.add(app);
        // 옵션 객체가 여러 줄일 수 있어 데코레이터 시작부터 다섯 줄 안에서 name 을 찾는다.
        const window = lines.slice(i, i + 5).join('\n');
        const name = NAME_IN_OPTIONS.exec(window)?.[1];
        if (!name) return;
        const seen = namesByApp.get(app) ?? new Map<string, string>();
        namesByApp.set(app, seen);
        const first = seen.get(name);
        if (first) {
          violations.push({ path, line: i + 1, rule: 'duplicate-name', detail: `@CronOnce name '${name}' already used in ${first}` });
        } else {
          seen.set(name, path);
        }
      }
    });
  }

  for (const app of appsUsingCronOnce) {
    if (!appsImportingModule.has(app)) {
      violations.push({ path: app, line: 0, rule: 'module-missing', detail: `${app} uses @CronOnce but never imports CronOnceModule — its crons would silently never run` });
    }
  }
  return violations;
}
```

- [ ] **Step 4: 통과 확인**

```bash
npx jest libs/cron-once/src/guards
```

Expected: PASS 7건. 전수 it 이 실패하면 출력된 위반 목록이 곧 남은 작업이다 — Task 7~10 에서 빠뜨린 파일을 고친다. 가드를 완화하지 않는다.

- [ ] **Step 5: Commit**

```bash
git add libs/cron-once/src/guards
git commit -m "test(cron-once): 가드 스펙 — 마커 없는 @Cron·이름 중복·모듈 누락을 막는다 (#821)

Claude-Session: https://claude.ai/code/session_01Wc3bLKCeRbM6pzjspyK2zt"
```

---

### Task 12: 통합 스펙 — 앱 컨텍스트 둘의 선점 경쟁

**Files:**
- Create: `libs/cron-once/src/cron-once.integration.spec.ts`
- Modify: `scripts/local/test-core-integration-local.sh` (`DEFAULT_PATTERN`)
- Modify: `package.json` (`test:cron-once:integration` 스크립트)

**Interfaces:**
- Consumes: `CronOnceModule`, `CronOnce`, `CRON_ONCE_INSTANCE_ID`, `SCHEDULE_ROOT`, `DbModule.forRoot`

- [ ] **Step 1: 스펙 작성**

`libs/cron-once/src/cron-once.integration.spec.ts`:

```ts
import { Injectable, Module } from '@nestjs/common';
import { Test, TestingModule } from '@nestjs/testing';
import { DbModule, DbService } from '@app/db';
import { SCHEDULE_ROOT } from '@app/shared/schedule/schedule-root';
import { sql } from 'drizzle-orm';
import { CRON_ONCE_INSTANCE_ID } from './cron-once.constants';
import { CronOnce } from './cron-once.decorator';
import { CronOnceModule } from './cron-once.module';

/**
 * 실물 경쟁: 같은 DB 를 보는 Nest 앱 컨텍스트 **둘**이 매초 크론 `probe` 를 동시에 선점한다.
 * ECS 롤링 겹침 / `scaling.max > 1` 을 로컬에서 재현하는 판정이다 (#821 §7).
 *
 * 실행: `npm run test:cron-once:integration` (core 로컬 DB, `cron_runs` 는 core 마이그레이션이 만든다)
 * 또는 `npm run test:core:integration:local` (기본 패턴에 포함).
 *
 * 판정:
 *   - 행 수 ≥ 2 (최소 두 주기가 지났다)
 *   - 두 컨텍스트 실행 카운터 합 = 행 수 (주기마다 정확히 한 번)
 *   - claimed_by 에 두 인스턴스가 모두 등장 (한쪽이 항상 이기는 게 아니다)
 * 한 프로세스라 시계는 공유하지만 선점은 DB 에서 일어나므로 경쟁 자체는 실물이다.
 */
const DATABASE_URL = process.env.DATABASE_URL;
if (process.env.REQUIRE_CRON_ONCE_DB === '1' && !DATABASE_URL) {
  throw new Error('DATABASE_URL is required for the cron-once integration suite.');
}
const describeIfDb = DATABASE_URL ? describe : describe.skip;

const PROBE = 'probe-cron-once-integration';
const RUN_FOR_MS = 4_000;

const counters = { a: 0, b: 0 };

const buildApp = async (instanceId: 'a' | 'b'): Promise<TestingModule> => {
  @Injectable()
  class Probe {
    @CronOnce('* * * * * *', { name: PROBE })
    async tick(): Promise<void> {
      counters[instanceId] += 1;
    }
  }
  @Module({
    imports: [SCHEDULE_ROOT, CronOnceModule, DbModule.forRoot({ config: { connectionString: DATABASE_URL as string }, schema: {} })],
    providers: [Probe, { provide: CRON_ONCE_INSTANCE_ID, useValue: instanceId }],
  })
  class Root {}
  const app = await Test.createTestingModule({ imports: [Root] }).compile();
  await app.init();
  return app;
};

describeIfDb('cron-once 선점 경쟁 (앱 컨텍스트 둘)', () => {
  let a: TestingModule;
  let b: TestingModule;
  let admin: DbService;

  beforeAll(async () => {
    a = await buildApp('a');
    b = await buildApp('b');
    admin = a.get(DbService);
    await admin.db.execute(sql`DELETE FROM cron_runs WHERE name = ${PROBE}`);
  });

  afterAll(async () => {
    await b?.close();
    await a?.close();
  });

  it('주기마다 정확히 한 인스턴스만 실행하고, 두 인스턴스가 모두 이겨 본다', async () => {
    await new Promise((resolve) => setTimeout(resolve, RUN_FOR_MS));
    // 진행 중인 tick 이 마감할 시간을 준 뒤 두 앱을 멈춘다 (close 가 registry 의 크론을 정리한다).
    await b.close();
    await a.close();

    const rows = (await admin.db.execute(sql`
      SELECT period_at, claimed_by, outcome FROM cron_runs WHERE name = ${PROBE} ORDER BY period_at
    `)) as unknown as { period_at: Date; claimed_by: string; outcome: string | null }[];

    expect(rows.length).toBeGreaterThanOrEqual(2);
    expect(counters.a + counters.b).toBe(rows.length);
    expect(new Set(rows.map((r) => r.claimed_by))).toEqual(new Set(['a', 'b']));
    expect(rows.every((r) => r.outcome === 'ok')).toBe(true);

    await admin.db.execute(sql`DELETE FROM cron_runs WHERE name = ${PROBE}`);
  }, 15_000);
});
```

`afterAll` 의 close 는 it 안에서 이미 닫힌 앱에 다시 부르므로, `TestingModule.close()` 가 두 번째 호출에서 throw 하면 it 안의 close 두 줄을 지우고 `afterAll` 에만 둔 채 판정 전에 `await a.get(SchedulerRegistry).getCronJob(PROBE).stop()` (b 도) 으로 멈추는 방식으로 바꾼다.

- [ ] **Step 2: 러너에 포함**

`scripts/local/test-core-integration-local.sh` 의

```bash
DEFAULT_PATTERN='(apps/core|libs/events|scripts/fulfillment-v2)/.*integration'
```

를

```bash
DEFAULT_PATTERN='(apps/core|libs/events|libs/cron-once|scripts/fulfillment-v2)/.*integration'
```

로. `package.json` scripts 의 `"test:bulk-session:integration"` 다음 줄에:

```json
    "test:cron-once:integration": "REQUIRE_CRON_ONCE_DB=1 dotenv -e apps/core/.env -- jest --runInBand libs/cron-once/src/cron-once.integration.spec.ts",
```

- [ ] **Step 3: 기본 게이트에서 skip 되는지 확인**

```bash
npx jest libs/cron-once/src/cron-once.integration.spec.ts
```

Expected: `skipped` 1 (DATABASE_URL 없음). `scripts/jest/no-self-loaded-env-in-specs.spec.ts` 도 통과해야 한다:

```bash
npx jest scripts/jest
```

- [ ] **Step 4: 실 DB 로 실행**

```bash
docker compose up -d postgres
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/core npx drizzle-kit migrate --config apps/core/drizzle.config.ts
REQUIRE_CRON_ONCE_DB=1 DATABASE_URL=postgresql://postgres:postgres@localhost:5432/core npx jest --runInBand libs/cron-once/src/cron-once.integration.spec.ts
```

Expected: PASS 1건. (`core` DB 가 없으면 `npm run test:core:integration:local -- cron-once` 가 postgres 기동·마이그레이션까지 한다.) `claimed_by` 에 한쪽만 나오면(예: `a` 만) 4초 안에 우연히 한쪽이 전승한 것일 수 있다 — 두 번째 실행에서도 같으면 selection 편향을 의심하고 보고한다. 실패를 조용히 넘기지 않는다.

- [ ] **Step 5: Commit**

```bash
git add libs/cron-once/src/cron-once.integration.spec.ts scripts/local/test-core-integration-local.sh package.json
git commit -m "test(cron-once): 앱 컨텍스트 둘의 선점 경쟁 통합 스펙 — 주기당 정확히 한 번 (#821)

Claude-Session: https://claude.ai/code/session_01Wc3bLKCeRbM6pzjspyK2zt"
```

---

### Task 13: 전체 게이트 + 로컬 두 프로세스 실측 (완료 판정)

**Files:** 없음 (검증만). 실측 결과는 Task 14 의 이슈 코멘트에 적는다.

- [ ] **Step 1: 검증 게이트**

```bash
npm run type-check 2>&1 | tail -3
npx jest --maxWorkers=2 2>&1 | tail -8
```

Expected: 에러 0, 실패 0. 빨간 것은 이 브랜치가 만든 것이다 — 고친다.

- [ ] **Step 2: core 빌드**

```bash
npx nest build core 2>&1 | tail -2
ls dist/apps/core/main.js
```

- [ ] **Step 3: 로컬 postgres + core 마이그레이션**

```bash
docker compose up -d postgres
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/core npx drizzle-kit migrate --config apps/core/drizzle.config.ts
```

(`apps/core/.env` 의 `DATABASE_URL` 이 이 로컬 DB 를 가리키는지 `grep DATABASE_URL apps/core/.env` 로 확인한다. 다르면 아래 두 프로세스에 `DATABASE_URL=…` 을 명시한다.)

- [ ] **Step 4: core 를 포트만 다르게 두 개 기동 (백그라운드)**

```bash
mkdir -p /tmp/claude-cron-once
(cd . && PORT=3101 dotenv -e apps/core/.env -- node dist/apps/core/main.js > /tmp/claude-cron-once/core-3101.log 2>&1 &)
(cd . && PORT=3102 dotenv -e apps/core/.env -- node dist/apps/core/main.js > /tmp/claude-cron-once/core-3102.log 2>&1 &)
sleep 20
grep -h "registered .* cron-once job" /tmp/claude-cron-once/core-3101.log /tmp/claude-cron-once/core-3102.log
```

Expected: 두 로그 모두 `registered 11 cron-once job(s): …`. 부팅이 죽으면(예: `Cron Job with the given name (…) already exists`) 로그를 보고한다 — 그건 이 설계가 잡으려던 바로 그 사고다.

- [ ] **Step 5: 2분 뒤 판정**

```bash
sleep 130
docker compose exec -T postgres psql -U postgres -d core -c "SELECT name, period_at, claimed_by, outcome FROM cron_runs WHERE name IN ('product-sellable-quantity-sales-period-refresh','bulk-image-sweep') ORDER BY period_at DESC, name;"
grep -c "skip .*: claimed by another instance" /tmp/claude-cron-once/core-3101.log /tmp/claude-cron-once/core-3102.log
```

Expected:
- 매분 크론 두 개 각각 **주기당 1행** (같은 `name`·`period_at` 이 두 번 나올 수 없다 — PK). 최소 2주기.
- skip 로그가 두 프로세스에 걸쳐 나온다 (debug 레벨이 로컬에서 안 찍히면 표만으로 판정한다).
- `claimed_by` 두 프로세스가 같은 호스트라 같은 값이다 — 로컬 실측에선 «행 수 = 주기 수» 가 판정이고 인스턴스 분산은 Task 12 가 본다.

- [ ] **Step 6: 정리**

```bash
pkill -f "dist/apps/core/main.js"
```

- [ ] **Step 7: 결과 기록**

위 psql 출력을 `/tmp/claude-cron-once/verdict.txt` 로 저장해 Task 14 에서 이슈 코멘트에 붙인다.

---

### Task 14: 문서 — ADR-0036, CLAUDE.md, 이슈 #821 본문 교정

**Files:**
- Create: `docs/adr/0036-cron-runs-once-per-period-by-claim.md`
- Modify: `CLAUDE.md` (`### Events / Kafka` 절 앞에 새 절)
- Modify: `libs/cron-once/src/cron-runs.schema.ts` 주석의 ADR 번호가 `0036` 인지 확인 (이미 그렇다)

- [ ] **Step 1: ADR 작성**

`docs/adr/0036-cron-runs-once-per-period-by-claim.md`:

```markdown
# 크론은 리더 선출 대신 «주기당 선점» 으로 한 번만 돈다

## Status
Accepted (2026-09-10). #821 의 결정. 설계 스펙은 `docs/superpowers/specs/2026-09-10-cron-once-design.md`.

## Context
ECS 롤링 배포는 매 배포마다 같은 앱의 태스크 두 개를 겹치게 띄우고, `scaling.max > 1` 이면 겹침이
상시가 된다. #707 감사는 크론 44개가 겹침을 «정확성» 면에서 견딘다는 것을 확인했지만(국소 결함은
#818·#820 이 닫음), 외부 호출(국세청·Medusa admin·멤버십)이 두 배로 나가는 «비용» 과, 새 크론이
같은 결함을 다시 들여올 위험은 남았다. #599 에서는 Nest 11 의 모듈 dedupe 변경으로 모든 크론이
8일간 두 번씩 돌았는데 증상이 조용해 발견이 늦었다.

## Decision
- **리더를 선출하지 않는다. 주기마다 선점한다.** 앱 DB 마다 `public.cron_runs(name, period_at)` PK
  테이블을 두고, 크론 발화 시 «이름 + 크론식에서 도출한 예정 발화 시각» 을
  `INSERT … ON CONFLICT DO NOTHING RETURNING` 으로 선점한다. 행을 얻은 태스크만 본문을 돌린다.
- **앱의 크론은 `@nestjs/schedule` 의 `@Cron` 대신 `@app/cron-once` 의 `@CronOnce(expr, { name })` 를
  쓴다.** `name` 은 필수·앱 내 유일. 앱 루트 모듈은 `CronOnceModule` 을 import 한다.
- **예외는 마커로 표시한다.** 겹쳐도 안전하고 오히려 스케일아웃해야 하는 폴러(`lease_until` CAS
  로 작업을 집는 core 의 5초·10초 워커 4개)는 `@Cron` 을 유지하되 바로 윗줄에
  `// cron-overlap-safe: <근거>` 를 단다. 가드 스펙 `libs/cron-once/src/guards/cron-once-guard.spec.ts`
  가 마커 없는 `@Cron`, 이름 중복, 모듈 누락을 막는다.
- **주기 키는 벽시계가 아니다.** `cron-parser.prev()` 로 크론식·타임존에서 도출한다(+50ms 지터
  여유). 두 태스크의 시계가 한 주기 미만으로 어긋나면 같은 키를 낸다.
- **선점 쿼리 실패는 건너뛴다(fail closed).** 본문은 어차피 DB 를 쓴다.
- **기록 행이 곧 실행 로그다.** 「한 주기 실행 1건」 판정은
  `SELECT name, period_at, claimed_by FROM cron_runs …` 로 한다. 보존 7일.

## Consequences
- 같은 이름이 두 번 등록되면 `SchedulerRegistry` 가 부팅에서 throw 한다. #599 형 사고가 «8일 무증상»
  에서 «즉시 부팅 실패» 로 바뀐다. 의도한 성질이다.
- 앱 7개에 additive 마이그레이션 1건씩. 배포는 `migrate → deploy` (user-service 는 `lcnine-auth`).
- 한 주기의 본문이 다음 주기보다 오래 걸리면 다음 주기를 다른 태스크가 선점해 두 본문이 겹칠 수
  있다. 한 프로세스에서 `@Cron` 이 겹치던 것과 같은 성질이라 여기서 다루지 않는다 — 그 크론이
  문제라면 본문 쪽 lease 로 막는다.
- `setInterval` 폴러(#815 consumer lag 등)는 대상이 아니다.

## Rejected
- **advisory lock 만** — 주기 키 없이는 짧은 작업에서 A 가 락을 놓은 뒤 B 가 몇 ms 늦게 발화하면
  둘 다 돈다. 커넥션이 끊기면 이중 실행 창도 생긴다.
- **`RETURNING` 규율만(가드 스펙)** — 정확성은 지키지만 비용 2배는 못 막는다.
- **크론 전용 태스크 분리** — ServicesBundleA/B 가 여러 앱을 한 태스크에 묶은 구조라 태스크 수가
  늘어 비용 절감과 충돌한다.
```

- [ ] **Step 2: CLAUDE.md 에 절 추가**

`CLAUDE.md` 의 `### Events / Kafka (`@app/events`)` 절 **바로 앞**에:

```markdown
### 크론 (`@app/cron-once`)

앱의 크론은 `@nestjs/schedule` 의 `@Cron` 이 아니라 **`@CronOnce(expr, { name })`** 를 쓴다 (ADR-0036, #821).
ECS 태스크가 겹쳐도(롤링 배포·`scaling.max > 1`) 한 주기가 클러스터 전체에서 한 번만 돌게, 앱 DB 의
`cron_runs(name, period_at)` PK 를 `INSERT … ON CONFLICT DO NOTHING` 으로 선점한다. `name` 은 필수·앱 내
유일이고, 앱 루트 모듈이 `CronOnceModule` 을 import 해야 한다 — 빠지면 크론이 **조용히 안 돈다**.

겹쳐도 안전하고 스케일아웃해야 하는 폴러(`lease_until` CAS 워커)만 `@Cron` 을 유지하고 바로 윗줄에
`// cron-overlap-safe: <근거>` 를 단다. `libs/cron-once/src/guards/cron-once-guard.spec.ts` 가 마커 없는
`@Cron`·이름 중복·모듈 누락을 막는다. 크론 목록은 `grep -rn "@CronOnce(\|@Cron(" apps/*/src --include=*.ts | grep -v '\.spec\.'`
로 도출한다. 로컬에서 겹침을 실측하려면 core 를 `PORT` 만 다르게 둘 띄우고 `cron_runs` 가 주기당 1행인지 본다.
```

- [ ] **Step 3: 게이트 재확인 후 커밋**

```bash
npx jest --maxWorkers=2 libs/cron-once scripts 2>&1 | tail -5
git add docs/adr/0036-cron-runs-once-per-period-by-claim.md CLAUDE.md
git commit -m "docs: ADR-0036 크론은 주기당 선점으로 한 번만 돈다 + CLAUDE.md 크론 절 (#821)

Claude-Session: https://claude.ai/code/session_01Wc3bLKCeRbM6pzjspyK2zt"
```

- [ ] **Step 4: 이슈 #821 본문의 완료 판정 절 교정**

이슈 본문을 읽어 `## 완료 판정 (#707 3단계에서 이관)` 절만 아래로 바꾼다 (`gh issue view 821 --json body -q .body > /tmp/claude-cron-once/body.md` → 편집 → `gh issue edit 821 --body-file /tmp/claude-cron-once/body.md`). 나머지 절은 손대지 않는다.

```markdown
## 완료 판정

dev 스테이지는 쓰지 않으므로 «`scaling.max=2` 스테이지» 대신 **로컬 두 프로세스 실측**으로 판정한다
(2026-09-10 교정). 라이브에서 `scaling.max` 를 올리는 날 같은 쿼리로 재확인한다.

1. 로컬 postgres 에 core 마이그레이션 적용 (`cron_runs` 생성).
2. core 를 `PORT` 만 다르게 두 번 기동.
3. 2분 뒤 `SELECT name, period_at, claimed_by FROM cron_runs ORDER BY period_at DESC` 에서 매분 크론
   (`product-sellable-quantity-sales-period-refresh`, `bulk-image-sweep`) 이 **주기당 1행**이면 완료.

반복 판정은 `npm run test:cron-once:integration` (앱 컨텍스트 둘의 선점 경쟁).
```

- [ ] **Step 5: 실측 결과를 이슈 코멘트로**

Task 13 Step 7 의 `verdict.txt` 를 붙여 코멘트한다:

```bash
gh issue comment 821 --body-file /tmp/claude-cron-once/comment.md
```

`comment.md` 내용: 브랜치명, 설계 요약 두 줄(주기당 선점 · 마커 예외 4개), 전환 개수(40 교체 / 4 마커 / 마이그 7), psql 출력, 통합 스펙 결과, 배포 순서(`migrate → deploy`, `lcnine-services` + `lcnine-auth`).

---

### Task 15: PR

- [ ] **Step 1: 푸시 및 PR 생성**

```bash
git push -u origin feat/821-cron-once
gh pr create --base develop --title "feat(cron-once): 크론은 주기당 선점으로 한 번만 돈다 — @CronOnce · cron_runs · 가드 스펙 (#821)" --body-file /tmp/claude-cron-once/pr.md
```

`pr.md`:

```markdown
Closes #821

## 무엇
- 새 라이브러리 `@app/cron-once`: `@CronOnce(expr, { name })` · `CronOnceExplorer` · `CronRunClaimer`(`cron_runs(name, period_at)` PK 선점) · `CronOnceModule`
- `@Cron` 44개 중 40개를 `@CronOnce` 로 교체, core CAS 폴러 4개는 `// cron-overlap-safe:` 마커로 유지
- 앱 7개에 `cron_runs` additive 마이그레이션 (analytics · channel-adapter · core · membership · ugc-service · user-service · wallet)
- 가드 스펙: 마커 없는 `@Cron` · 이름 중복 · `CronOnceModule` 누락
- 통합 스펙: 앱 컨텍스트 둘의 선점 경쟁 (`npm run test:cron-once:integration`)
- ADR-0036, CLAUDE.md 크론 절

## 왜
#707 감사가 이연한 과제. 설계 스펙 `docs/superpowers/specs/2026-09-10-cron-once-design.md`, 결정 근거 `docs/adr/0036-*.md`.

## 배포
expand 이므로 **`migrate → deploy`**. `lcnine-services` 와 `lcnine-auth`(user-service) 양쪽 마이그레이션 선행.

## 실측
(Task 13 psql 출력)

https://claude.ai/code/session_01Wc3bLKCeRbM6pzjspyK2zt
```

- [ ] **Step 2: CI 확인**

```bash
gh pr checks --watch
```

Expected: verification-gates 초록. 빨간 것이 있으면 이 PR 이 만든 것이다.
