/**
 * "브랜드" 루트 카테고리(브랜드관)의 slug. 이 카테고리의 직계 자식이 브랜드다.
 * 스토어프론트가 같은 값(BRAND_CATEGORY_HANDLE)으로 홈 브랜드 섹션·브랜드형
 * 카테고리 헤더를 판정하므로, 브랜드 카테고리 편집 화면에 안내를 띄우는 데 쓴다.
 */
export const BRAND_ROOT_SLUG = 'cafe24-cat-728';
