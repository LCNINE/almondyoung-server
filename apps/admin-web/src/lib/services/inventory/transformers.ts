// src/lib/services/inventory/transformers.ts
// 재고 관련 데이터 변환 로직

// 임시 구현 - 실제 데이터 변환이 필요한 경우 구현
export const transformStock = (dto: any) => dto;
export const transformSku = (dto: any) => dto;
export const transformWarehouse = (dto: any) => dto;
export const transformInbound = (dto: any) => dto;
export const transformInspection = (dto: any) => dto;
export const transformMovement = (dto: any) => dto;
export const transformConsolidation = (dto: any) => dto;
