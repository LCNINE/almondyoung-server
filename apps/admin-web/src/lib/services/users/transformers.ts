// src/lib/services/users/transformers.ts
// 사용자 관련 데이터 변환 로직

// 임시 구현 - 실제 데이터 변환이 필요한 경우 구현
export const transformUser = (dto: any) => dto;
export const transformAdmin = (dto: any) => dto;
export const transformConsent = (dto: any) => dto;
export const transformBusinessLicense = (dto: any) => dto;
