# 멤버십 정책 테이블 명세서

> **버전**: 1.0.0  
> **작성일**: 2025-10-17  
> **목적**: 하드코딩된 비즈니스 정책을 DB 테이블로 관리하여 유연성 확보

---

## 📌 왜 정책 테이블이 필요한가?

### 현재 문제점 (하드코딩)

```typescript
// ❌ 코드에 박혀있는 정책들
const TRIAL_DAYS = 7; // 무료 체험 기간
const REFUND_WINDOW_HOURS = 24; // 재구독 환불 가능 시간
const MAX_PAUSE_DAYS = 90; // 최대 일시정지 기간
const MIN_PAUSE_DAYS = 7; // 최소 일시정지 기간

// 정책 변경 시:
// 1. 코드 수정
// 2. 테스트
// 3. 배포
// 4. 서버 재시작 ← 다운타임!
```

### 정책 테이블 사용 시

```typescript
// ✅ DB에서 동적으로 로드
const policy = await policyService.getPolicy('RESUBSCRIPTION_REFUND_WINDOW');
const refundWindowHours = policy.value; // 24

// 정책 변경 시:
// 1. DB UPDATE만 실행
// 2. 즉시 반영 (재시작 불필요)
// 3. 이력 추적 가능
```

---

## 🗄️ 기존 스키마 확인

```typescript
// apps/membership/src/shared/schemas/entities/schema.ts

export const policyRuleTypeEnum = pgEnum('policy_rule_type', [
  // Pause-related policies
  'MAX_PAUSES_PER_YEAR',
  'MIN_PAUSE_DURATION_DAYS',
  'MAX_PAUSE_DURATION_DAYS',
  'PAUSE_COOLDOWN_DAYS',
  'PAUSE_BLACKOUT_PERIODS',

  // Plan change policies
  'PLAN_CHANGE_COOLDOWN_DAYS',
  'ALLOWED_PLAN_CHANGES',
  'DOWNGRADE_RESTRICTIONS',
  'UPGRADE_BENEFITS',

  // Tier-specific policies
  'TIER_SPECIFIC_LIMITS',
  'VIP_USER_BENEFITS',
  'NEW_USER_GRACE_PERIOD',

  // Promotional policies
  'PROMOTIONAL_PERIODS',
  'SEASONAL_RESTRICTIONS',
  'SPECIAL_EVENT_RULES',
]);

export const subscriptionPolicies = pgTable('subscription_policies', {
  id: uuid('id').primaryKey().defaultRandom(),
  ruleType: policyRuleTypeEnum('rule_type').notNull(),
  ruleValue: jsonb('rule_value').notNull(),
  tierId: uuid('tier_id').references(() => tiers.id), // NULL이면 전체 티어 적용
  isActive: boolean('is_active').default(true).notNull(),
  validFrom: date('valid_from'),
  validUntil: date('valid_until'),
  createdAt: timestamp('created_at', { withTimezone: true })
    .defaultNow()
    .notNull(),
  updatedAt: timestamp('updated_at', { withTimezone: true })
    .defaultNow()
    .notNull(),
});
```

---

## 📋 정책 타입 확장 제안

### 1. 환불 관련 정책 추가

```typescript
export const policyRuleTypeEnum = pgEnum('policy_rule_type', [
  // ... 기존 정책들 ...

  // Refund policies (신규 추가)
  'TRIAL_REFUND_ENABLED', // 무료 체험 중 환불 가능 여부
  'RESUBSCRIPTION_REFUND_WINDOW_HOURS', // 재구독 후 환불 가능 시간
  'BENEFIT_USAGE_AFFECTS_REFUND', // 혜택 사용 시 환불 영향 여부
  'PARTIAL_REFUND_CALCULATION_METHOD', // 부분 환불 계산 방식
  'REFUND_PROCESSING_DAYS', // 환불 처리 소요 일수

  // Trial policies (신규 추가)
  'TRIAL_DURATION_DAYS', // 무료 체험 기간
  'TRIAL_REUSE_PREVENTION', // 무료 체험 재사용 방지 여부
  'TRIAL_COOLDOWN_DAYS', // 무료 체험 재사용 가능 대기 기간
]);
```

---

## 📊 정책 데이터 구조

### 1. 단순 값 정책

```json
// MAX_PAUSE_DURATION_DAYS
{
  "id": "uuid-1",
  "ruleType": "MAX_PAUSE_DURATION_DAYS",
  "ruleValue": {
    "days": 90
  },
  "tierId": null,
  "isActive": true,
  "validFrom": null,
  "validUntil": null
}
```

### 2. 티어별 정책

```json
// TRIAL_DURATION_DAYS (티어별 다른 체험 기간)
{
  "id": "uuid-2",
  "ruleType": "TRIAL_DURATION_DAYS",
  "ruleValue": {
    "days": 14
  },
  "tierId": "premium-tier-uuid",
  "isActive": true,
  "validFrom": null,
  "validUntil": null
}

{
  "id": "uuid-3",
  "ruleType": "TRIAL_DURATION_DAYS",
  "ruleValue": {
    "days": 7
  },
  "tierId": "basic-tier-uuid",
  "isActive": true,
  "validFrom": null,
  "validUntil": null
}
```

### 3. 복잡한 정책 (조건부)

```json
// RESUBSCRIPTION_REFUND_WINDOW_HOURS
{
  "id": "uuid-4",
  "ruleType": "RESUBSCRIPTION_REFUND_WINDOW_HOURS",
  "ruleValue": {
    "hours": 24,
    "conditions": {
      "benefitUsageAllowed": false,
      "maxRefundAmount": null,
      "requiresApproval": false
    }
  },
  "tierId": null,
  "isActive": true,
  "validFrom": "2025-01-01",
  "validUntil": null
}
```

### 4. 블랙아웃 기간 정책

```json
// PAUSE_BLACKOUT_PERIODS
{
  "id": "uuid-5",
  "ruleType": "PAUSE_BLACKOUT_PERIODS",
  "ruleValue": {
    "periods": [
      {
        "name": "블랙프라이데이",
        "startDate": "2025-11-20",
        "endDate": "2025-11-30",
        "reason": "프로모션 기간 중 일시정지 불가"
      },
      {
        "name": "연말 세일",
        "startDate": "2025-12-20",
        "endDate": "2025-12-31",
        "reason": "연말 프로모션 기간"
      }
    ]
  },
  "tierId": null,
  "isActive": true,
  "validFrom": "2025-01-01",
  "validUntil": "2025-12-31"
}
```

---

## 🏗️ 서비스 구현

### 1. PolicyService 인터페이스

```typescript
// apps/membership/src/services/membership-policy.service.ts

import { Injectable, Logger } from '@nestjs/common';
import { DbService } from '@app/db';
import { membershipSchema } from '../shared/schemas/entities/schema';
import * as schema from '../shared/schemas/entities/schema';
import { eq, and, lte, gte, or, isNull } from 'drizzle-orm';

export type PolicyRuleType =
  | 'MAX_PAUSES_PER_YEAR'
  | 'MIN_PAUSE_DURATION_DAYS'
  | 'MAX_PAUSE_DURATION_DAYS'
  | 'PAUSE_COOLDOWN_DAYS'
  | 'PAUSE_BLACKOUT_PERIODS'
  | 'TRIAL_DURATION_DAYS'
  | 'TRIAL_REUSE_PREVENTION'
  | 'RESUBSCRIPTION_REFUND_WINDOW_HOURS'
  | 'BENEFIT_USAGE_AFFECTS_REFUND'
  | 'PARTIAL_REFUND_CALCULATION_METHOD';

export interface PolicyValue {
  [key: string]: any;
}

export interface Policy {
  id: string;
  ruleType: PolicyRuleType;
  ruleValue: PolicyValue;
  tierId: string | null;
  isActive: boolean;
  validFrom: string | null;
  validUntil: string | null;
}

@Injectable()
export class MembershipPolicyService {
  private readonly logger = new Logger(MembershipPolicyService.name);

  // 캐시 (선택적 - 성능 최적화)
  private policyCache = new Map<string, Policy>();
  private cacheExpiry = new Map<string, number>();
  private readonly CACHE_TTL = 5 * 60 * 1000; // 5분

  constructor(private readonly dbService: DbService<typeof membershipSchema>) {}

  /**
   * 정책 조회 (티어별 우선순위)
   *
   * 우선순위:
   * 1. 티어별 정책 (tierId 일치)
   * 2. 전체 티어 정책 (tierId = null)
   */
  async getPolicy(
    ruleType: PolicyRuleType,
    tierId?: string,
  ): Promise<Policy | null> {
    const cacheKey = `${ruleType}:${tierId || 'global'}`;

    // 캐시 확인
    if (this.isCacheValid(cacheKey)) {
      return this.policyCache.get(cacheKey) || null;
    }

    const today = new Date().toISOString().split('T')[0];

    // 1. 티어별 정책 조회 (우선)
    if (tierId) {
      const tierPolicy = await this.dbService.db
        .select()
        .from(schema.subscriptionPolicies)
        .where(
          and(
            eq(schema.subscriptionPolicies.ruleType, ruleType),
            eq(schema.subscriptionPolicies.tierId, tierId),
            eq(schema.subscriptionPolicies.isActive, true),
            or(
              isNull(schema.subscriptionPolicies.validFrom),
              lte(schema.subscriptionPolicies.validFrom, today),
            ),
            or(
              isNull(schema.subscriptionPolicies.validUntil),
              gte(schema.subscriptionPolicies.validUntil, today),
            ),
          ),
        )
        .limit(1);

      if (tierPolicy.length > 0) {
        this.setCache(cacheKey, tierPolicy[0]);
        return tierPolicy[0];
      }
    }

    // 2. 전체 티어 정책 조회 (fallback)
    const globalPolicy = await this.dbService.db
      .select()
      .from(schema.subscriptionPolicies)
      .where(
        and(
          eq(schema.subscriptionPolicies.ruleType, ruleType),
          isNull(schema.subscriptionPolicies.tierId),
          eq(schema.subscriptionPolicies.isActive, true),
          or(
            isNull(schema.subscriptionPolicies.validFrom),
            lte(schema.subscriptionPolicies.validFrom, today),
          ),
          or(
            isNull(schema.subscriptionPolicies.validUntil),
            gte(schema.subscriptionPolicies.validUntil, today),
          ),
        ),
      )
      .limit(1);

    if (globalPolicy.length > 0) {
      this.setCache(cacheKey, globalPolicy[0]);
      return globalPolicy[0];
    }

    return null;
  }

  /**
   * 정책 값 추출 (타입 안전)
   */
  async getPolicyValue<T = any>(
    ruleType: PolicyRuleType,
    tierId?: string,
    defaultValue?: T,
  ): Promise<T> {
    const policy = await this.getPolicy(ruleType, tierId);

    if (!policy) {
      if (defaultValue !== undefined) {
        this.logger.warn(`Policy not found, using default: ${ruleType}`, {
          defaultValue,
        });
        return defaultValue;
      }
      throw new Error(`Policy not found: ${ruleType}`);
    }

    return policy.ruleValue as T;
  }

  /**
   * 숫자 정책 값 추출
   */
  async getNumberPolicy(
    ruleType: PolicyRuleType,
    key: string,
    tierId?: string,
    defaultValue?: number,
  ): Promise<number> {
    const value = await this.getPolicyValue(ruleType, tierId, {
      [key]: defaultValue,
    });
    return value[key];
  }

  /**
   * 불린 정책 값 추출
   */
  async getBooleanPolicy(
    ruleType: PolicyRuleType,
    key: string,
    tierId?: string,
    defaultValue?: boolean,
  ): Promise<boolean> {
    const value = await this.getPolicyValue(ruleType, tierId, {
      [key]: defaultValue,
    });
    return value[key];
  }

  /**
   * 정책 생성/업데이트
   */
  async upsertPolicy(
    ruleType: PolicyRuleType,
    ruleValue: PolicyValue,
    tierId?: string,
    validFrom?: string,
    validUntil?: string,
  ): Promise<Policy> {
    const [policy] = await this.dbService.db
      .insert(schema.subscriptionPolicies)
      .values({
        ruleType,
        ruleValue,
        tierId: tierId || null,
        isActive: true,
        validFrom: validFrom || null,
        validUntil: validUntil || null,
      })
      .onConflictDoUpdate({
        target: [
          schema.subscriptionPolicies.ruleType,
          schema.subscriptionPolicies.tierId,
        ],
        set: {
          ruleValue,
          validFrom: validFrom || null,
          validUntil: validUntil || null,
          updatedAt: new Date(),
        },
      })
      .returning();

    // 캐시 무효화
    this.invalidateCache(ruleType, tierId);

    return policy;
  }

  /**
   * 정책 비활성화
   */
  async deactivatePolicy(id: string): Promise<void> {
    await this.dbService.db
      .update(schema.subscriptionPolicies)
      .set({
        isActive: false,
        updatedAt: new Date(),
      })
      .where(eq(schema.subscriptionPolicies.id, id));

    // 전체 캐시 무효화
    this.policyCache.clear();
    this.cacheExpiry.clear();
  }

  /**
   * 캐시 관리
   */
  private isCacheValid(key: string): boolean {
    const expiry = this.cacheExpiry.get(key);
    if (!expiry) return false;
    return Date.now() < expiry;
  }

  private setCache(key: string, policy: Policy): void {
    this.policyCache.set(key, policy);
    this.cacheExpiry.set(key, Date.now() + this.CACHE_TTL);
  }

  private invalidateCache(ruleType: PolicyRuleType, tierId?: string): void {
    const globalKey = `${ruleType}:global`;
    const tierKey = `${ruleType}:${tierId}`;

    this.policyCache.delete(globalKey);
    this.policyCache.delete(tierKey);
    this.cacheExpiry.delete(globalKey);
    this.cacheExpiry.delete(tierKey);
  }
}
```

---

## 🎯 사용 예시

### 1. 환불 정책 적용

```typescript
// subscription-cancellation.manager.ts

async checkRefundEligibility(
  contract: Contract,
  plan: Plan,
): Promise<RefundEligibility> {
  // 1. 무료 체험 환불 정책 확인
  const trialRefundEnabled = await this.policyService.getBooleanPolicy(
    'TRIAL_REFUND_ENABLED',
    'enabled',
    plan.tierId,
    true, // 기본값
  );

  if (trialRefundEnabled && this.isInTrialPeriod(contract, plan)) {
    return {
      eligible: true,
      reason: '무료 체험 기간 중 취소',
      amount: plan.price,
    };
  }

  // 2. 재구독 환불 정책 확인
  const refundWindowHours = await this.policyService.getNumberPolicy(
    'RESUBSCRIPTION_REFUND_WINDOW_HOURS',
    'hours',
    plan.tierId,
    24, // 기본값
  );

  const hoursSinceCreation = differenceInHours(new Date(), contract.createdAt);

  if (hoursSinceCreation < refundWindowHours) {
    const isResubscription = await this.isRecentResubscription(contract);

    if (isResubscription) {
      // 3. 혜택 사용 영향 정책 확인
      const benefitAffectsRefund = await this.policyService.getBooleanPolicy(
        'BENEFIT_USAGE_AFFECTS_REFUND',
        'enabled',
        plan.tierId,
        true,
      );

      if (benefitAffectsRefund) {
        const hasBenefitUsage = await this.hasBenefitUsage(contract.id);

        if (!hasBenefitUsage) {
          return {
            eligible: true,
            reason: `재구독 후 ${refundWindowHours}시간 이내 + 혜택 미사용`,
            amount: plan.price,
          };
        } else {
          // 부분 환불
          const usedAmount = await this.calculateUsedBenefitAmount(contract.id);
          return {
            eligible: true,
            reason: '재구독 후 혜택 사용 - 부분 환불',
            amount: Math.max(0, plan.price - usedAmount),
          };
        }
      } else {
        // 혜택 사용 여부 무시
        return {
          eligible: true,
          reason: `재구독 후 ${refundWindowHours}시간 이내`,
          amount: plan.price,
        };
      }
    }
  }

  return {
    eligible: false,
    reason: '환불 가능 기간이 지났습니다',
    amount: 0,
  };
}
```

### 2. 일시정지 정책 적용

```typescript
// pause.service.ts

async validatePauseRequest(
  userId: string,
  startDate: Date,
  endDate: Date,
  tierId: string,
): Promise<void> {
  // 1. 최소/최대 기간 확인
  const minDays = await this.policyService.getNumberPolicy(
    'MIN_PAUSE_DURATION_DAYS',
    'days',
    tierId,
    7,
  );

  const maxDays = await this.policyService.getNumberPolicy(
    'MAX_PAUSE_DURATION_DAYS',
    'days',
    tierId,
    90,
  );

  const pauseDays = differenceInDays(endDate, startDate);

  if (pauseDays < minDays) {
    throw new Error(`최소 ${minDays}일 이상 일시정지해야 합니다`);
  }

  if (pauseDays > maxDays) {
    throw new Error(`최대 ${maxDays}일까지만 일시정지 가능합니다`);
  }

  // 2. 블랙아웃 기간 확인
  const blackoutPolicy = await this.policyService.getPolicyValue<{
    periods: Array<{
      name: string;
      startDate: string;
      endDate: string;
      reason: string;
    }>;
  }>('PAUSE_BLACKOUT_PERIODS', tierId);

  if (blackoutPolicy?.periods) {
    for (const period of blackoutPolicy.periods) {
      const blackoutStart = new Date(period.startDate);
      const blackoutEnd = new Date(period.endDate);

      if (
        (startDate >= blackoutStart && startDate <= blackoutEnd) ||
        (endDate >= blackoutStart && endDate <= blackoutEnd)
      ) {
        throw new Error(
          `${period.name} 기간에는 일시정지할 수 없습니다: ${period.reason}`,
        );
      }
    }
  }

  // 3. 연간 최대 횟수 확인
  const maxPausesPerYear = await this.policyService.getNumberPolicy(
    'MAX_PAUSES_PER_YEAR',
    'count',
    tierId,
    2,
  );

  const pauseCount = await this.getPauseCountThisYear(userId);

  if (pauseCount >= maxPausesPerYear) {
    throw new Error(`연간 최대 ${maxPausesPerYear}회까지만 일시정지 가능합니다`);
  }
}
```

---

## 📝 초기 데이터 마이그레이션

```sql
-- 기본 정책 데이터 삽입

-- 1. 무료 체험 정책
INSERT INTO subscription_policies (rule_type, rule_value, tier_id, is_active)
VALUES
  ('TRIAL_DURATION_DAYS', '{"days": 7}', NULL, true),
  ('TRIAL_REUSE_PREVENTION', '{"enabled": true}', NULL, true);

-- 2. 환불 정책
INSERT INTO subscription_policies (rule_type, rule_value, tier_id, is_active)
VALUES
  ('TRIAL_REFUND_ENABLED', '{"enabled": true}', NULL, true),
  ('RESUBSCRIPTION_REFUND_WINDOW_HOURS', '{"hours": 24, "conditions": {"benefitUsageAllowed": false}}', NULL, true),
  ('BENEFIT_USAGE_AFFECTS_REFUND', '{"enabled": true}', NULL, true);

-- 3. 일시정지 정책
INSERT INTO subscription_policies (rule_type, rule_value, tier_id, is_active)
VALUES
  ('MIN_PAUSE_DURATION_DAYS', '{"days": 7}', NULL, true),
  ('MAX_PAUSE_DURATION_DAYS', '{"days": 90}', NULL, true),
  ('MAX_PAUSES_PER_YEAR', '{"count": 2}', NULL, true),
  ('PAUSE_COOLDOWN_DAYS', '{"days": 30}', NULL, true);

-- 4. 블랙아웃 기간 (예시)
INSERT INTO subscription_policies (rule_type, rule_value, tier_id, is_active, valid_from, valid_until)
VALUES
  ('PAUSE_BLACKOUT_PERIODS',
   '{"periods": [
     {"name": "블랙프라이데이", "startDate": "2025-11-20", "endDate": "2025-11-30", "reason": "프로모션 기간"},
     {"name": "연말 세일", "startDate": "2025-12-20", "endDate": "2025-12-31", "reason": "연말 프로모션"}
   ]}',
   NULL, true, '2025-01-01', '2025-12-31');
```

---

## 🧪 테스트

```typescript
// membership-policy.service.spec.ts

describe('MembershipPolicyService', () => {
  let service: MembershipPolicyService;
  let db: DbService;

  beforeEach(async () => {
    const module = await Test.createTestingModule({
      providers: [MembershipPolicyService, DbService],
    }).compile();

    service = module.get(MembershipPolicyService);
    db = module.get(DbService);
  });

  describe('정책 조회', () => {
    it('전체 티어 정책을 조회해야 함', async () => {
      const policy = await service.getPolicy('TRIAL_DURATION_DAYS');

      expect(policy).toBeDefined();
      expect(policy?.ruleValue).toEqual({ days: 7 });
    });

    it('티어별 정책이 전체 정책보다 우선해야 함', async () => {
      const premiumTierId = 'premium-tier-uuid';

      // 전체 정책: 7일
      // 프리미엄 정책: 14일
      const policy = await service.getPolicy(
        'TRIAL_DURATION_DAYS',
        premiumTierId,
      );

      expect(policy?.ruleValue).toEqual({ days: 14 });
    });

    it('숫자 정책 값을 추출해야 함', async () => {
      const days = await service.getNumberPolicy(
        'MAX_PAUSE_DURATION_DAYS',
        'days',
      );

      expect(days).toBe(90);
    });
  });

  describe('정책 캐싱', () => {
    it('같은 정책을 두 번 조회하면 캐시를 사용해야 함', async () => {
      const spy = jest.spyOn(db.db, 'select');

      await service.getPolicy('TRIAL_DURATION_DAYS');
      await service.getPolicy('TRIAL_DURATION_DAYS');

      expect(spy).toHaveBeenCalledTimes(1); // 한 번만 DB 조회
    });
  });
});
```

---

## ✅ 구현 체크리스트

### Phase 1: 기본 구조 (1일)

- [ ] PolicyService 구현
- [ ] 정책 조회 메서드
- [ ] 캐싱 로직
- [ ] 단위 테스트

### Phase 2: 정책 적용 (2일)

- [ ] 환불 정책 적용 (subscription-cancellation.manager.ts)
- [ ] 일시정지 정책 적용 (pause.service.ts)
- [ ] 무료 체험 정책 적용 (subscription.creator.ts)
- [ ] 통합 테스트

### Phase 3: 관리 기능 (1일)

- [ ] Admin API (정책 CRUD)
- [ ] 정책 이력 추적
- [ ] 정책 검증 로직

---

## 🎯 핵심 포인트

1. **유연성**: 코드 수정 없이 정책 변경 가능
2. **티어별 차별화**: 프리미엄 사용자에게 더 나은 정책 제공
3. **시간 제한**: validFrom/validUntil로 프로모션 기간 관리
4. **성능**: 5분 캐싱으로 DB 부하 최소화
5. **안전성**: 기본값 제공으로 정책 누락 방지

---

**END OF SPEC v1.0.0**
