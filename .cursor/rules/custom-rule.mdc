---
description: 
globs: 
alwaysApply: true
---
# Custom Rules

## HMS API 타입 주의사항
- HMS API 래퍼의 `memberId`는 반드시 **문자열**로 전달해야 함 (`dto.userId.toString()` 사용)
- 숫자 타입 전달 시 "회원 ID가 중복됩니다" 에러가 발생할 수 있음


