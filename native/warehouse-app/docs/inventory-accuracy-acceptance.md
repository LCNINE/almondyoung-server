# 재고 정확성 구현 및 합격 검사 기록

검증일: 2026-09-15. 기준 커밋: `015bb0760`. 작업 브랜치: `fix/warehouse-inventory-accuracy`.

## 구현된 동작

- 입고·출고·실사에서 접수한 동일 상품 연속 스캔을 개별 이벤트로 보존하고 순서대로 처리한다. 발주 목록 선택은 잔량을 제안하고, 바코드 첫 스캔은 실제 1개 또는 포장 단위부터 센다.
- 재고 변경 전에 IndexedDB에 원래 본문·작업 키·사용자/API 범위를 저장한다. 응답이 유실되거나 손상되면 같은 작업을 확인하며, 결과를 알기 전에 내용을 바꾸거나 새 키로 다시 처리하지 않는다. 다른 계정으로 자동 전송하지 않는다.
- 앱을 다시 열면 입고 초안·접수 스캔·미확인 작업을 복구한다. 적치 완료 기록을 입고 화면의 누계에 다시 반영하며, 완료 직후 화면 저장 전에 종료되어도 잔여량이 되돌아가지 않는다.
- 서버 v2 계약은 인증된 사용자와 원래 본문을 중복 방지에 포함한다. 동일 키의 다른 요청은 거절하고, 확정 결과는 재생한다. JSONB에서 읽은 날짜도 정상 응답으로 변환한다.
- 실사는 카운트 revision, 최초 카운트 시점의 원장 version, 검토한 전체 세션의 previewToken을 확인한다. 카운트 이후 재고가 변하면 다시 세기를 요구한다. null(미카운트)과 0(명시적 실사 결과)을 구별한다.
- 실사 완료와 재고 증가·감소가 같은 재고 잠금을 사용한다. 검토한 재고 값에 대한 조정만 원자적으로 반영한다.
- 일반 화면에는 필요한 행동 안내만 표시한다. 개발자 모드는 기본 꺼짐이며 서버 `inventory.manage` 권한 확인 후 직접 켜야 한다. 진단 기록은 24시간/1,000건의 제한된 메타데이터만 보관한다.
- admin-web 실사도 v2·카운트 revision·previewToken·영속 재확인을 사용한다. 실제 CustomError 형식을 처리하며, 인식할 수 없는 성공 응답도 미확인 작업으로 보존한다.

## 실제 검증 환경

기존 로컬 PostgreSQL을 이용하되 `warehouse_accuracy_task2_20260915`, `warehouse_accuracy_http_20260915`라는 테스트 전용 DB만 생성했다. 기존 DB를 초기화하지 않았다. 추가 스키마 마이그레이션은 두 테스트 DB에만 적용했다.

로컬 Redis·Kafka·ZooKeeper와 core HTTP 서버를 기동했다. 실제 앱의 React 화면, API 제공자, IndexedDB 실행기를 로컬 core에 연결해 사용했다. 브라우저 검증용으로만 Tauri HTTP를 browser fetch로 바꾸고 로컬 테스트 계정을 주입했다. 이 어댑터와 인증 fixture는 저장소 밖 임시 파일이며 제품 코드에 인증 우회를 추가하지 않았다.

이 검증은 브라우저 창을 닫았다 다시 여는 복구를 포함한다. Android/Windows Tauri 프로세스 강제 종료, 실제 HID/PDA 하드웨어, 실제 Wi-Fi 단절 검증을 대신하지 않는다.

## 검증 결과

| 범위 | 결과와 증거 |
|---|---|
| warehouse-app 전체 | 73 files / 353 tests 통과. 타입 검사와 production build 통과 |
| 서버 최종 실사·공통 잠금·회수·권한 검사 | 19 suites / 85 tests 통과, skip 없음 |
| 입고·발주·출고·회수·실제 HTTP 재생 회귀 | 11 suites / 86 tests 통과, skip 없음 |
| admin-web 최종 클라이언트 회귀 | 25 tests 통과. CustomError, 손상된 2xx, 동일 본문 복구, 계정 변경, 29일 제한 포함 |
| core/admin 타입 검사 | 모두 통과 |
| 실사 완료 중 입고/증가 조정/내부 이동/출고 취소 | 실제 DB 경합 4건 통과. 대기 잠금 확인 후 실사 −1과 증가 +3이 각각 적용되어 최종 7개 |
| 실제 HTTP 입고 10개 | 같은 키 두 번 모두 201, 동일 입고. 본문 변경 409. 한 번만 반영 |
| 실제 HTTP 권한·전환 설정 | 작업 계약 v2 확인, 일반 작업자 진단 403, v1 차단 설정 활성 시 미보호 요청 426 |
| 앱 정상 입고·적치 | 두 번 스캔 → 초안 2개, 새로 열어도 2개, 입고 2개 후 A-01-01로 2개 적치. 출발/도착 DB 확인 |
| 앱 응답 유실·창 재개 | 서버가 입고를 커밋한 뒤 성공 응답을 버림. 창을 다시 열어 같은 키로 확인 → 입고 1개 복구. DB 입고 행 정확히 1건 |
| 일반 화면·개발자 모드 | 일반 홈에서 진단 숨김. 권한 확인 후 켤 수 있고 새로 열면 다시 꺼짐 |
| 부분 적치 직후 종료 | 간편입고·발주입고 모두 완료 기록 3개/초안 10개를 복원하여 잔여 7개. 반복 재개에도 누계 중복 없음 |

서버 테스트 묶음은 서로 겹칠 수 있으므로 합산해 고유 테스트 총수라고 해석하지 않는다. 최초 TDD 실패 로그와 리뷰 재현 테스트는 별도 실행 기록에 보관했다. 실사 카운트가 바뀐 뒤 완료가 계속 열리는 문제, 중복 실사 스캔, 원장 경합, 날짜 재생 실패, 적치 복구 누락은 수정 전 실패와 수정 후 통과를 확인했다.

## 현장 배포 전 남은 검사

- [ ] Android PDA와 Windows 각각 실제 HID 연속 A·A·B 및 100스캔, 포장 단위, 입력 포커스·스캐너 Enter를 검사한다.
- [ ] 네이티브 프로세스를 커밋 직후 강제 종료하고 미확인 작업·초안·실사 위치·적치 잔여량을 복구한다.
- [ ] 실제 Wi-Fi 단절/복구, 인증 만료/재로그인, 디스크 부족 상태에서 작업자 안내와 재고 대사를 확인한다.
- [ ] 두 작업자의 실제 출고 박스 담당자/리스 충돌을 확인한다. 서버 회귀 테스트 통과와 현장 조작 검증을 구분한다.
- [ ] 지정 계정·기기의 제한된 시험 운영에서 당일 원장/실물 대사를 통과한다.

## 배포와 중단 순서

1. `20260914183823_stocktaking-count-baseline.sql`을 정상 운영 절차로 적용한다. 기존 카운트의 기준 version은 만들어 넣지 않으며 해당 실사는 다시 센다.
2. v2 서버를 먼저 배포한다. 이때 `WAREHOUSE_REQUIRE_OPERATION_V2`는 비활성으로 두어 기존 클라이언트 호환성을 유지한다. 이 단계만으로 기존 클라이언트의 안전성이 확보되지는 않는다.
3. warehouse-app과 admin-web을 배포하고 계약 v2 확인 및 복구 동작을 검사한다. 기존 미확인 작업을 먼저 대사하며, 과거 키를 계약 버전을 바꿔 재전송하지 않는다.
4. 현장 장비 검사와 시험 운영 후 `WAREHOUSE_REQUIRE_OPERATION_V2=true`로 미보호 v1 재고 변경을 차단한다. 전환 설정은 운영에 아직 적용하지 않았다.
5. 문제가 생기면 새로운 재고 변경을 멈추고 미확인 작업을 보존한다. 이전 불안전 클라이언트로 자동 복귀하거나 저장소를 삭제하지 않는다. 서버 기록·실물을 대사한 뒤 재개한다.

작업 원본은 서버 보존 기간보다 짧은 29일까지만 자동 재확인한다. 오래된 미확인 건은 관리자 대사가 필요하다. 진단 로그 삭제와 업무 기록 삭제는 분리되어 있다.

## 실행 중 판단과 그 한계

- 실행 스킬에 따라 서버 담당과 앱 담당을 분리하고, 같은 파일을 동시에 수정하지 않았다. 판단이 잘못되면 계약 통합 누락이 생길 수 있어 별도 검토와 통합 테스트로 확인했다.
- 서버 v1 호환성을 유지하는 additive 배포로 정했다. 새 앱은 v2 capability가 없으면 변경을 허용하지 않는다. 이 판단의 비용은 최종 v1 차단 전까지 구형 호출의 기존 위험이 남는다는 점이며, 전환 설정을 별도 배포 단계로 명시했다.
- 로컬 테스트 기동은 사용자가 승인한 범위로 실행하고 원격 배포/병합은 실행하지 않았다. 이 판단의 비용은 운영 반영 절차가 별도로 남는다는 점이다.

## 최종 실행 기록

- `npm test`: 73 files / 353 tests, 실패·skip 0.
- `npm run build`: TypeScript 검사 및 Vite production build 통과.
- `npm run lint`: exit 0, 오류 0, 경고 19개. React Fast Refresh export 및 Hook 의존성 경고가 남아 있으며 lint 무경고 상태라고 주장하지 않는다. 빌드에도 500kB 초과 chunk 경고가 남아 있다.
- `git diff --check`: 통과.
- 실사 스캔은 접수 시 위치/상품 의도를 저장한다. 위치 응답 전에 이어진 상품 입력 및 위치 초안 저장 후 종료를 복원하는 회귀 테스트가 통과했다. 위치가 맞지 않은 미전송 입력만 명시적으로 제외할 수 있으며, 이미 영속 작업이 존재하면 제외할 수 없다.
- 임시 core HTTP 및 브라우저 검증용 개발 서버는 종료했다. 전용 테스트 DB와 로컬 Docker 의존 서비스는 남겨 재검증할 수 있다.


최종 독립 검토: 공통 서버 계약·실사 경합·관리자 화면·적치 복구·실사 스캔 복구를 검토하고 발견한 사항을 수정했다. 검토 범위에서 남은 P1/P2 결함 없음. 실사 화면 15개 테스트도 검토자가 별도로 통과시켰다.

## PR #874 이후 후속 검증 — 스캔 저장 실패

검증일: 2026-09-15. 최신 `origin/develop`을 fetch하여 [PR #874](https://github.com/LCNINE/almondyoung-server/pull/874)의 머지 커밋 `c1469f854f36b832db4c75df917535dd9897f2a6`에서 시작했다. 작업 브랜치는 `codex/warehouse-readiness-followup`이다. 위 결과는 PR #874 당시의 기록이며, 아래 결과와 구분한다.

### 조사한 흐름과 우선순위

PR 본문과 이 문서, `docs/superpowers/specs/inventory-accuracy-design.md`를 먼저 읽었다. 다음 결함을 미리 정하지 않고 기록된 미검증 조건과 각 화면의 입력·저장·복구 경계를 추적했다.

| 흐름 | 이번에 확인한 경계 | 증거와 한계 |
|---|---|---|
| 간편·발주 입고 | 스캔 저장 실패/초기 복원 실패와 부분 수량 확정 | 공통 처리기 및 화면 코드 조사. 간편입고 결함을 화면 회귀 검사로 재현. 수정 후 로컬 HTTP·DB 확인 |
| 적치 | 입고의 확정 기록에서 잔여량 복원, 별도 적치 대기 목록의 제출 경로 | 코드 조사. 공통 입고 화면 변경에 따른 기존 부분 적치 회귀만 실행. 실제 선반 작업을 재검증한 것은 아님 |
| 이동 | 영속 작업 실행기, 이동 수량·출발 재고 검증 | 화면·서버 경로 조사. 이번에 수정한 스캔 수량 대기열을 사용하지 않으므로 기존 이동 DB 검사는 반복하지 않음 |
| 출고 | 스캔 저장 실패 중 수량 지정·강제출고 가능 여부 | 공통 처리기와 화면 차단 조건 조사 및 영향 범위 회귀. 두 작업자의 실제 박스 담당자/리스 충돌은 미검증 |
| 실사 | 스캔 저장/복원 실패 중 위치 전환·수동 정정 가능 여부 | 공통 처리기와 화면 차단 조건 조사 및 영향 범위 회귀. 이전 원장 경합 DB 검사는 반복하지 않음 |

실제 장비가 필요한 항목은 소프트웨어 검사로 대체하지 않았다. 공통 처리기에서 수량을 누락시키는 아래 결함이 재현되어 이번 우선 수정 대상으로 정했다. 취소·보류 등 별도 기능 확대는 하지 않았다.

### 확인한 결함: B1 — 저장 실패를 빈 스캔 대기열로 취급 (P1)

1. 간편입고에서 같은 상품을 한 번 스캔해 초안 수량 1을 만든다.
2. 두 번째 스캔의 IndexedDB 저장에 `QuotaExceededError`를 주입한다.
3. 수정 전에는 그 입력이 사라지고 대기 수가 0으로 돌아가 **등록 버튼이 활성화**된다. 작업자는 실물 2개를 1개로 입고할 수 있다.
4. 저장 공간이 복구되어도 화면의 “다시 확인”은 이미 사라진 입력을 되살리지 못한다. 별도 A·A·B 검사에서도 복구 후 처리 결과가 빈 배열이었다.

재개 시에도 같은 문제가 있었다. 초안 1개와 별도의 미처리 스캔 1개를 저장한 뒤, 스캔 기록 읽기만 실패시키면 초안 복원이 끝난 후 등록이 활성화됐다. 따라서 저장 실패뿐 아니라 아직 읽지 못한 입력도 확정 차단 조건에 포함해야 한다.

수정 전 실패 증거는 `ReliabilityReview.test.tsx`의 두 등록 차단 검사와 `useWorkScanQueue.test.tsx`의 입력 복구 검사다. 실패 로그는 로컬 `/tmp/warehouse-readiness-storage-red.log`, `/tmp/warehouse-readiness-queue-red.log`, `/tmp/warehouse-readiness-read-failure-red.log`에 보관했다. DB에 잘못된 수량을 일부러 반영한 재현은 아니다.

### 수정 내용

- `useWorkScanQueue`가 저장 전 입력도 별도 대기열에 원래 ID·내용·순서대로 보존한다. 저장이 성공하기 전에는 업무 처리기로 넘기지 않는다. 오류 이후 입력도 앞선 입력을 추월하지 않는다.
- 복구 버튼은 보존한 입력의 저장부터 다시 수행한다. 초기 기록 읽기가 실패했으면 먼저 저장된 입력을 복원하고 이어서 새 입력을 처리한다.
- `blocked()`는 기록 복원이 끝나지 않았거나 어느 대기열이든 남아 있을 때 참이다. 입고 확정, 수량 편집, 출고 강제처리, 실사 위치 전환·정정 및 화면 이동에 이 조건을 사용한다. `size()`는 실제로 알고 있는 입력 수만 반환한다.
- 작업자 안내: “스캔을 저장하지 못했어요. 이 화면을 유지한 채 저장 공간을 확보하고 다시 확인해 주세요. 상품은 다시 찍지 마세요.” 발주 입고의 저장 실패에는 “이 스캔 제외”를 표시하지 않는다.
- 요청·응답·오류 코드 등 기술 진단을 작업자 화면에 추가하지 않았다. 기존 개발자 모드 권한 경계는 유지한다. 서버/API/DB 스키마 변경은 없다.

**보존 한계:** 저장 자체가 실패한 입력은 공간 확보 전까지 메모리에만 있다. 화면 이동은 막지만 OS 강제 종료·전원 차단에도 남는다고 보장하지 않는다. 해당 상태에서는 화면을 유지해야 한다.

### 검증 결과

| 검사 | 결과 |
|---|---|
| 새 회귀 4건 | 저장 실패 후 등록 차단·2개 복구, A·A·B 순서/원래 ID/저장 선행/이탈 방지, 초기 읽기 실패 후 기존 A→신규 B 순서 복구, 부분 초안의 조기 등록 차단. 모두 통과 |
| 영향 범위 회귀 | `corepack yarn --cwd native/warehouse-app test src/core/hardware/scan src/core/operations src/domains/inbound src/domains/outbound src/domains/stocktaking`: **29 files / 172 tests**, 실패·skip 0 |
| 빌드 | `corepack yarn --cwd native/warehouse-app build`: 타입 검사와 production build 통과. 500kB 초과 chunk 경고는 남음 |
| lint | `corepack yarn --cwd native/warehouse-app lint`: 오류 0, 경고 19개 |
| 독립 검토 | 저장 실패 대기열과 복원/확정 차단 조건 검토 완료. 검토 범위에서 남은 조치 필요 결함 없음 |
| 실제 브라우저·로컬 HTTP | 첫 스캔 1개 → 두 번째 저장 실패 → 등록·편집 차단 및 행동 안내 → 공간 복구와 “다시 확인” → 재스캔 없이 2개 → 등록·적치 대기 2개 |
| PostgreSQL 대사 | 입고 라인 **1건/2개**, RECEIVE 이벤트 **1건/2개**, ON_HAND **2개/version 1**, `inbound.simple.v2` 작업 기록 **1건**. 화면을 다시 연 후에도 동일 |

신규 DB `warehouse_readiness_storage_20260915`를 **로컬 PostgreSQL(127.0.0.1:5432)**에 생성했다. 이전 검증용 DB의 스키마만 복사하고 검증용 창고/SKU를 새로 만들었다. 운영 데이터와 기존 DB 데이터는 변경하지 않았다.

브라우저는 실제 React 작업 화면·스캔 버스·IndexedDB 저장소·영속 작업 실행기를 사용했다. Tauri 전송은 browser fetch로 연결했고, 저장 실패는 테스트 전용 어댑터에서 주입했다. 서버는 실제 `InboundController`, DTO 검증, `InboundService`, 중복 방지, 입고 kernel, 원장 쓰기를 사용했다. 로그인/권한 및 상품 조회는 로컬 테스트 fixture이므로 전체 core 기동·실제 로그인 검증으로 해석하지 않는다. 검증용 상태 조회의 컬럼명 오류로 DB 대사는 별도 SQL 직접 조회로 수행했다.

로컬 증거: SKU `90b7baaa-2d4a-4aac-bcf5-d802fc67c014`, 입고 라인 `3d55fdfa-be7d-4c58-a8d2-e119a0e68a82`. SQL 결과와 임시 검증 어댑터는 `/tmp/warehouse-readiness-storage/`에 있다. 검증 서버(4177·4178)와 브라우저는 종료했고 전용 DB는 보존했다.

### 남은 미검증 항목

- Android PDA·Windows의 실제 HID 연속 스캔/100회/포장 단위/포커스/Enter.
- 네이티브 프로세스 강제 종료, 실제 디스크 부족·IndexedDB 접근 실패·전원 장애에서의 안내 및 대사. 이번 오류 주입 통과는 실제 장비 합격이 아니다.
- 실제 Wi-Fi 단절/복구, 인증 만료/재로그인 후 업무 복구.
- 두 작업자의 실제 출고 박스 담당자·리스 충돌.
- 제한된 시험 운영 및 당일 원장/실물 대사, 운영 마이그레이션·v2 전환 순서 확인.

전체 실사용 합격으로 판정하지 않는다. 이번에는 B1 한 결함의 수정과 영향 범위 검증을 완료했다. 병합·배포는 별도 승인 대상이다.


## PR #875 이후 — 적치 대기 검색 및 페이지 조회

검증일: 2026-09-15. 기준 `develop` 머지 커밋 `78258d49a`. 작업 브랜치 `fix/putaway-pending-search-pagination`.

### 결함 및 변경

적치 대기 API가 오래된 200건만 반환하고 앱이 그 목록에서만 스캔 상품을 찾았다. 따라서 대기가 있는 201번째 상품이나 화면의 기간 밖 상품도 “대기가 없어요”로 안내할 수 있었다.

- 기존 `GET /inbound/putaway/pending`에 `skuIds`(쉼표 구분 UUID 최대 100개), `cursor`를 추가했다. 응답의 기존 필드는 유지하고 `nextCursor: string | null`을 추가한다. `total`은 해당 페이지 반환 건수이며 전체 대기 건수가 아니다.
- SKU 조건을 LIMIT보다 먼저 적용한다. 페이지당 최대 200건, 오래된 입고 시각·라인 ID 순서로 조회한다. 페이지 토큰은 조회 창고/기간/SKU 집합에 연결하며 PostgreSQL 시각의 마이크로초와 최초 조회의 기간 기준을 보존한다.
- 스캔은 선택 창고의 전체 기간에서 해당 SKU의 대기를 서버 조회한다. 단건은 적치 화면, 여러 건은 후보 선택 화면을 열고 후보에도 ‘더 보기’를 제공한다. 일반 목록에도 ‘더 보기’와 실패 시 재확인을 제공한다.
- 다른 상품 스캔, 창고/기간 전환, 목록에서 직접 선택한 뒤에는 이전 검색 결과가 화면을 열지 않는다. 같은 상품 재스캔도 서버의 새 확인을 기다린다.
- 실패·갱신 중인 조회로 대기 없음을 단정하지 않는다. 구형 서버가 상품 필터를 무시한 응답은 적치 대상으로 쓰지 않는다.
- 실제 적치 변경은 기존 서버의 잔량·창고 검증과 영속 작업 실행기를 유지한다. DB 스키마 변경은 없다.

### 검증

- 앱: 전체 **74개 파일 / 366개 테스트** 통과. 마지막 Hook 의존성 정리 후 관련 화면/조회 **30개 테스트** 재검증 통과. 최종 타입 검사와 production build 통과.
- 서버: 컨트롤러·실제 PostgreSQL 적치 리더·재고 권한 검사 **3 suites / 45 tests**, skip 없이 통과. Core 타입 검사 및 변경 서버 파일 ESLint 통과.
- 실패 선행 검사: 화면의 목록 밖 상품/양쪽 페이지 조회/조회 실패 4건과 서버의 상품 필터/커서/입력 검사를 수정 전 실패로 확인했다.
- DB 검사는 `127.0.0.1:5432`의 전용 DB `putaway_pending_20260915`에서 수행했다. 기존 검증 DB의 스키마만 복사했으며 fixture 데이터는 트랜잭션 종료 시 rollback했다. 운영 데이터는 변경하지 않았다.
- 서버 검사는 200건 초과, 동일 SKU의 다수 입고, 기간 밖 SKU 검색, 앞선 건의 적치 완료 후 페이지 이동, 시각 정밀도, 페이지 간 기간 기준, 잘못된 토큰·조회 범위 및 권한을 포함한다.
- 화면 검사는 일반 목록 로딩/실패와 독립된 스캔, 오래된 검색 응답 무시, 페이지 실패 후 기존 목록 유지·재시도, 같은 SKU 재스캔 최신 확인, 구형 서버의 필터 무시를 포함한다.
- 독립 검토에서 빈 목록의 갱신 실패 후 과거 결과를 ‘없음’으로 표시하는 문제를 추가 재현·수정했다. 검토 후 남은 조치 필요 사항 없음.
- 앱 lint는 오류 0, 기존 경고 18개. 빌드의 500kB 초과 chunk 경고는 남는다.

### 배포와 한계

서버를 먼저 배포해 SKU 필터 및 `nextCursor` 응답을 제공하고 앱을 배포한다. 기존 앱의 API 응답 호환성은 유지한다. 신규 앱이 구형 서버에 연결되면 상품별 조회를 안전하게 중단하므로 정상 스캔 사용에는 서버 업데이트가 필요하다.

페이지 목록은 조회 시점의 재고를 보여준다. 과거 시각으로 새로 입력된 입고는 목록을 새로 조회해야 반영된다. 스냅샷 전체를 고정하는 기능은 추가하지 않았다.

이번 검증은 React 화면 테스트와 서버/DB 검사다. 실제 PDA·Windows 스캔, 네이티브 프로세스 장애, 현장 실물 대사 합격을 뜻하지 않는다. Push·PR 생성·병합·운영 배포는 수행하지 않았다.

## 스테이션 단독 물류 운영 — A/B/C 구현 및 로컬 검증

검증일: 2026-09-15. 기준 `develop`의 `df44cf4fb`, 실행 브랜치 `feat/station-logistics-workflows`. 제품 코드 확인 기준 `f27bd3247`. 위의 이전 작업 기록과 구분한다.

### 구현한 업무

| 묶음 | 실제 사용 흐름 |
|---|---|
| A1–A4 | 스테이션 홈에서 입고·적치·이동·실사 진입. 상품 검색과 키보드 수량 입력. 검수 없이 간편입고로 실제 도착 수량을 기본존에 등록하고 적치는 나중에 진행. 발주 물건은 발주 수령 경로와 잔량 처리를 유지 |
| A5–A6 | 바코드 없는 SKU와 장부에 없는 품목도 실사에 추가. 기존 품목 0과 미입력을 구분. 추가는 create-only이고 재고는 실사 완료에서만 조정. 초안/원래 키를 저장하고 미확인·복구 실패 동안 추가 확정 차단 |
| B1–B3 | 날짜·상품·상태별 직접/발주 입고 이력 및 취소 이력. 출처별 전량 취소 후 직접입고 적치 대기와 발주 잔량 갱신. 취소는 서버가 잠금 아래 재검증하고, 취소된 초안의 적치를 막음 |
| C1–C3 | 선택 창고와 송장 창고 일치 확인, 출고 준비 및 위치별 잔량 표시, 접수 시 출발 위치 고정. 지정 위치 잔량을 다른 위치에서 채우지 않음. 스캔 생략도 위치별 실물 수량·사유를 요구. 구형 미확인 요청은 기존 경로·본문·키로 복구 |

수량은 낱개 정수이며 일반 업무는 1 이상, 실사는 0 이상이다. 입력란의 빈 값은 0으로 바꾸지 않는다. 검색·수량 편집의 Enter와 전역 스캔은 분리했으며, 완료된 전역 HID 입력의 Enter가 포커스된 업무 버튼까지 누르지 않도록 처리했다.

### 자동 검사

- `corepack yarn --cwd native/warehouse-app test --run`: **82 files / 404 tests 통과**, 실패·skip 0.
- `corepack yarn --cwd native/warehouse-app build`: TypeScript 및 production build 통과.
- `corepack yarn --cwd native/warehouse-app lint`: 오류 0, 경고 **22개**. Hook 의존성과 Fast Refresh export 경고가 남는다. 빌드의 500kB 초과 chunk 경고도 남으며 bundle 약 556kB다.
- A/B/C 통합 Core 회귀: **16 suites / 172 tests 통과**, 실패·skip 0. 신규 실사 및 기존 count-version/preview/complete, 입고 history/DTO/취소/kernel, 신규·기존 출고/controller/route-order/waybill, 공통 권한을 하나의 명령으로 실행했다.
- `corepack yarn tsc --noEmit --incremental false -p apps/core/tsconfig.app.json`: 최종 C 서버 코드에서 통과.
- C의 변경 서버·테스트 15개 파일 ESLint 통과. B의 기존 `inbound.service.ts`에는 변경 전부터 있던 미사용 import/인수 오류 3개가 남으며 서버 전체 lint 통과로 주장하지 않는다.

Core 실행은 `source work/station-local.env` 후 `DATABASE_URL="$STATION_TEST_DATABASE_URL" corepack yarn test --runInBand --silent --runTestsByPath ...`를 사용했다. 검사 목록과 실행 결과는 로컬 `work/station-final-core-tests.log`, 앱 결과는 `work/station-final-app-{tests,build,lint}.log`에 보관한다. 이 파일들과 테스트 인증 fixture는 제품 커밋에 포함하지 않는다.

새 출고 테스트는 두 위치·두 SKU, 다른 위치/창고·초과 입력 거절, 동일 키의 actor/body 변경 거절, force 실패 시 선행 피킹 전체 롤백, 기존/신규 계약의 내부 키 충돌 방지, 독립 DB 연결의 동일 키 시작·서로 다른 스캔 키·두 작업자 리스 경합을 포함한다.

### 실제 React → HTTP → DB 대사

| 작업 | 확인한 원장·업무 결과 |
|---|---|
| 키보드로 두 SKU 10+3 입고 후 첫 품목 3 적치 | 한 입고 회차13개, 기본존7/적치위치3/두 번째SKU3, 합계13 유지 |
| 직접입고 5 → 취소 5 → 신규입고 3 | RECEIVE5 / ADJUST_DOWN5 / RECEIVE3, 최종 재고3 |
| 적치3 후 A→B 이동3 | MOVE3 두 건, A0/B3, 총재고3 유지 |
| 발주10 → 입고4 → 취소4 | 발주 잔량10으로 복원, 수령 누계0, 해당 SKU 재고0 |
| 기존 실사 예상5→실물0 + 바코드 없는 신규 SKU0→2 | 완료 전 재고 불변. 완료 시 정확히 ADJUST_DOWN5와 ADJUST_UP2, 창고 총량8→5 |
| 출고 할당 A2/B1 중 B1 처리 후 새로 열기 | B만 피킹1, A미피킹2 유지; 새로 열어도 동일 상태 복구 |
| 다른 위치 출고 요청 | HTTP409 `LOCATION_OUTBOUND_SOURCE_MISMATCH`, 원장·커스터디·할당 변화0 |
| 나머지 A2를 수량·사유로 명시 확인 | 출고완료 화면, SHIP1(B)+SHIP2(A), 총재고3→0 |

브라우저 업무 검증 후 확인된 출고 내부 키 충돌과 입고 복구/연속 스캔 수정은 서버·화면 회귀 검사로 재검증했다. 의도적인 응답 유실은 영속 실행기 테스트에서 검사했으며 이번 실제 브라우저 검사는 일반 새로 열기까지다.

브라우저는 제품 React 화면·라우터·IndexedDB 실행기와 실제 Nest 컨트롤러·DTO·재고 서비스를 사용했다. Tauri 전송 대신 browser fetch, 로그인/권한 대신 로컬 fixture를 사용했다. 실제 권한 검사는 별도 HTTP 회귀 테스트로 검사했으나 실제 계정 로그인 검증은 아니다. 독립된 신규 DB `warehouse_station_20260915`(127.0.0.1:5432)에서만 실행했다. 기존 DB 초기화 및 운영 데이터 변경은 없다.

### 독립 검토와 수정

A의 신규 실사/화면, B의 서버/화면, C의 서버/화면, 전체 연결 경계를 따로 검토했다. 복구 중 실사·입고 편집 차단, 취소된 입고의 적치 재개 차단, 출고 응답의 창고/송장 검사, 확정된 출고 응답 재생 후 최신 잔량 갱신, 새 출고 내부 키를 구형 문자열 키와 구분하는 보완을 반영했다. 입고 복구 중에는 입력을 막되 이후 연속 스캔에는 불필요한 재확인을 끼우지 않는다.

검토 범위에서 남은 조치 필요 P1/P2 사항 없음.

### 배포 전 남은 확인

- [ ] Core 서버를 먼저 배포하고 새 capability/이력/위치 출고 계약을 확인한 다음 Windows 앱을 배포한다. 이번 기능 추가 자체의 새 DB 마이그레이션은 없다. 기존 v2/실사 baseline 전제는 기존 배포 절차에 따른다.
- [ ] 실제 Windows 설치·계정 로그인·권한별 메뉴/작업을 확인한다.
- [ ] 실제 HID 입력과 키보드 포커스 전환, 통신 단절·재로그인·네이티브 강제 종료 후 미확인 작업 복구를 확인한다.
- [ ] 두 실제 작업자의 동일 박스 충돌과 당일 실물/원장 대사를 제한된 시험 운영에서 확인한다.

로컬 구현·검증은 완료 범위이며, 현장 전체 사용 합격과 운영 배포 완료를 뜻하지 않는다. Push·PR 생성·병합·배포는 수행하지 않고 실행 브랜치와 worktree를 보존한다.

임시 검증용 Nest/Vite 서버는 종료했다. 전용 로컬 DB와 work/의 실행 로그·fixture는 재검증을 위해 보존한다.

## 물류팀 시연 결함 D1–D3 수정 및 검증

검증일: 2026-09-15. 기준 `cfe7d5e7a`, 브랜치 `codex/warehouse-demo-defects`. 제품 변경 기준 `0c6fd4454`. 위의 과거 검사 기록과 구분한다.

### 수정한 동작

- **D1 연속 스캔:** 동일 송장·창고·출발 위치의 정상 전송 중에는 다음 입력을 영속 큐에 접수한다. 위치 변경·강제출고는 대기가 끝날 때까지 차단한다. 초기 복원, 다른 작업, 미확인, 계정 범위 오류, 저장 실패에는 스캔 예외를 허용하지 않는다. 처리 완료 후 과잉 입력은 미반영 사실을 안내한다. 전역 스캔 구독이 이전 렌더의 잠금·위치를 참조하지 않게 했다.
- **D2 권한과 복구:** `work-context.permissions.forceDispatch`를 서버의 실제 ScopeGuard 규칙으로 계산한다. 일반 작업자는 강제출고를 실행할 수 없고, 권한 조회 실패·구형 서버에서도 일반 출고는 유지한다. 강제출고 직전에 권한을 다시 확인한다.
- **D2 미확인 결과:** 위치별 강제출고는 원래 키·본문·사용자 범위로 `location-outbound-force-resolutions`를 호출한다. 서버가 저장한 성공 결과 또는 원자적으로 확정한 미반영 결과만 종결한다. 미반영 기록은 늦게 도착한 원래 force 요청도 막는다. 일반 401/403을 일괄 실패로 바꾸거나 이전 작업을 새 키로 만들지 않는다. 결과 저장은 사용자 범위와 현재 처리권을 확인하며 저장 실패 시 계속 차단한다.
- **D3 완료 송장:** 창고 일치 확인 뒤 출고완료 여부를 먼저 판정한다. 활성 배치 작업이 없는 완료 송장도 ‘이미 출고된 송장이에요’로 안내하며 조회만 수행한다.

### 자동 검사

| 검사 | 결과 |
|---|---|
| warehouse-app 전체 | 83 files / **450 tests**, 실패·skip 0 |
| 앱 production build | TypeScript 및 Vite 통과 |
| 앱 lint | 오류 0, 경고 23개. Hook 의존성·Fast Refresh 경고이며 새 `WorkBoundary` hook export 경고 1개 포함 |
| Core 입고·이동·출고·권한 회귀 | 9 suites / **115 tests**, 실패·skip 0 |
| 서버 테스트 타입 표현 정리 후 영향 검사 | 2 suites / 54 tests, 실패·skip 0 |
| 권한·명령·양방향 경합 검사 묶음 | 8 suites / 99 tests, 실패·skip 0 |
| Core 타입 검사 | `tsc --noEmit -p apps/core/tsconfig.app.json` 통과 |
| 변경 서버·테스트 9개 파일 ESLint | 오류·경고 0 |

서버 묶음은 겹치므로 합산한 고유 총수로 해석하지 않는다. 빌드의 500 kB 초과 chunk 경고는 남는다. 신규 DB migration은 없다.

실행 명령은 저장소 루트의 `corepack yarn --cwd native/warehouse-app test --run`, `build`, `lint`와 `DATABASE_URL=<전용 DB> corepack yarn test --runInBand --runTestsByPath ...`다. 115개 회귀의 파일은 다음과 같다.

- `inbound.service.idempotency.spec.ts`, `inbound-receipt.kernel.integration.spec.ts`, `movement.service.idempotency.spec.ts`
- `shipment-waybill.reader.integration.spec.ts`, `location-outbound.service.integration.spec.ts`, `location-outbound.controller.spec.ts`
- `fulfillment-command.service.spec.ts`, `warehouse-operation-auth.spec.ts`, `outbound-v2-authorization.spec.ts`

실제 runtime 화면 검사에는 IndexedDB 실행기·WorkBoundary·ScanProvider를 포함했다. 동일 입력 100회, 포커스된 입력과 전역 키보드 이벤트, 응답 유실·구형 작업 복원, 다른 계정, 손상 응답, 저장 실패, 처리권 경합을 검사했다. 서버 경합 검사는 서로 다른 PostgreSQL 연결에서 양방향 대기를 확인해 force 선행 시 출고 한 번, resolver 선행 시 재고 변경 0을 검증했다.

### 실제 React → 로컬 HTTP → DB 대사

| 시나리오 | 확인한 결과 |
|---|---|
| 입고10 → A 적치10 → B 이동4 → B 출고3 | RECEIVE10/MOVE10/MOVE4/SHIP3. 최종 A6/B1/기본존0, 총재고7 |
| 동일 상품 100회, 서버 처리 1초 지연 | 입력창에서 100회 바코드+Enter 접수, HTTP201 정확히100회, SHIP100 한 번, 최종재고0 |
| 첫 스캔 커밋 뒤 지속적인 응답 유실·창 재개 | 미확인1개와 후속2개 보존. ‘처리 내역 확인’ 후 ‘이어서 작업’으로 복구. 첫 키의 재시도5회에도 고유 스캔3개, SHIP3 한 번 |
| 일반 작업자 | 관리자 권한 안내만 표시하고 강제출고 버튼 없음. 일반 스캔 출고 성공 |
| 권한 확인 직후 force403 | 같은 키·본문의 resolver201로 미반영 확정. 당시 재고3/이벤트0. 확인창을 닫고 일반 스캔3개로 완료 |
| force 커밋 후 응답 유실·권한 철회 | 일반 작업 권한으로 원래 키·본문의 resolver201 성공 복구. force 재전송 없이 SHIP3 한 번, 완료 화면 |
| 완료 송장 재조회 | ‘이미 출고된 송장이에요’, 조회 이후 추가 POST0 |

전용 DB는 `127.0.0.1:5432/warehouse_demo_defects_20260915`다. 기존 DB의 스키마만 복사하고 합성 데이터를 생성했다. 기존 DB를 초기화하거나 운영 데이터를 변경하지 않았다.

로컬 검증은 제품 React 라우트·영속 실행기와 실제 Nest 컨트롤러·DTO·ScopeGuard·재고 서비스를 연결했다. Tauri 전송을 browser fetch로 대체하고 합성 작업자/관리자 인증을 주입했다. 응답 유실은 실제 서버 커밋 뒤 검증 전송 어댑터가 응답을 버리는 방식이다. 제품 코드에 인증 우회나 장애 주입을 추가하지 않았다. 검증 서버는 배치 목록 컨트롤러를 포함하지 않아 목록 조회가 404였으며, 이번 실제 화면 검사는 송장 조회 진입을 사용했다.

검증 fixture·HTTP 기록·대사 JSON은 worktree의 `.superpowers/demo-http/`, 실행 로그는 `/tmp/demo-*` 및 각 작업 검사 로그에 남겼다. 합격 여부는 위 결과와 정식 회귀 테스트로 판단한다.

### 릴리스와 남은 현장 검사

1. Core 서버를 먼저 배포하고 `permissions.forceDispatch`와 결과 확인 endpoint를 확인한다. 기존 정상 force 응답과 기존 명령 키는 호환 유지한다.
2. Windows 앱을 배포한다. 구형 서버 연결에서는 강제출고를 비활성화하며 기존 미확인 기록을 삭제하지 않는다. 오래된 simple-force 작업은 기존 복구/관리자 확인 경로를 유지한다.
3. 실제 Windows 설치·일반 작업자/관리자 로그인, HID100회·입력 포커스, Wi-Fi 단절·재로그인, 네이티브 강제 종료 후 복구를 현장에서 검사한다. 두 작업자의 실제 박스 충돌과 실물/원장 대사도 별도다.

**로컬 시연 흐름은 통과했다. 실제 Windows 장비·계정·네트워크 검사는 이 환경에서 수행하지 못했으며 현장 인수 완료를 뜻하지 않는다.** 원격 push·PR·병합·운영 배포는 수행하지 않았다.

최종 독립 검토: D1·D2 서버·D2 앱·D3 각각의 검토와 전체 브랜치 검토를 완료했다. 차단 결함 없음. 별도로 지연시킨 두 GET 응답의 역전만 검사하는 테스트는 선택적 보강 사항으로 남으며 기존 요청 순서 보호는 확인했다. 로컬 테스트용 HTTP/Vite 서버는 종료했고 브랜치·전용 DB·검증 fixture는 보존한다.
