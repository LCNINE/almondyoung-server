# 멤버십 서비스 레이어 리팩토링 계획

## 📋 개요

현재 멤버십 서비스들이 레이어 아키텍처 규칙을 위반하고 있습니다.

- **문제**: Service가 구현 계층 역할을 하고 있음 (검증, DB 접근 등)
- **목표**: Service를 추상화 계층으로 변경 (2-3줄 오케스트레이션만)

## 🎯 리팩토링 원칙

### Service (Business Layer)

- **역할**: 2-3줄로 비즈니스 흐름만 표현
- **금지**: 검증 로직, DB 접근, 상세 구현
- **허용**: Reader/Manager 호출, 흐름 중계

### Reader (Implementation Layer)

- **역할**: 데이터 조회
- **패턴**: `{domain}-{concept}.reader.ts`
- **예시**: `entitlement.reader.ts`, `subscription-contract.reader.ts`

### Manager (Implementation Layer)

- **역할**: 검증 + 비즈니스 로직 + DB 접근
- **패턴**: `{domain}-{concept}.manager.ts`
- **예시**: `entitlement.manager.ts`, `subscription.manager.ts`

### Creator (Implementation Layer)

- **역할**: 신규 엔티티 생성
- **패턴**: `{domain}-{concept}.creator.ts`
- **예시**: `subscription.creator.ts`

## 📊 현재 서비스 분석

### ✅ 완료된 리팩토링

1. **Phase 1**: `EntitlementService` → Reader + Manager 분리 완료
2. **Phase 2**: `SubscriptionService` → Reader + Manager + Creator 분리 완료
3. **Phase 3**: `PauseService` → Reader + Manager 분리 완료
4. **Phase 4**: `PlanService` → Reader + Manager 분리 완료
5. **Phase 5**: `BenefitTrackingService` → Reader + Manager 분리 완료
6. **Phase 6**: `AdminOperationsService` → 오케스트레이션 패턴 완료
7. **통합**: `CancellationReasonService` → `CancellationReasonReader`로 통합 완료
8. **재배치**: `ContractEventService` → `ContractEventManager`로 이동 (subscription 폴더)

### 🔄 리팩토링 필요 서비스

1. **RecurringBillingService** (진행 중)

---

## 1️⃣ EntitlementService 리팩토링

### 현재 문제점

```typescript
// ❌ Service가 직접 DB 접근 및 검증 수행
async createEntitlement(tx, userId, tierId, startsAt, endsAt, sourceBatchId) {
  await this.terminateActiveEntitlement(tx, userId, sourceBatchId);
  const [newEntitlement] = await tx.insert(...).returning();
  return newEntitlement;
}
```

### 리팩토링 계획

#### 📁 파일 구조

```
apps/membership/src/services/entitlement/
├── entitlement.reader.ts          # 권한 조회
├── entitlement.manager.ts         # 권한 생성/수정/종료
└── entitlement.creator.ts         # 권한 생성 전용
```

#### 🔧 entitlement.reader.ts

```typescript
@Injectable()
export class EntitlementReader {
  /**
   * 활성 권한 조회
   */
  async findActiveEntitlement(userId: string): Promise<Entitlement | null>;

  /**
   * 사용자 권한 상세 조회 (계약, 플랜, 티어 포함)
   */
  async getUserEntitlementDetails(userId: string);

  /**
   * 권한 ID로 조회
   */
  async findById(entitlementId: string): Promise<Entitlement | null>;
}
```

#### 🔧 entitlement.manager.ts

```typescript
@Injectable()
export class EntitlementManager {
  /**
   * 권한 생성 (기존 권한 종료 포함)
   */
  async createEntitlement(
    tx: DrizzleTransaction,
    userId: string,
    tierId: string,
    startsAt: Date,
    endsAt: Date,
    sourceBatchId: string,
  ): Promise<Entitlement>;

  /**
   * 권한 연장/차감
   */
  async adjustEntitlement(
    userId: string,
    days: number,
    reason: string,
    adminId: string,
  ): Promise<Entitlement>;

  /**
   * 권한 만료 처리 (Lazy Expiration)
   */
  async expireEntitlement(entitlementId: string, userId: string): Promise<void>;

  /**
   * 활성 권한 종료
   */
  async terminateActiveEntitlement(
    tx: DrizzleTransaction,
    userId: string,
    closedBatchId: string,
  ): Promise<void>;
}
```

#### 🔧 entitlement.service.ts (리팩토링 후)

```typescript
@Injectable()
export class EntitlementService {
  constructor(
    private readonly reader: EntitlementReader,
    private readonly manager: EntitlementManager,
  ) {}

  /**
   * ✅ 흐름만 표현: "권한 체크 → 만료 시 처리"
   */
  async checkAndUpdateSubscription(userId: string): Promise<boolean> {
    const entitlement = await this.reader.findActiveEntitlement(userId);
    if (!entitlement) return false;

    const isExpired = this.isExpired(entitlement.endsAt);
    if (isExpired) {
      await this.manager.expireEntitlement(entitlement.id, userId);
      return false;
    }

    return true;
  }

  /**
   * ✅ 흐름만 표현: "권한 조회"
   */
  async getUserEntitlement(userId: string) {
    return this.reader.getUserEntitlementDetails(userId);
  }

  /**
   * ✅ 흐름만 표현: "권한 생성"
   */
  async createEntitlement(tx, userId, tierId, startsAt, endsAt, sourceBatchId) {
    return this.manager.createEntitlement(
      tx,
      userId,
      tierId,
      startsAt,
      endsAt,
      sourceBatchId,
    );
  }

  /**
   * ✅ 흐름만 표현: "권한 조정"
   */
  async adjustEntitlement(userId, days, reason, adminId) {
    return this.manager.adjustEntitlement(userId, days, reason, adminId);
  }
}
```

---

## 2️⃣ SubscriptionService 리팩토링

### 현재 문제점

```typescript
// ❌ Service가 직접 트랜잭션 관리 및 DB 접근
async createSubscription(userId, planId) {
  return await this.dbService.db.transaction(async (tx) => {
    const existingEntitlement = await this.entitlementService.getUserEntitlement(userId);
    if (existingEntitlement) throw new ActiveSubscriptionExistsException();

    const plan = await this.planService.getPlanDetails(planId);
    // ... 복잡한 로직 ...
  });
}
```

### 리팩토링 계획

#### 📁 파일 구조

```
apps/membership/src/services/subscription/
├── subscription.reader.ts          # 구독 조회 (기존 contract.reader 확장)
├── subscription.manager.ts         # 구독 생성/업그레이드
├── subscription.creator.ts         # 구독 생성 전용
└── subscription-contract.reader.ts # 계약 조회 (기존 유지)
```

#### 🔧 subscription.creator.ts

```typescript
@Injectable()
export class SubscriptionCreator {
  /**
   * 새 구독 생성 (계약 + 권한)
   */
  async createNewSubscription(
    userId: string,
    plan: Plan,
    tier: Tier,
  ): Promise<{ contractId: string; entitlementId: string }>;
}
```

#### 🔧 subscription.manager.ts

```typescript
@Injectable()
export class SubscriptionManager {
  /**
   * 구독 업그레이드 처리
   */
  async upgradeSubscription(
    userId: string,
    currentContract: Contract,
    newPlan: Plan,
    newTier: Tier,
  ): Promise<UpgradeResult>;

  /**
   * 구독 취소 처리 (계약 무효화)
   */
  async voidSubscription(
    userId: string,
    contract: Contract,
    reason?: string,
  ): Promise<void>;
}
```

#### 🔧 subscription.service.ts (리팩토링 후)

```typescript
@Injectable()
export class SubscriptionService {
  constructor(
    private readonly entitlementService: EntitlementService,
    private readonly planService: PlanService,
    private readonly contractReader: SubscriptionContractReader,
    private readonly subscriptionCreator: SubscriptionCreator,
    private readonly subscriptionManager: SubscriptionManager,
  ) {}

  /**
   * ✅ 흐름만 표현: "권한 체크 → 플랜 조회 → 구독 생성"
   */
  async createSubscription(userId: string, planId: string) {
    await this.entitlementService.checkAndUpdateSubscription(userId);
    const existing = await this.entitlementService.getUserEntitlement(userId);
    if (existing) throw new ActiveSubscriptionExistsException();

    const plan = await this.planService.getPlanDetails(planId);
    if (!plan) throw new PlanNotFoundException();

    return this.subscriptionCreator.createNewSubscription(
      userId,
      plan.plan,
      plan.tier,
    );
  }

  /**
   * ✅ 흐름만 표현: "권한 체크 → 계약 조회 → 플랜 검증 → 업그레이드"
   */
  async upgradeSubscription(userId: string, newPlanId: string) {
    const current = await this.entitlementService.getUserEntitlement(userId);
    if (!current) throw new SubscriptionNotFoundException();

    const newPlan = await this.planService.getPlanDetails(newPlanId);
    if (!newPlan) throw new PlanNotFoundException();

    return this.subscriptionManager.upgradeSubscription(
      userId,
      current.contract,
      newPlan.plan,
      newPlan.tier,
    );
  }

  /**
   * ✅ 흐름만 표현: "권한 조회"
   */
  async getCurrentSubscriptionDetails(userId: string) {
    await this.entitlementService.checkAndUpdateSubscription(userId);
    return this.entitlementService.getUserEntitlement(userId);
  }
}
```

---

## 3️⃣ PauseService 리팩토링 ✅

### 일시정지 시 정기결제 처리 규칙

#### 비즈니스 규칙

**패턴: 일시정지 기간 = 무료 연장 (Adobe, Notion 방식)**

- 일시정지 기간만큼 `endsAt` 연장 ✅ (구현 완료)
- 일시정지 기간만큼 `nextBillingDate` 연장 ❌ (추가 필요)
- 일시정지 중에는 결제 스킵

**예시 1: 월간 구독자 (30일 플랜)**

```
- 1월 1일: 구독 시작
  endsAt = 1월 31일
  nextBillingDate = 2월 1일

- 1월 15일: 30일 일시정지
  endsAt = 3월 2일 (31일 + 30일) ✅
  nextBillingDate = 3월 3일 (2월 1일 + 30일) ❌ 추가 필요

- 2월 1일: 결제 스킵 (pausedAt IS NOT NULL)
- 3월 3일: 결제 재개
```

**예시 2: 연간 구독자 (365일 플랜)**

```
- 1월 1일: 구독 시작
  endsAt = 내년 1월 1일
  nextBillingDate = 내년 1월 1일

- 4월 15일: 30일 일시정지
  endsAt = 내년 1월 31일 (1월 1일 + 30일) ✅
  nextBillingDate = 내년 1월 31일 (1월 1일 + 30일) ❌ 추가 필요

- 내년 1월 1일: 결제 스킵
- 내년 1월 31일: 결제 재개
```

### 완료된 구조

#### 📁 파일 구조

```
apps/membership/src/services/pause/
├── pause.reader.ts          # 일시정지 조회 ✅
└── pause.manager.ts         # 일시정지 생성/재개 ✅
```

#### 🔧 pause.service.ts (리팩토링 완료)

```typescript
@Injectable()
export class PauseService {
  constructor(
    private readonly pauseReader: PauseReader,
    private readonly pauseManager: PauseManager,
  ) {}

  /**
   * ✅ 흐름만 표현: "권한 조회 → 일시정지 실행"
   */
  async pauseSubscription(userId, startDate, endDate, reason) {
    const entitlement =
      await this.pauseReader.findActiveNonPausedEntitlement(userId);
    if (!entitlement) throw new Error('Active subscription not found');

    return this.pauseManager.startPause(
      userId,
      entitlement,
      startDate,
      endDate,
      reason,
    );
  }

  /**
   * ✅ 흐름만 표현: "권한 조회 → 일시정지 재개"
   */
  async resumeSubscription(userId) {
    const entitlement = await this.pauseReader.findPausedEntitlement(userId);
    if (!entitlement || !entitlement.pausedAt) {
      throw new Error('No paused subscription found');
    }

    return this.pauseManager.resumePause(userId, entitlement);
  }

  /**
   * ✅ 흐름만 표현: "이력 조회"
   */
  async getPauseHistory(userId) {
    return this.pauseReader.findPauseHistory(userId);
  }
}
```

#### 주요 변경사항

1. **PauseReader 생성**
   - `findPauseHistory()`: 일시정지 이력 조회
   - `findPausedEntitlement()`: 현재 일시정지된 권한 조회
   - `findActiveNonPausedEntitlement()`: 활성 상태이며 일시정지되지 않은 권한 조회

2. **PauseManager 생성**
   - `startPause()`: 일시정지 시작 (권한 연장 포함)
   - `resumePause()`: 일시정지 재개
   - 이벤트 배치 및 이력 기록 처리

3. **PauseService 간소화**
   - 모든 DB 접근 로직 제거
   - Reader/Manager 호출로 단순화
   - 2-3줄 오케스트레이션만 유지

---

## 4️⃣ PlanService 리팩토링 ✅

### 완료된 구조

#### 📁 파일 구조

```
apps/membership/src/services/plan/
├── plan.reader.ts          # 플랜/티어 조회 ✅
└── plan.manager.ts         # 플랜/티어 생성/수정 ✅
```

#### 🔧 plan.service.ts (리팩토링 완료)

```typescript
@Injectable()
export class PlanService {
  constructor(
    private readonly planReader: PlanReader,
    private readonly planManager: PlanManager,
  ) {}

  /**
   * ✅ 흐름만 표현: "플랜 목록 조회"
   */
  async getAllPlans() {
    return this.planReader.findAllActivePlans();
  }

  /**
   * ✅ 흐름만 표현: "중복 검증 → 티어 생성"
   */
  async createTier(createTierInput, adminId) {
    if (await this.planReader.existsByTierCode(createTierInput.code)) {
      throw new Error(`Tier code already exists`);
    }
    if (
      await this.planReader.existsByPriorityLevel(createTierInput.priorityLevel)
    ) {
      throw new Error(`Priority level already exists`);
    }
    return this.planManager.createTier(createTierInput, adminId);
  }

  /**
   * ✅ 흐름만 표현: "티어 검증 → 플랜 생성"
   */
  async createPlan(createPlanInput, adminId) {
    const tier = await this.planReader.findTierById(createPlanInput.tierId);
    if (!tier) throw new Error('Tier not found');
    return this.planManager.createPlan(createPlanInput, adminId);
  }
}
```

#### 주요 변경사항

1. **PlanReader 생성**
   - `findAllActivePlans()`: 모든 활성 플랜 조회
   - `findPlanById()`: 플랜 상세 조회
   - `findAllTiers()`: 모든 티어 조회
   - `findPlansByTierId()`: 티어별 플랜 조회
   - `existsByTierCode()`, `existsByPriorityLevel()`: 중복 검증

2. **PlanManager 생성**
   - `createTier()`, `updateTier()`: 티어 생성/수정
   - `createPlan()`, `updatePlan()`, `deactivatePlan()`: 플랜 생성/수정/비활성화
   - 이벤트 배치 기록 처리

3. **PlanService 간소화**
   - 모든 DB 접근 로직 제거
   - 검증 로직은 Service에서 Reader 호출로 처리
   - Manager는 순수 실행만 담당
   - 2-3줄 오케스트레이션으로 단순화

---

## 5️⃣ BenefitTrackingService 리팩토링 ✅

### 완료된 구조

#### 📁 파일 구조

```
apps/membership/src/services/benefit/
├── benefit.reader.ts          # 혜택 조회 ✅
└── benefit.manager.ts         # 혜택 기록/취소 ✅
```

#### 🔧 benefit-tracking.service.ts (리팩토링 완료)

```typescript
@Injectable()
export class BenefitTrackingService {
  constructor(
    private readonly subscriptionService: SubscriptionService,
    private readonly benefitReader: BenefitReader,
    private readonly benefitManager: BenefitManager,
  ) {}

  /**
   * ✅ 흐름만 표현: "구독 조회 → 혜택 기록"
   */
  async recordDiscount(dto: RecordDiscountDto) {
    const subscription = await this.subscriptionService.getActiveSubscription(
      dto.userId,
    );
    if (!subscription) {
      this.logger.warn('No active subscription');
      return;
    }

    await this.benefitManager.recordDiscount({
      orderId: dto.orderId,
      userId: dto.userId,
      membershipDiscountAmount: dto.membershipDiscountAmount,
      tierId: dto.tierId,
      orderDate: dto.orderDate,
      subscriptionId: subscription.id,
      billingDate: subscription.billingDate,
    });
  }

  /**
   * ✅ 흐름만 표현: "이벤트 조회 → 혜택 취소"
   */
  async cancelDiscount(orderId: string) {
    const event = await this.benefitReader.findDiscountEventByOrderId(orderId);
    if (!event) throw new Error('DISCOUNT_EVENT_NOT_FOUND');
    await this.benefitManager.cancelDiscount(orderId, event);
  }

  /**
   * ✅ 흐름만 표현: "구독 조회 → 혜택 조회"
   */
  async getCurrentCycleBenefit(userId: string) {
    const subscription =
      await this.subscriptionService.getActiveSubscription(userId);
    if (!subscription) throw new Error('NO_ACTIVE_SUBSCRIPTION');
    return this.benefitReader.findCurrentCycleBenefit(
      userId,
      subscription.billingDate,
      subscription.type,
    );
  }
}
```

#### 주요 변경사항

1. **BenefitReader 생성**
   - `findCurrentCycleBenefit()`: 현재 주기 혜택 조회
   - `findCycleBenefitHistory()`: 주기별 혜택 이력 조회
   - `findDiscountEventByOrderId()`: 할인 이벤트 조회

2. **BenefitManager 생성**
   - `recordDiscount()`: 할인 혜택 기록 (멱등성 보장)
   - `cancelDiscount()`: 할인 혜택 취소
   - 주기별 혜택 집계 업데이트

3. **BenefitTrackingService 간소화**
   - 모든 DB 접근 로직 제거
   - 주기 계산 로직을 Manager로 이동
   - 2-3줄 오케스트레이션으로 단순화

---

## 📦 ContractEventService 재배치 ✅

### 문제점

`ContractEventService`는 Service가 아닌 **Implementation Layer의 Manager** 역할을 하고 있었습니다.

- 이벤트 소싱 패턴을 위한 유틸리티 클래스
- 다른 Manager들이 사용하는 도구
- Business Layer가 아닌 Implementation Layer

### 해결

```
Before: apps/membership/src/services/contract-event.service.ts
After:  apps/membership/src/services/subscription/contract-event.manager.ts
```

**변경사항:**

- `ContractEventService` → `ContractEventManager`로 이름 변경
- `subscription/` 폴더로 이동
- 기존 파일은 하위 호환성을 위해 re-export로 유지
- 모든 사용처 업데이트 완료

---

## 6️⃣ AdminOperationsService 리팩토링 ✅

### 현재 상태 분석

AdminOperationsService는 **이미 올바른 패턴**을 따르고 있었습니다:

- 다른 서비스들을 오케스트레이션하는 역할만 수행
- 자체 비즈니스 로직 최소화
- 각 도메인 서비스에 역할 위임

### 문제점 및 해결

단 하나의 문제: `getUserPauseHistory()`에서 직접 DB 접근

**Before:**

```typescript
// ❌ 직접 DB 접근
async getUserPauseHistory(userId: string) {
  const result = await this.planService['dbService'].db
    .select({...})
    .from(schema.pauseEvents)
    // ...
}
```

**After:**

```typescript
// ✅ PauseService 사용
async getUserPauseHistory(userId: string) {
  return this.pauseService.getPauseHistory(userId);
}
```

### 완료된 작업

1. ✅ PauseService 주입 추가
2. ✅ `getUserPauseHistory()` 메서드를 PauseService 호출로 변경
3. ✅ 불필요한 DB 접근 제거
4. ✅ 주석 및 문서화 개선

### 결론

AdminOperationsService는 **올바른 오케스트레이션 패턴**의 좋은 예시입니다:

- 각 도메인 서비스를 호출만 함
- 자체 비즈니스 로직 없음
- 관리자 작업을 위한 단순 중계 역할

---

## 7️⃣ RecurringBillingService 리팩토링 ✅

### 현재 상태

RecurringBillingService는 **이미 Reader/Manager로 분리 완료**되었습니다:

- ✅ BillingReader: 결제 대상 조회
- ✅ BillingManager: 결제 처리 로직
- ✅ RecurringBillingService: 스케줄러 + 오케스트레이션

### 추가 필요 사항

#### 검증 로직 보완 (Medusa 정기결제 플로우 기준)

**현재 검증:**

- ✅ 결제 주기 도달 확인 (nextBillingDate)
- ✅ 계약 무효화 여부 (isVoided)
- ✅ 연체 상태 재시도 (isPastDue)

**추가 필요:**

- ❌ 일시정지 상태 제외 (isPaused)
- ❌ 플랜 활성화 상태 검증

**결제 프로필 유효성 검증 책임 분리:**

- **Membership 책임**: 결제 프로필 ID 존재 여부만 확인 (null 체크)
- **Wallet 책임**: 실제 결제 수단 유효성, 만료 여부, 사용 가능 여부
- **현재 구현**: ✅ 올바르게 분리됨 (없으면 기본 프로필 조회, 실제 유효성은 결제 시도 시 확인)

#### 📁 파일 구조 (완료)

```
apps/membership/src/services/billing/
├── recurring-billing.service.ts      # ✅ 스케줄러 + 오케스트레이션
├── billing.reader.ts                 # ✅ 결제 대상 조회
├── billing.manager.ts                # ✅ 결제 처리 로직
└── payment-client.service.ts         # ✅ 외부 API 클라이언트
```

#### 역할 분리 (완료)

**RecurringBillingService (Business Layer)** ✅

- Cron 스케줄러 실행
- 전체 흐름 오케스트레이션만
- Reader/Manager 호출

**BillingReader (Implementation Layer)** ✅

- 결제 대상 계약 조회
- Dunning 큐 조회
- 계약 정보 조회

**BillingManager (Implementation Layer)** ✅

- 개별 결제 처리
- 결제 성공/실패 처리
- Dunning 큐 관리
- 트랜잭션 처리

### 보완 작업

#### 🔧 billing.reader.ts 수정

```typescript
/**
 * 오늘 결제 예정인 계약들 조회
 * - 무효화되지 않음 (isVoided = false)
 * - 일시정지되지 않음 (isPaused = false) ← 추가
 * - 결제일 도달 (nextBillingDate <= today)
 * - 정상 결제 또는 연체 재시도 대상
 */
async findDueContracts(date: string): Promise<DueContract[]> {
  return this.dbService.db
    .select({...})
    .from(schema.subscriptionContracts)
    .where(
      and(
        eq(schema.subscriptionContracts.isVoided, false),
        eq(schema.subscriptionContracts.isPaused, false), // ✅ 추가
        lte(schema.subscriptionContracts.nextBillingDate, date),
        or(
          eq(schema.subscriptionContracts.isPastDue, false),
          eq(schema.subscriptionContracts.isPastDue, true),
        ),
      ),
    );
}
```

#### 🔧 billing.manager.ts 수정

```typescript
/**
 * 개별 계약 결제 처리
 */
async processSingleBilling(contract: any): Promise<BillingResult> {
  return this.dbService.db.transaction(async (tx) => {
    // 1. 플랜 정보 조회 및 검증
    const plan = await this.planService.getPlanDetails(contract.planId);
    if (!plan) {
      throw new Error(`Plan not found: ${contract.planId}`);
    }
    if (!plan.plan.isActive) { // ✅ 추가
      throw new Error(`Plan is not active: ${contract.planId}`);
    }

    // 2. 결제 프로필 확인 (존재 여부만)
    let paymentProfileId = contract.paymentProfileId;
    if (!paymentProfileId) {
      // 기본 프로필 조회 시도
      const defaultProfile = await this.paymentClient.getDefaultPaymentProfile(contract.userId);
      paymentProfileId = defaultProfile.id;

      // 계약에 저장
      await tx.update(schema.subscriptionContracts)
        .set({ paymentProfileId: defaultProfile.id })
        .where(eq(schema.subscriptionContracts.id, contract.id));
    }

    // 3-5. 결제 처리 (기존 로직 유지)
    // PaymentIntent 생성 → 결제 실행 → 결과 처리
  });
}
```

---

## 8️⃣ 서비스 통합 계획

### 통합 대상

현재 여러 개로 나뉜 서비스들을 도메인별로 통합:

#### Before (분산)

```
- SubscriptionService
- SubscriptionCancellationService
- EntitlementService
- PauseService
```

#### After (통합)

```
apps/membership/src/services/
├── subscription.service.ts              # 통합 구독 서비스
│
├── subscription/
│   ├── subscription.reader.ts
│   ├── subscription.creator.ts
│   ├── subscription.manager.ts
│   ├── subscription-contract.reader.ts
│   └── subscription-cancellation.manager.ts
│
├── entitlement/
│   ├── entitlement.reader.ts
│   └── entitlement.manager.ts
│
└── pause/
    ├── pause.reader.ts
    └── pause.manager.ts
```

### 통합 SubscriptionService

```typescript
@Injectable()
export class SubscriptionService {
  // 구독 생성
  async createSubscription(userId, planId);

  // 구독 조회
  async getCurrentSubscriptionDetails(userId);
  async getSubscriptionHistory(userId);

  // 구독 변경
  async upgradeSubscription(userId, newPlanId);
  async downgradeSubscription(userId, newPlanId);

  // 구독 취소
  async cancelSubscription(userId, reasonCode, reasonText);

  // 일시정지
  async pauseSubscription(userId, pauseDays, reason);
  async cancelPause(userId, reason);
}
```

---

## 📋 리팩토링 완료 현황

### ✅ 완료된 리팩토링

1. ✅ **EntitlementService** → Reader + Manager 분리
2. ✅ **SubscriptionService** → Reader + Manager + Creator 분리
3. ✅ **PauseService** → Reader + Manager 분리
4. ✅ **PlanService** → Reader + Manager 분리
5. ✅ **BenefitTrackingService** → Reader + Manager 분리
6. ✅ **AdminOperationsService** → 오케스트레이션 패턴 적용
7. ✅ **CancellationReasonService** → CancellationReasonReader로 통합
8. ✅ **ContractEventService** → ContractEventManager로 재배치
9. ✅ **RecurringBillingService** → Reader + Manager 분리 (검증 보완 필요)

### 🔄 남은 작업

#### Task 1: RecurringBillingService 검증 로직 보완 ✅

**목표**: 정기결제 대상 필터링 강화

**작업 내역:**

1. ✅ `billing.reader.ts` 수정
   - `findDueContracts()`: entitlement.pausedAt IS NULL 조건 추가
2. ✅ `billing.manager.ts` 수정
   - `processSingleBilling()`: 플랜 활성화 상태 검증 추가
3. ✅ `recurring-billing.service.ts` 정리
   - 중복 메서드 제거 (Reader/Manager 사용)
   - 에러 처리 개선 (PLAN_NOT_ACTIVE 추가)

**검증 방법**:

- 일시정지된 구독은 결제 대상에서 제외되는지 확인
- 비활성화된 플랜은 결제 실패 처리되는지 확인

---

#### Task 2: PauseManager에 정기결제 연동 추가

**목표**: 일시정지 시 nextBillingDate도 함께 연장

**작업 내역:**

1. `pause.manager.ts` 수정
   - `startPause()`: Contract의 nextBillingDate 연장
   - userId로 Contract 조회 후 nextBillingDate 업데이트

**예상 시간**: 30분

**검증 방법**:

- 일시정지 시 nextBillingDate가 pauseDurationDays만큼 연장되는지 확인
- 일시정지 중에는 정기결제가 발생하지 않는지 확인

---

#### Task 3: 전체 서비스 통합 테스트

**목표**: 리팩토링된 모든 서비스 동작 검증

**작업 내역:**

1. 기존 통합 테스트 실행
2. 실패하는 테스트 수정
3. 누락된 테스트 케이스 추가

**예상 시간**: 1-2시간

**검증 항목**:

- [ ] 구독 생성/취소 플로우
- [ ] 정기결제 플로우
- [ ] 일시정지/재개 플로우
- [ ] 플랜 업그레이드/다운그레이드
- [ ] 혜택 추적

---

#### Task 4: 문서화 및 정리

**목표**: 리팩토링 결과 문서화

**작업 내역:**

1. 각 서비스별 README 작성
2. 아키텍처 다이어그램 업데이트
3. API 문서 업데이트

**예상 시간**: 1시간

---

## 🔍 리팩토링 체크리스트

### Service Layer

- [ ] 메서드가 2-3줄인가?
- [ ] 검증 로직이 없는가?
- [ ] DB 접근이 없는가?
- [ ] Reader/Manager만 호출하는가?

### Implementation Layer

- [ ] Reader는 조회만 하는가?
- [ ] Manager는 검증+로직+DB를 담당하는가?
- [ ] 파일명이 규칙을 따르는가?

### 테스트

- [ ] Service 테스트는 Mock을 사용하는가?
- [ ] Manager 테스트는 실제 로직을 검증하는가?

---

## 📝 다음 단계

1. **EntitlementService 리팩토링 시작**
2. **SubscriptionService 리팩토링**
3. **서비스 통합**
4. **전체 테스트 실행**

이 문서를 기반으로 단계별로 리팩토링을 진행하면 됩니다!

---

## 5️⃣ CancellationReasonService 리팩토링

### 현재 문제점

```typescript
// ❌ Service인데 단순 조회만 수행 (Reader 역할)
@Injectable()
export class CancellationReasonService {
  async getActiveReasons(): Promise<CancellationReason[]>;
  async getReasonByCode(code: string): Promise<CancellationReason | null>;
}
```

### 리팩토링 계획

#### 📁 파일 구조 변경

```
Before:
- services/cancellation-reason.service.ts

After:
- services/subscription/cancellation-reason.reader.ts
```

#### 🔧 cancellation-reason.reader.ts

```typescript
@Injectable()
export class CancellationReasonReader {
  /**
   * 활성화된 취소 이유 목록 조회
   */
  async findActiveReasons(): Promise<CancellationReason[]>;

  /**
   * 취소 이유 코드로 조회
   */
  async findByCode(code: string): Promise<CancellationReason | null>;
}
```

#### 🔧 SubscriptionCancellationService 통합

```typescript
@Injectable()
export class SubscriptionCancellationService {
  constructor(
    private readonly entitlementService: EntitlementService,
    private readonly contractReader: SubscriptionContractReader,
    private readonly cancellationManager: SubscriptionCancellationManager,
    private readonly reasonReader: CancellationReasonReader, // 추가
  ) {}

  /**
   * ✅ 흐름만 표현: "취소 이유 조회"
   */
  async getCancellationReasons() {
    return this.reasonReader.findActiveReasons();
  }
}
```

### 통합 이유

1. **역할 명확화**: Service가 아닌 Reader 역할
2. **도메인 응집도**: 구독 취소와 관련된 모든 기능을 한 곳에
3. **레이어 규칙 준수**: 단순 조회는 Reader로

---

## 📊 최종 서비스 구조

### After 리팩토링

```
apps/membership/src/services/
├── subscription-cancellation.service.ts  # 취소 오케스트레이션
├── subscription.service.ts               # 구독 오케스트레이션
├── entitlement.service.ts                # 권한 오케스트레이션
├── pause.service.ts                      # 일시정지 오케스트레이션
├── plan.service.ts                       # 플랜 오케스트레이션
│
├── subscription/
│   ├── subscription.creator.ts
│   ├── subscription.manager.ts
│   ├── subscription-contract.reader.ts
│   ├── subscription-cancellation.manager.ts
│   └── cancellation-reason.reader.ts     # 이동
│
├── entitlement/
│   ├── entitlement.reader.ts
│   └── entitlement.manager.ts
│
├── pause/
│   └── pause.manager.ts
│
└── plan/
    ├── plan.reader.ts
    └── plan.manager.ts
```

---

## 🎯 Task 상세 계획

### Task 1: RecurringBillingService 검증 로직 보완

#### 1-1. BillingReader 수정

**파일**: `apps/membership/src/services/billing/billing.reader.ts`

**변경 내용**:

```typescript
// Before
where(
  and(
    eq(schema.subscriptionContracts.isVoided, false),
    lte(schema.subscriptionContracts.nextBillingDate, date),
    or(
      eq(schema.subscriptionContracts.isPastDue, false),
      eq(schema.subscriptionContracts.isPastDue, true),
    ),
  ),
);

// After
where(
  and(
    eq(schema.subscriptionContracts.isVoided, false),
    eq(schema.subscriptionContracts.isPaused, false), // ✅ 추가
    lte(schema.subscriptionContracts.nextBillingDate, date),
    or(
      eq(schema.subscriptionContracts.isPastDue, false),
      eq(schema.subscriptionContracts.isPastDue, true),
    ),
  ),
);
```

**이유**: 일시정지된 구독은 결제 대상에서 제외해야 함

---

#### 1-2. BillingManager 수정

**파일**: `apps/membership/src/services/billing/billing.manager.ts`

**변경 내용**:

```typescript
// processSingleBilling() 메서드 내부

// 1. 플랜 정보 조회
const plan = await this.planService.getPlanDetails(contract.planId);
if (!plan) {
  throw new Error(`Plan not found: ${contract.planId}`);
}

// ✅ 추가: 플랜 활성화 상태 검증
if (!plan.plan.isActive) {
  throw new Error(`Plan is not active: ${contract.planId}`);
}
```

**이유**: 비활성화된 플랜으로는 결제를 진행하면 안 됨

---

#### 1-3. RecurringBillingService 에러 처리 개선

**파일**: `apps/membership/src/services/billing/recurring-billing.service.ts`

**변경 내용**:

```typescript
// processDueBillings() 메서드 내부

// 에러 타입별 분류 개선
let errorCode = 'UNKNOWN_ERROR';
if (error.message.includes('Cannot connect to Wallet server')) {
  errorCode = 'WALLET_CONNECTION_ERROR';
} else if (error.message.includes('Payment intent not found')) {
  errorCode = 'PAYMENT_INTENT_ERROR';
} else if (error.message.includes('No active payment profile')) {
  errorCode = 'NO_PAYMENT_PROFILE';
} else if (error.message.includes('Plan not found')) {
  errorCode = 'PLAN_NOT_FOUND';
} else if (error.message.includes('Plan is not active')) {
  // ✅ 추가
  errorCode = 'PLAN_NOT_ACTIVE';
}
```

**이유**: 에러 원인을 명확히 분류하여 모니터링 및 디버깅 용이

---

### Task 2: 스키마 확인

#### 2-1. subscriptionContracts 테이블 확인

**확인 사항**:

- `isPaused` 컬럼이 존재하는지 확인
- 존재하지 않으면 마이그레이션 필요

**파일**: `apps/membership/src/shared/schemas/entities/schema.ts`

---

### Task 3: 테스트 작성

#### 3-1. BillingReader 테스트

**파일**: `apps/membership/src/services/billing/__tests__/billing.reader.spec.ts`

**테스트 케이스**:

```typescript
describe('BillingReader', () => {
  describe('findDueContracts', () => {
    it('일시정지된 구독은 제외해야 함', async () => {
      // Given: 일시정지된 구독
      // When: findDueContracts 호출
      // Then: 결과에 포함되지 않음
    });

    it('무효화된 구독은 제외해야 함', async () => {
      // Given: 무효화된 구독
      // When: findDueContracts 호출
      // Then: 결과에 포함되지 않음
    });

    it('연체 상태 구독은 포함해야 함', async () => {
      // Given: 연체 상태 구독
      // When: findDueContracts 호출
      // Then: 결과에 포함됨
    });
  });
});
```

---

#### 3-2. BillingManager 테스트

**파일**: `apps/membership/src/services/billing/__tests__/billing.manager.spec.ts`

**테스트 케이스**:

```typescript
describe('BillingManager', () => {
  describe('processSingleBilling', () => {
    it('비활성화된 플랜은 결제 실패해야 함', async () => {
      // Given: 비활성화된 플랜
      // When: processSingleBilling 호출
      // Then: 'Plan is not active' 에러 발생
    });

    it('결제 프로필이 없으면 기본 프로필 조회해야 함', async () => {
      // Given: paymentProfileId가 null인 계약
      // When: processSingleBilling 호출
      // Then: getDefaultPaymentProfile 호출됨
    });
  });
});
```

---

### Task 4: 통합 테스트

#### 4-1. 정기결제 통합 테스트

**파일**: `apps/membership/test/integration/recurring-billing.integration.spec.ts`

**시나리오**:

1. 정상 결제 플로우
2. 결제 실패 → Dunning 큐 추가
3. 재시도 성공
4. 최대 재시도 초과
5. 일시정지 중 결제 스킵

---

## 📊 작업 우선순위

### 우선순위 1 (즉시 작업)

- [ ] Task 1-1: BillingReader isPaused 조건 추가
- [ ] Task 1-2: BillingManager 플랜 활성화 검증 추가
- [ ] Task 2-1: 스키마 확인 (isPaused 컬럼)

### 우선순위 2 (검증)

- [ ] Task 1-3: 에러 처리 개선
- [ ] Task 3-1: BillingReader 테스트
- [ ] Task 3-2: BillingManager 테스트

### 우선순위 3 (통합)

- [ ] Task 4-1: 정기결제 통합 테스트
- [ ] 전체 서비스 통합 테스트 실행

---

## 🎉 리팩토링 완료 후 기대 효과

### 코드 품질

- ✅ 레이어 아키텍처 규칙 준수
- ✅ 단일 책임 원칙 (SRP) 적용
- ✅ 테스트 용이성 향상

### 유지보수성

- ✅ 비즈니스 로직과 구현 분리
- ✅ 코드 재사용성 증가
- ✅ 변경 영향 범위 최소화

### 개발 생산성

- ✅ 새로운 기능 추가 용이
- ✅ 버그 수정 시간 단축
- ✅ 코드 이해도 향상
